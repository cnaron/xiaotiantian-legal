# RECEIPT · X122 颗粒 10 —— 法律站权限表 6 → 5(麦克风整条下线)

> 仓库 `cnaron/xiaotiantian-legal`(**公开**),分支 `main`,起点 `ece09cf`(X123 颗粒 7)。
> 起因:X121 颗粒 18(App build 117,已 OTA)把麦克风**彻底删掉**,不是关掉。

## 简短大白话

**手机 App 上个版本已经把麦克风整个删了,今天把法律网页上还写着的那一条也删了。**

1. **隐私政策原来列了 6 条权限,现在是 5 条。** 删掉的是「麦克风权限」整条 ——
   标题、用途、说明三段一起走,不是改小、不是加个「已停用」。
2. **原来写着「视频里会录进绳子声、加油声、旁边的人说话」,这句现在是假话,已经删掉。**
   改成一句实话:视频**只有画面,不含声音**。隐私政策和支持页两处都改了。
3. **支持页那条常见问题「录像会录到声音吗?」问题保留,答案整个反过来** ——
   原来是「会」,现在是「不会」。用户还是会问这个问题,把问题也删掉反而更奇怪。
4. **「设置 → 隐私与安全性 → 相机 / 麦克风 / 照片 / 健康」这行路径去掉了麦克风** ——
   现在系统设置里根本没有这一项,留着会让人白找。
5. **目录页上写的「6 项系统权限」改成「5 项」**,隐私政策页顶的版本日期改成 2026年9月9日。
6. **两个站都是新的了**:主站(Cloudflare)和备份站(GitHub Pages)四张页面**逐字节相同**,
   八张页面里「麦克风」一个字都搜不到(改之前是 7 处)。用户协议**一个字节没动**
   (两张截图**逐像素 100.00% 相同**)。

**对产品意味着什么**:纸面(隐私政策)和实物(App)重新对上了。之前是**政策说采集声音、
App 却已经不采集** —— 这个方向不违规但会让人以为我们在采集;现在两边一致。
下次提审时,**ASC 后台的权限用途说明里也要把麦克风那条删掉**(不在本颗粒范围,见 §7)。

---

# 1. 改了什么(7 处 + 1 处配置)

| # | 页 | 位置 | 改动 |
|---|---|---|---|
| P1 | privacy | 页顶 `meta-info` | 版本更新日期 / 版本生效日期 **2026年9月8日 → 2026年9月9日**(README 约定:改权限必须同步) |
| P2 | privacy | §一.2 本地数据「回看视频」 | 「一条**带画面与声音**的视频」→「一条视频(**只有画面,不含声音**)」 |
| P3 | privacy | §三 设备权限调用 | **整个 `<li>` 删除**(5 行):`<strong>麦克风权限</strong>` + 用途段 + 说明段 |
| P4 | privacy | §六 您的权利 · 撤回权限 | 路径列表 `相机 / 麦克风 / 照片 / 健康` → `相机 / 照片 / 健康` |
| S1 | support | 常见问题 4 | 问题保留,答案由「会。授权麦克风后……」整句换成「**不会。**视频里**只有画面,不含声音** —— 本 App 不会录制任何声音。」 |
| I1 | index | 隐私政策入口描述 | 「**6 项**系统权限各自用途」→「**5 项**」;「2026年9月8日生效」→「9月9日」 |
| — | terms | — | **0 处**(查过为空,不是没查;§3.4 有逐像素证据) |
| C1 | `content/pages.json` | index / support 的 `版本更新日期` | 2026年9月8日 → 2026年9月9日(这两页本轮正文有改动;terms 未改,**故意不动**) |

改法与颗粒 9 同构:`tools/edits_g10_claudecode_20260909.py`,每条 = (文件, 唯一行锚点, 新整行),
**锚点命中数必须恰好 1**,否则报错退出;删整块的那条另外断言「包住它的 `<li>…</li>` 恰好 5 行」。
可从 KV 基线重放:`python3 tools/edits_g10_claudecode_20260909.py --dry --src /Users/cc/x122/g10/baseline`。

## 1.1 改后的 5 条权限 = App build 117 包里的 5 条

| 隐私政策条目 | build 117 包内权限串(RECEIPT-X121-G18 §2 实测) |
|---|---|
| 相机权限 | `Camera` |
| 相册读取权限 | `PhotoLibrary` |
| 相册写入权限 | `PhotoLibraryAdd` |
| Apple 健康(HealthKit)写入权限 | `HealthUpdate` |
| Apple 健康(HealthKit)读取声明 | `HealthShare` |
| ~~麦克风权限~~ | ~~`Microphone`~~ —— **包里命中 0 处**(同一把尺子量 build 116 命中 1) |

线上页面实测条目数 **= 5**(从 `<h3>3. 设备权限调用</h3>` 那个 `<ul>` 里数 `<li><strong>`)。

---

# 2. 闸账(绿 / 红 / **没跑**,三态)

| 闸 | 结果 | 数字 |
|---|:--:|---|
| **G10-BASE** 不覆盖 owner 改动 | 🟢 绿 | 开跑前 `GET /api/export` 取基线,四页 sha 与仓库 `content/`(rstrip 尾换行)**逐字节相同** ⇒ 颗粒 9 之后 owner 没在 `/edit` 上改过东西。基线仍以 **KV 为准**,不以仓库为准(§3.1) |
| **G10-MIC/KV** | 🟢 绿 | KV 四页正文 `麦克风` **7 → 0** |
| **G10-MIC/HTML** | 🟢 绿 | 线上渲染四页 `麦克风` **7 → 0** |
| **G10-MIC/EDIT** | 🟢 绿 | 编辑器四处(`/edit` + `/{privacy,terms,support}/edit`)`麦克风` **7 → 0** |
| **G10-MIC·阴性对照** | 🟢 绿 | 同一把尺子在改动**前**三个面各读到 **7** ⇒ 这个 0 不是「尺子瞎了」(§3.2) |
| **G10-录音·判别力不足** | 🟡 **黄** | `录音` 改前就是 **0**,改后仍 0 ⇒ 这半把尺子**本轮没有判别力**,不当证据用(诚实登记,§6①) |
| **G10-SOUND 方向** | 🟢 绿 | `声音` **8 → 4**,且**剩下的 4 处全部是「没有声音」这个方向**(逐行核过:privacy 1 处「只有画面,不含声音」;support 3 处 = 问题标题 1 + 答案 2) |
| **G10-PERM5** | 🟢 绿 | 线上 privacy §三 权限条目 **6 → 5**,逐条与 build 117 包内权限串一一对应(§1.1) |
| **G10-COUNT** | 🟢 绿 | 目录页 `5 项系统权限` = 1 处 / `6 项系统权限` = **0** 处 |
| **G10-DIFF** 只改预期行 | 🟢 绿 | 变更行 index **2** / privacy **13** / support **2** / terms **0**,逐行贴在 §4,全部落在 §1 清单内 |
| **G10-SECTION** 无关段落 sha 不变 | 🟢 绿 | privacy 9 个 `<section>`:**只有 §一、§六 变**,其余 7 个 sha256 逐字节相同;`<head>`(整套内联 CSS)sha `32ff2f7d04189495` **改前改后相同** |
| **G10-STRUCT** | 🟢 绿 | privacy `<li>`/`</li>` 各 38 → 37(**净减 1 个,配平**);`<ul>`/`<section>`/`<style>` 计数一个没变 |
| **G8 行距/结构闸复跑** | 🟢 绿 | `gate_g8.mjs` **20/20**,含 `G8-METRIC` privacy/terms 与 support **7 项排版指标全等**(body lh=28.799999px)+ 6 条真阴性对照 ⇒ 行高/样式**没有回归** |
| **G6 缓存闸复跑** | 🟢 绿 | `gate_g6.mjs` **15/15**,含 4 条阴性对照 |
| **G5 文案三闸** | 🟢 绿 | NOSITE / PRICE / TECH **全 0 命中**(豁免仍为空) |
| **邮箱尺子(颗粒 7)** | 🟢 绿 | 八页 `@126.com` / `@qq.com` / `邮箱` 命中 **0**(由 `G8-MAIL` 实跑) |
| **时效话术(颗粒 8)** | 🟢 绿 | `14 个工作日内` 仍恰好 **3 处**(反方向:必须不为 0 的东西没被误删) |
| **实现细节尺子(颗粒 9)** | 🟢 绿 | `Cloudflare / GitHub / 工单 / 发送记录` 四页命中 **0** |
| **渲染器单测 + 双端同源** | 🟢 绿 | `test_render.mjs` **46 passed / 0 failed**;`build.py` 三项自检过(渲染器三副本 sha 一致 / class 白名单 / Function 侧组装 == `docs/*.html` 逐字节) |
| **G10-LIVE 两地可达** | 🟢 绿 | 主站四页 200 且与 `docs/` 逐字节;**备份站四页也已追平**并与 `docs/` 逐字节(§3.5) |
| **G10-SHOT 截图** | 🟢 绿 | terms 改前/改后 **PNG 逐字节相同**(逐像素 100.00%、diffbox=None);privacy −362px / support −56px / index 99.77% 且差异只在一个框内(§3.4) |
| **KV 收尾** | 🟢 绿 | 两套老闸都会往线上真写一次再改回;跑完再导一次 KV,四页 sha 与本轮交付版**完全一致**,闸没把改动搅掉 |
| **ASC 后台权限用途说明** | ⚪ **没跑** | 本轮只改网页;App Store Connect 后台那份还写着麦克风,**下次提审时删**(§7①) |
| **App 内「联系我们」页文案** | ⚪ **没跑** | 与颗粒 9 同,仍是老债 |

---

# 3. 实测数字

## 3.1 G10-BASE(生产 `/api/export`,四页 `from=kv`)

```
                改前(基线 04:20:24Z)                     改后(线上)
index      1304B sha=5440445fa4539097        →     1304B sha=a8ed8601257ac18d
privacy   23547B sha=2ea1d4a0cf0a7d26        →    22978B sha=bf435c220990757b   (−569B)
terms     21015B sha=f2364b4978f2fbc6        →    21015B sha=f2364b4978f2fbc6   ← 一个字节没动
support    6882B sha=7b0b27040de22afc        →     6732B sha=6913c398271f742e   (−150B)
```

`/api/save` 三次回执全 `ok=true`(privacy `ts=1788927894868` / support `1788927898805` /
index `1788927902790`;terms 没改所以没保存)。保存后重新导出,**四页与仓库 `content/` 逐字节相同**。
两套老闸跑完**又导了第三次**,四页 sha 与上表右列**完全一致**。

## 3.2 G10-MIC 三个面 × 改前改后(阴性对照在同一张表里)

| 尺串 | KV 正文(4 页) | 线上渲染 HTML(4 页) | 编辑器(4 处) |
|---|---|---|---|
| `麦克风` | **7 → 0** | **7 → 0** | **7 → 0** |
| `录音` | 0 → 0 | 0 → 0 | 0 → 0 |
| `声音` | 8 → **4** | 8 → **4** | 8 → **4** |

改前逐页(KV):privacy `麦克风`=4 / `声音`=5,support `麦克风`=3 / `声音`=3,index 与 terms 全 0。
改后逐页(线上):privacy `声音`=1,support `声音`=3,index / terms 全 0。

**这就是阴性对照**:同一条 `grep` 命令、同样四个文件、同样三个面,改动**前**读到 7,
改动**后**读到 0 ⇒ 那个 0 是真的删干净了,不是尺子看不见。
⚠️ `录音` 那一行**两头都是 0**,本轮**不构成证据**(§6①)。

## 3.3 剩下的 4 处「声音」逐句(全部是「没有声音」方向)

```
privacy:136  …每局跳绳生成的一条视频(<strong>只有画面,不含声音</strong>)…
support:165  <h3>4. 录像会录到声音吗?</h3>
support:166  <strong>不会。</strong>视频里<strong>只有画面,不含声音</strong> —— 本 App 不会录制任何声音。
```

脚本自证 ② 把这条钉成硬闸:任何含「声音」的行都必须落在白名单
`['只有画面,不含声音', '录像会录到声音吗']` 里,否则报错退出。

## 3.4 G10-SHOT(mini 上 `tools/shot`,900pt 宽 @2x)

改前 = 改动前抓下来的线上 HTML,经**本机 HTTP 服务**渲染;改后 = 取**线上**页面(带 cache-buster)。

| 页 | 改前高 | 改后高 | 逐像素 |
|---|---|---|---|
| index | 2000 | 2000 | **99.77% 相同**,差异只在一个框内 `(502,247)–(1570,913)` = 页顶日期 + 被改的那个入口 |
| privacy | 10488 | 10126 | 尺寸不同(**−362 px**)⇒ 麦克风那一条真的没了 |
| support | 5540 | 5484 | 尺寸不同(−56 px) |
| **terms** | 9342 | 9342 | **PNG 逐字节相同**(逐像素 100.00%,diffbox=None) |

terms 那行同时是三件事的证据:①terms 真的 0 改动(字节 + 像素双证);
②「改前」(本机 HTTP)与「改后」(线上 Cloudflare)两条渲染链路**可比**;
③截图工具**没吃缓存**(同一次运行里它给 privacy/support 画的是新版)。

产物在 mini `/Users/cc/x122/g10/shots/`,已 scp 到 Air `~/Downloads/X122-G10/`(mini 的 `~/Downloads` 未留副本)。

## 3.5 G10-LIVE 两地

```
主站 Cloudflare(无 .html,零跳转)          备份站 GitHub Pages(带 .html)
  index    200   4935B                      index    200   4935B
  privacy  200  22979B                      privacy  200  22979B
  terms    200  21016B                      terms    200  21016B
  support  200  10381B                      support  200  10381B
八页与本地 docs/ 逐字节:全部 OK   ·   八页 `麦克风` 合计 0(改前 14)
```

部署 id **`f30f85be`** — <https://f30f85be.xiaotiantian-app.pages.dev>(上传 3 个文件 / 106 个已存在)。

⚠️ **备份站的实际情况与任务书的假设不一样,照实记**:任务书写「备份站不自动同步,本颗粒不动它」。
我**没有单独去发布备份站**,但按仓库 git 纪律 `git push origin main` 之后,
**GitHub Pages 从 `/docs` 目录自动构建**,备份站自己就追平了(上表实测,与 `docs/` 逐字节相同)。
⇒ 结论不是「我违规动了它」,而是「**它本来就跟着 push 走**」——「不自动同步」那句说的是
**KV 里的编辑不会回流仓库**(颗粒 3 登记的那件事),不是「push 了也不动」。
这个方向是对的:备份站要是停在旧版,就会出现一个还写着麦克风的法律页面挂在公网上。

---

# 4. 逐句改动表(由真实 diff 生成)

## privacy(13 行:4 处改 = 4 旧 + 4 新,1 处整块删 = 5 旧)

```
100,101c100,101
<             <p>版本更新日期:2026年9月8日</p>
<             <p>版本生效日期:2026年9月8日</p>
---
>             <p>版本更新日期:2026年9月9日</p>
>             <p>版本生效日期:2026年9月9日</p>

136c136
<   <li><strong>回看视频:</strong>每局跳绳生成的一条带画面与声音的视频,<strong>固定只保留最新的一局</strong>,…
---
>   <li><strong>回看视频:</strong>每局跳绳生成的一条视频(<strong>只有画面,不含声音</strong>),<strong>固定只保留最新的一局</strong>,…

150,154d149            ← 麦克风整块(diff 的对齐把「</li> + 下一个 <li>」算作被删的那两行,净效果等价)
<     <strong>麦克风权限</strong>
<     <p class="mt-2 ml-4"><strong>用途:</strong>把跳绳现场的声音录进您的视频。</p>
<     <p class="mt-2 ml-4"><strong>说明:</strong>绳子的声音、您的加油声,<strong>以及旁边的人说话</strong>,都会被录进这条视频。声音只存在本机,不会上传。不想录可以在 iOS「设置 → 隐私与安全性 → 麦克风」里关掉本应用的麦克风权限,视频照常生成,只是没有声音。</p>
<   </li>
<   <li>

228c223
<   <li><strong>撤回权限:</strong>在 iOS「设置 → 隐私与安全性 → 相机 / 麦克风 / 照片 / 健康 → 小天天练跳绳」可随时关闭已授予的权限。</li>
---
>   <li><strong>撤回权限:</strong>在 iOS「设置 → 隐私与安全性 → 相机 / 照片 / 健康 → 小天天练跳绳」可随时关闭已授予的权限。</li>
```

## support(2 行)

```
21c21
<   <p>会。授权麦克风后,绳子的声音、加油声,以及 <strong>旁边的人说话</strong> 都会被录进去。不想录可以在 iOS「设置 → 隐私与安全性 → 麦克风」里关闭本 App 的麦克风权限,视频照常生成,只是没有声音。</p>
---
>   <p><strong>不会。</strong>视频里<strong>只有画面,不含声音</strong> —— 本 App 不会录制任何声音。</p>
```

## index(2 行)

```
10c10
<   …<span>收集什么、不收集什么、6 项系统权限各自用途、跳绳视频存在哪里、「联系我们」会发送什么 · 2026年9月8日生效</span>…
---
>   …<span>收集什么、不收集什么、5 项系统权限各自用途、跳绳视频存在哪里、「联系我们」会发送什么 · 2026年9月9日生效</span>…
```

## terms:**0 行**

## privacy 分节 sha(9 节,只有 2 节该变)

```
  §0 引言                   98f9d1deba1e  SAME
  §1 一、我们收集的信息        906f1e941a46  CHANGED   ← P2(回看视频)+ P3(权限表)
  §2 二、第三方 SDK 集成情况   f71d32bf486f  SAME
  §3 三、信息的存储与保护       ba4cae3dfdb2  SAME
  §4 四、购买与支付            bfc290a09ea5  SAME
  §5 五、未成年人保护          06487ec67aa1  SAME
  §6 六、您的权利             4badd4fc9a77  CHANGED   ← P4(撤回权限)
  §7 七、本政策的修订          6be136bd5931  SAME
  §8 八、如何联系我们          806093df5a7f  SAME
  <head>(整套内联 CSS)      32ff2f7d04189495  SAME(改前 == 改后)
```

---

# 5. 脚本自证(9 条,dry run 与真跑都过)

`tools/edits_g10_claudecode_20260909.py` 在写盘**之前**跑这 9 条,任一不过就 `AssertionError` 退出:

| # | 内容 |
|---|---|
| ① | 四页 `麦克风` / `录音` 合计 **0** |
| ② | 残留的每一处「声音」都必须落在白名单(= 只能是「没有声音」这个方向) |
| ③ | privacy 权限标题恰好 **5** 条,且就是 build 117 那 5 条 |
| ④ | 目录页 `5 项系统权限` = 1、`6 项系统权限` = 0 |
| ⑤ | 邮箱尺子(颗粒 7)仍 0 |
| ⑥ | 时效话术(颗粒 8)仍恰好 3 处 —— **必须不为 0** 的反方向 |
| ⑦ | 实现细节尺子(颗粒 9)仍 0 |
| ⑧ | `terms.html` 根本没进过写缓冲(与「0 处改动」的登记对撞) |
| ⑨ | **本轮新写的文案**不许有半角 `,` `;`(owner 全角令) |

★ **闸当场逮到我两次**(都在 dry run 阶段,没进线上):
1. 我给 support 写的新答案里带了「本 App **不申请麦克风权限**」—— 自证 ① 立刻红:
   `support.html: 违禁词 {'麦克风': 1}`。想说的意思是对的,但**尺子是「麦克风 = 0」**,
   一句解释性的「不申请麦克风」也会让尺子非 0 ⇒ 改成不点名的「本 App 不会录制任何声音」。
   **教训**:「删除某个词」类任务里,连「我们不做 X」这种否定句也会撞自己的尺子,写之前先想清楚
   尺子量的是**词**还是**事实**。
2. 自证 ⑨ 第一版写成扫整页,当场红在**页面里既有的 Tailwind CSS 注释**上(`v3.4.17`,半角逗号)。
   **红的是尺子不是内容**:owner 的全角令管的是**用户看得见的文案**,不是 CSS 和 HTML 注释。
   改成只量**本轮新写的那几行**(也正好对上 owner「改到哪屏统一哪屏,不做全仓」的原话)。

---

# 6. 诚实边界(没验到 / 判别力不足的)

1. ⚠️ **`录音` 这半把尺子本轮没有判别力。** 任务书要求「`麦克风|录音` 改前 >0 改后 =0」,
   实测 `录音` **改前就是 0** —— 四页里从来没写过这两个字。它改后仍是 0 这件事
   **不能拿来当「删干净了」的证据**,真正承重的是 `麦克风` 的 7 → 0。照实登记。
2. **ASC(App Store Connect)后台的权限用途说明没看也没改。** 那不在这个仓库里,
   本轮一个字都没动它。它还写着麦克风(见 §7①)。
3. **App 内那份「隐私政策」入口指向的是不是这个站,本轮没验。** X122 颗粒 1 登记过
   「App 内链接与 ASC 后台仍未指向新站」,这条债**还挂着**,本轮没碰。
   ⇒ 也就是说:**这次改好的页面,用户从 App 里点进去看到的可能还不是它**。
4. **「隐私政策现在与 App 完全一致」这句话我只验到麦克风这一条。** 其余 5 条权限、
   视频留存、价格档位等是颗粒 5 逐条核过的,本轮**没有重新逐条复核**。
5. **没有用真机打开页面看过。** 证据是 curl 字节 + WKWebView 离屏截图,
   没在 iPhone Safari 上人眼确认过排版(G8-METRIC 的 7 项指标是数值证据,不是人眼)。
6. **`声音` 剩 4 处是我判定「方向正确」,不是零。** 如果 owner 希望法律页上一个「声音」
   都不出现(连「不含声音」也不写),那要再改一轮 —— 但那样支持页就答不了「会不会录到声音」这个问题了。
7. **KV 的历史版本里仍留着带麦克风的旧正文**(`/api/save` 每次存一版,最多 20 版)。
   这是编辑器的回滚能力,不是对外页面;但「线上彻底没有麦克风」这句话严格说只覆盖**当前正文**。

---

# 7. 交给下一颗粒的

1. ★ **ASC 后台权限用途说明删掉麦克风** —— X121 颗粒 18 §6 已登记「下次提审时同步删」。
   **提审前必须做**,否则会出现「审核材料申报了麦克风、包里没有」的反向不一致。
2. **App 内「隐私政策 / 用户协议」链接与 ASC 的两个 URL 是否已指向 `xiaotiantian-app.pages.dev`** ——
   X122 颗粒 1 起挂着,连欠多轮,建议下一轮直接结掉。
3. **App 内「联系我们」页与 ASC 入口文案是否统一成「小天天 → 联系我们」** —— 颗粒 7 §8③ 老债。
4. **App 一旦接上「反馈带截图」,privacy 与 support 必须同步补一句** —— 颗粒 9 §7① 原样转下去
   (X123 颗粒 6 已把服务端图片能力整个撤掉 ⇒ 这条**目前不成立**,等 App/服务端哪天再开图片能力时再看)。

---

# 8. 复跑方式

```bash
cd /Users/cc/Public/x84sb/legal-site

# ① 从 KV 基线重放这 7 处(基线目录 = 本轮 04:20:24Z 那次 /api/export)
python3 tools/edits_g10_claudecode_20260909.py --dry --src /Users/cc/x122/g10/baseline

# ② 对仓库 content/ 就地改(默认)
python3 tools/edits_g10_claudecode_20260909.py

# ③ 重建 docs/ + 三条文案闸 + 渲染器单测
python3 build.py && python3 tools/gates_g5.py docs && node tools/test_render.mjs

# ④ 两套老闸复跑(会真往线上写一次再改回去)
EDIT_PASSPHRASE=<口令> node tools/gate_g8.mjs     # 行距/层叠/结构 20 条
EDIT_PASSPHRASE=<口令> node tools/gate_g6.mjs     # 缓存/一词一改 15 条

# ⑤ G10 尺子(三个面各扫一遍)
for w in 麦克风 录音 声音; do printf "%-8s %s\n" "$w" \
  "$(cat docs/index.html docs/privacy.html docs/terms.html docs/support.html | grep -o "$w" | wc -l | tr -d ' ')"; done
```

口令在 Air `~/Downloads/xiaotiantian-legal-site/EDIT-PASSPHRASE.txt`(**不进本仓库**)。
基线 / 改前改后 HTML / 截图产物在 mini `/Users/cc/x122/g10/`,截图另存 Air `~/Downloads/X122-G10/`。
