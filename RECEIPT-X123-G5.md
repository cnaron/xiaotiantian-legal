# X123 颗粒 5 · 工单支持图片(R2 存图 + 上传接口 + issue 正文嵌图)· 回执

> 状态:**已上线并端到端跑通**,2026-09-08 16:05 北京时间。
> 真工单自证:`cnaron/rope-counter#5`(本轮唯一一个真 issue,收工时已 close + 打 `test` 标签)。
> owner 裁定的前提:**App 端必须压缩 · 国内用户为主 · 不做视频 · 不改隐私/权限文案**(都遵守了,§7④)。
>
> ⚠️ **这个仓库是公开仓库(`cnaron/xiaotiantian-legal`,PUBLIC)。**
> 任何口令、key、secret 一律不写进来。需要口令的交接项在 mini 本地 `/Users/cc/x123/DONE-G5`(不入 git)。

## 大白话解释

**做了什么。** 用户在 App 里点「联系我们」写问题的时候,现在可以**带截图**了。图不放在
GitHub 上(GitHub 的接口没有「上传附件」这一说),而是放进 Cloudflare 自己的文件仓库
(叫 R2,免费额度 10 GB —— 我们一张图最多 1 MB,存 90 天自动删,用满也就几十兆),
然后在 GitHub 的工单帖子里写一行「这里有张图,地址是 ⋯」,GitHub 自己会去把图取过来显示。
owner 在 GitHub 上看工单,就能直接看见用户截的图。

**先量了再做。** 任务书要求先量「国内上传一张图要多久」,量出来的结论有点扎心:
**国内到 Cloudflare 的上传大约只有 100 KB/s**,而且它把我们的请求扔到**洛杉矶**去了
(不是香港也不是日本)。所以:传 1 MB 要 **9.8 秒**(最慢 13.5 秒),传 300 KB 要 **3.6 秒**,
传 100 KB 要 **2.4 秒**。同一件事从首尔的机器上做只要 **0.7 秒**,差 14 倍。
**这条链路我们改不了**——所谓「用 Worker 中转」是没用的,因为 Worker 本身就跑在那个边缘上,
慢的是「用户到边缘」这一段。**唯一的办法是让图更小。**

**还量掉了一个想当然。** 本来打算建议 App「四张图同时传,快一些」。实测**并不快**:
4 张 300 KB 串行 20.0 秒、并行 16.9 秒,差别小到淹没在这条链路自己的抖动里(串行的抖动
就有 ±5.1 秒),而且并行那 24 次里**有 2 次直接传失败了**(串行 24 次一次没失败)。
所以建议改成:**老老实实一张一张传,失败了重试,给用户看进度条**。

**顺手把服务端自己的耗时砍掉了一大半。** 量的时候发现:同样一张 98 KB 的图,打一个
「读完就扔」的探针要 1.9 秒,打真正的上传接口要 3.6 秒 —— 中间**1.6 秒是服务端自己
在记账**(记流水、记配额)。改成「先把 URL 给用户,记账在后台慢慢做」之后,在首尔边缘上
**12 对样本 12 对都变快,合计快了 1.2 秒**。

**踩到一个坑,当场修了。** 第一版让「嵌图」这个动作和「用户回复」共用一个
「5 次 / 10 分钟」的额度。结果是:用户发一条带图的、再追问两句带图的,光嵌图就用掉 3 次,
**第三句话直接被系统挡住发不出去**。线上当场撞出来了,改成各用各的额度,再测就通了。

**对产品意味着什么。** ① 工单能带图了,排查问题不用再靠用户文字描述;
② **App 那边必须把图压到 300 KB 以内**(长边 1280、JPEG 质量 0.7 左右),否则国内用户
要盯着进度条等十几秒,很可能直接退出去;③ 有一件事要 owner 拍板:图片一旦贴进 GitHub,
GitHub 会把它缓存到自己的图片服务器上,**拿到那个网址的人就能看图**(网址是 128 位随机数,
猜不出来,但不是「私有仓库」那种强度)。详见 §7①。

---

# 1. 上线了什么

Cloudflare Pages 项目 `xiaotiantian-app`(法律页 + 工单后端那个,同一个项目):

| 文件 | 作用 |
|---|---|
| `functions/api/feedback/upload.js` | **新** · 收一张已压缩的 JPEG,存进 R2,回一个公网可读的 URL |
| `functions/api/feedback/attach.js` | **新** · 把已上传的图嵌进工单的首帖或某条评论 |
| `functions/api/feedback/img/[[key]].js` | **新** · 从 R2 把图读出来(GitHub 的图片代理来取的就是这条) |
| `functions/_lib/images.js` | **新** · 键名规矩 / 归属校验 / markdown 嵌入与剥离 / JPEG 魔数 / 每日配额 |
| `functions/api/feedback/contact.js` | 改:返回里多一个 `ticket.cid`(纯新增字段) |
| `functions/api/feedback/thread.js` | 改:每条消息多一个 `images: []`(纯新增字段) |
| `tools/test_feedback.mjs` | 单测 **70 → 95** 条(新增 25 条,其中 12 条是阴性/阳性对照) |
| `wrangler.toml` | 加 R2 绑定 `FEEDBACK_IMG` → 桶 `xiaotiantian-feedback` |
| `functions/api/feedback/netprobe.js` | **临时**测速端点,§2 量完**已删**(见 §7⑤) |

**没动**:法律页四页、`/edit` 编辑器、样式表、字体、缓存口径、`build.py`、`docs/`、
`contact` / `thread` / `reply` 的**请求体字段与 HTTP 码**、GitHub App 的权限。

# 2. ★ 先量再做:国内上传到底有多慢

## 2.1 三个观测点(每格 10 次,随机不可压缩的字节)

| 观测点 | 落到哪个 Cloudflare 边缘 | 方向 | 大小 | p50 | p90 | p95 | max | 失败 |
|---|---|---|---|---|---|---|---|---|
| **国内**(`ssh appserver`,腾讯云) | **LAX(洛杉矶)** | 上传 | 300 KB | **3.58s** | 5.04 | **5.07** | 5.10 | 0/10 |
| **国内** | LAX | 上传 | **1 MB** | **9.80s** | 10.29 | **11.90** | 13.52 | 0/10 |
| **国内** | LAX | 下载 | 200 KB | 1.19s | 1.45 | 1.46 | 1.47 | 0/10 |
| mini(经 Surge TUN → 首尔) | ICN | 上传 | 300 KB | 2.55s | 3.37 | 3.53 | 3.69 | 0/10 |
| mini | ICN | 上传 | 1 MB | 2.37s | 3.36 | 3.92 | 4.48 | 0/10 |
| **海外**(首尔 VPS,直连) | ICN | 上传 | 300 KB | **0.69s** | 0.82 | 0.90 | 0.99 | 0/10 |
| **海外** | ICN | 上传 | 1 MB | **0.68s** | 0.76 | 0.76 | 0.77 | 0/10 |
| 海外 | ICN | 下载 | 200 KB | 0.06s | 0.07 | 0.07 | 0.08 | 0/10 |

**★ 任务书要的那个数:国内上传 1 MB,p50 = 9.80s,p95 = 11.90s。**
同样 1 MB 在海外是 0.68s ⇒ **14.4 倍**。

三件事值得单独点出来:

1. **国内的请求被路由到了洛杉矶(LAX),不是香港/东京。** `pages.dev` 在中国大陆没有
   加速节点,这一跳是跨太平洋的。
2. **下载比上传快得多**(200 KB 下载 1.19s ≈ 168 kB/s,而上传 ≈ 100 kB/s)——
   这条链路是**不对称**的,「看图」不是问题,「传图」才是。
3. **一次也没失败**(30/30 全 200)。慢,但不是断。

## 2.2 图压到多小才划算(国内,每格 8 次)

| 大小 | p50 | p90 | p95 | max | p50 对应吞吐 |
|---|---|---|---|---|---|
| 50 KB | 1.64s | 2.89 | 3.24 | 3.60 | 31 kB/s |
| 100 KB | 2.44s | 6.10 | 8.95 | 11.80 | 42 kB/s |
| 200 KB | 4.00s | 8.33 | 8.41 | 8.49 | 51 kB/s |
| 300 KB | 5.01s | 11.54 | 16.19 | 20.85 | 61 kB/s |
| 500 KB | 4.98s | 5.54 | 5.66 | 5.79 | 103 kB/s |
| 1 MB | 9.82s | 10.57 | 10.84 | 11.11 | 107 kB/s |

对 p50 做线性拟合:**耗时 ≈ 1.84 秒(固定开销)+ 字节 ÷ 132 kB/s**。
所以「压得再小也省不掉那 1.8 秒」——**压到 100 KB 以下收益就开始递减**,
从 300 KB 压到 100 KB 省 2.6 秒,从 100 KB 再压到 50 KB 只省 0.8 秒。

⚠️ 这张表**跑法有缺陷**:六个尺寸是**先后**跑的,不是交替跑的,所以「尺寸」和「跑的时刻」
纠缠在一起 —— 300 KB 那行 p95 = 16.19s 比 500 KB 那行的 5.66s 还大,这不可能是尺寸造成的,
是那段时间链路正好在抖。**只有 p50 这一列可以当趋势看,p90/p95 那几列不要拿来比大小。**
真要下结论用 §2.1 的表(那三格是同一批、每格 10 次)。

## 2.3 ★ 「四张图并行传」实测**没用**,而且更容易失败

从国内跑 6 轮,每轮先串行传 4 张 300 KB,再并行传 4 张 300 KB(同一批文件、同一时间窗):

| | 6 轮墙钟(秒) | 中位 | 均值 | 标准差 | 失败 |
|---|---|---|---|---|---|
| 串行 | 15.2 / 14.9 / 20.3 / 28.6 / 18.9 / 22.2 | 19.6s | 20.0s | 5.1 | **0 / 24 次请求** |
| 并行 | 15.5 / 18.3 / 12.4 / 16.5 / 20.8 / 18.0 | 17.2s | 16.9s | 2.8 | **2 / 24 次请求** |

均值差 3.1 秒,而串行自己的标准差就有 5.1 秒 ⇒ **这点差别分不出来是不是真的**。
并行那两次失败都是 `HTTP/2 stream 0 was not closed cleanly: PROTOCOL_ERROR`。

**结论:并行不是免费的加速,是「大概率白忙 + 8% 概率给用户一个失败」。建议串行 + 重试。**
(物理解释:如果瓶颈是「一条连接的拥塞窗口打不开」,并行会有 4 倍收益;实测没有 ⇒
瓶颈在**链路本身的容量/丢包**,开几条连接都一样。)

## 2.4 服务端自己吃掉了多少(同一条链路的配对对照)

同一份 98 KB 真 JPEG,从国内交替打「读完就扔的探针」和「真的上传接口」:

| | 探针 p50 | 上传接口 p50 | **配对差中位** | n |
|---|---|---|---|---|
| 改之前 | 1.92s | 3.59s | **+1.58s** | 6 |

⇒ **服务端自己花掉 1.6 秒,占用户等待的 44%**。花在哪:限流计数(KV 读+写)、
每日配额(KV 读+写)、发送记录(KV 读+写)、R2 写入。**四次 KV 往返里有三次是「记账」,
用户没有理由等它们。**

## 2.5 于是做了两步优化,并且**用交替 A/B 量了**

改动(都在 `upload.js` / `images.js`,语义没变):
1. 限流那次 KV 往返**与「读用户上传的字节」并发**跑(反正都要等用户传完);
   同时把它挂 `waitUntil`,免得中途 413 提前返回把还没写完的计数**取消掉**(那样限流会被超大包白嫖);
2. 发送记录、每日配额的**写**都改成 `waitUntil` 事后做(放不放行由**读**决定,写只是记账)。

量法:**同一时刻交替打两个 Pages 部署**(旧的那个部署 URL 还活着),不是先跑一轮再跑一轮 ——
这条链路方差太大,先后跑会把链路抖动算到改动头上。

| 对照 | 观测点 | 改前 p50 | 改后 p50 | 配对差中位 | 方向一致 |
|---|---|---|---|---|---|
| 第一步(记录改 waitUntil) | 首尔 VPS(ICN 边缘) | 2.39s | 1.65s | **−0.72s** | **6/6 全部变快** |
| 第二步(配额写也 waitUntil) | 首尔 VPS(ICN 边缘) | 1.66s | 1.16s | **−0.50s** | **6/6 全部变快** |
| 合计 | 首尔 VPS | 2.39s | 1.16s | **−1.22s** | 12/12 |
| 合计两步 | **国内**(LAX 边缘) | 3.73s | 3.47s | **−0.31s** | **4/4 全部变快**(n 只有 4,见 §7③) |

## 2.6 ★ 交给 owner 与 App 侧的结论(一句话版)

> **服务端这边已经没什么可省的了(1.6s 砍到 ~0.4s),剩下的全是链路。链路我们改不了。
> 图片体验的好坏,100% 取决于 App 把图压到多小。**

给 App(X121 颗粒 15)的**硬要求**,每条都有上面的数撑着:

| # | 要求 | 依据 |
|---|---|---|
| 1 | **压到 ≤300 KB**,建议长边 1280 + JPEG 质量 0.7;宁可再压狠一点到 ~150 KB | §2.2:300 KB ⇒ 3.6s;1 MB ⇒ 9.8s(p95 11.9s) |
| 2 | **一张一张串行传**,不要并行 | §2.3:并行不快,还多 8% 失败率 |
| 3 | **失败重试 3 次,指数退避**(1s / 2s / 4s) | §2.1 本轮 30/30 没失败,但 §2.3 并行下出现过传输层错误 ⇒ 重试是保险不是常态 |
| 4 | **必须有进度条**,并且允许「跳过图片直接提交」 | §2.2:哪怕压到 100 KB,p50 也还要 2.4 秒;4 张就是 10 秒 |
| 5 | 超时设 **60 秒**以上 | §2.1 国内 1 MB max 13.5s;弱网还会更差 |
| 6 | 先 `contact` 拿 `id`/`token`/`cid`,再逐张 `upload`,最后一次 `attach` | §3 契约 |

**不要做的事**:不要试图「让服务端中转加速」。Worker 就跑在那个边缘上,
慢的是**用户到边缘**这一段,中转不了。

# 3. 契约(X121 颗粒 15 照抄这一节)

## 3.0 顺序

```
POST /api/feedback/contact          → {ticket:{id, token, createdAt, mode, cid}}
   ↓ (对每张图,串行)
POST /api/feedback/upload           → {ok, key, url, bytes}
   ↓ (全部传完之后,一次)
POST /api/feedback/attach {id, token, cid, keys[]}  → {ok, cid, n}
```

追加回复带图也一样:`POST reply` 拿到 `cid` → 逐张 `upload` → 一次 `attach`。

## 3.1 `POST /api/feedback/upload`

| 项 | 值 |
|---|---|
| 请求头 | `X-RC-Key`(同现有值)· `X-RC-Ticket-Id`· `X-RC-Ticket-Token`· `Content-Type: image/jpeg` |
| 请求体 | **raw JPEG 字节**。⚠️ **不收 `multipart/form-data`** —— 一条路一份测试,少一个解析器少一处坑 |
| 限制 | 单张 ≤ **1 MB**(1048576 字节,超了 413)· 每工单每日 ≤ **20 张**(超了 429)· 每 IP ≤ 40 次 / 10 分钟 |
| 只收 JPEG | 服务端查魔数(开头 `FF D8 FF`、结尾 `FF D9`),PNG 改个扩展名传上来会被 400 挡掉 |
| 200 | `{"ok":true,"key":"<ticketId>/<32位十六进制>.jpg","url":"https://xiaotiantian-app.pages.dev/api/feedback/img/<key>","bytes":98481}` |
| 400 | `bad_request`(id 不合法 / 空体);`detail:"not_jpeg"` = 不是 JPEG |
| 403 | `forbidden`(`X-RC-Key` 错,或 `X-RC-Ticket-Token` 与 id 对不上) |
| 413 | `too_large` |
| 429 | `rate_limited`;`detail:"daily_image_quota"` = 撞每工单每日 20 张 |
| 500 | `storage_unavailable`(R2 挂了 —— App 应当**允许用户不带图直接提交**) |

**不查工单存不存在**(存在性检查在 `attach` 那一步):token = HMAC(secret, id),
算得出来就说明这个 id 是我们发的;为查一次存在性多打一次 GitHub,在国内链路上不值。

## 3.2 `POST /api/feedback/attach`

| 项 | 值 |
|---|---|
| 请求头 | `X-RC-Key` · `Content-Type: application/json` |
| 请求体 | `{"id":"5","token":"…","cid":0,"keys":["5/<32hex>.jpg", …]}` |
| `cid` | 省略或 `0` ⇒ 嵌进**首帖**;`>0` ⇒ 嵌进那条评论。**这个值从 `contact` 或 `reply` 的返回里拿** |
| `keys` | 最多 **4** 个,且**必须都属于这个工单**(键的前缀就是 ticketId);不属于的会被静默丢弃,全丢完就是 400 |
| 200 | `{"ok":true,"cid":<实际嵌进去的那条>,"n":<嵌了几张>}` |
| 400 | `bad_request`;`detail:"too_many_images"` = 超过 4 张 |
| 403 / 404 / 409 / 502 | 同 `reply`(`forbidden` / `not_found` / `not_ready` / `upstream_failed`);`detail:"cid"` = 那条评论不存在**或不属于这个工单** |
| 限流 | 自己的桶,20 次 / 10 分钟 / IP(**不再蹭 reply 的桶**,见 §5 G5-RATE-SPLIT) |

## 3.3 `GET /api/feedback/img/<ticketId>/<32hex>.jpg`

**不需要任何请求头**(必须公网可读:GitHub 的图片代理 camo 是从它自己的服务器来取的,
不会带我们的私有头)。回 `image/jpeg` + `Cache-Control: public, max-age=31536000, immutable`
+ `X-Content-Type-Options: nosniff` + `X-Robots-Tag: noindex`。键不合规矩或对象不在 ⇒ 404。

## 3.4 两个**纯新增**字段(老 App 忽略它们也照常工作)

| 接口 | 新字段 | 含义 |
|---|---|---|
| `contact` | `ticket.cid` | 这次留言落在哪条消息上:新开工单 = `0`(首帖);追加到本设备已有工单 = 那条评论的 id |
| `thread` | `messages[].images` | 这条消息带的图片 URL 数组(**正文里的 `![截图](…)` 已被剥掉**,App 气泡里不会出现这行字面量) |

★ 剥图这一步在服务端做,而且**必须在截取【问题描述】那一段之前做** —— 图片 markdown 追加在
正文最末尾,顺序反了图就被截没了。这条有单测钉着(§5 G5-UNIT)。

# 4. R2 配置(线上现状)

| 项 | 值 |
|---|---|
| 桶名 | `xiaotiantian-feedback`(Standard) |
| 绑定 | `wrangler.toml` 的 `[[r2_buckets]]`,binding = `FEEDBACK_IMG`(production 与 preview 都配了) |
| 公共访问 | **关**(`wrangler r2 bucket dev-url get` ⇒ `Public access via the r2.dev URL is disabled.`)。读一律经 Function |
| 对象键 | `<ticketId>/<32 位随机十六进制>.jpg`(128 bit 熵) |
| 对象元数据 | `contentType: image/jpeg` · `cacheControl: public, max-age=31536000, immutable` · `customMetadata: {ticket, at}` |
| 生命周期 | `expire-90d`:**90 天后自动删**,全前缀。`wrangler r2 bucket lifecycle list` 回显 `Expire objects after 90 days` |
| 免费额度 | 10 GB 存储 / 每月 100 万次 Class A(写)/ 1000 万次 Class B(读)。按每张 ≤1 MB、90 天保留算,要存满 10 GB 得在 90 天内收一万张图 |

# 5. 闸账(绿 / 红 / 没跑 三态)

| 闸 | 判定 | 证据 |
|---|---|---|
| **G5-UNIT** | 🟢 | `node tools/test_feedback.mjs` **95/95**(原 70 + 新增 25)。新增里 **12 条是对照**:路径穿越 / 键里塞子目录 / 大写十六进制 / 换扩展名 / 跨工单归属 / 前缀相同但不同号 / PNG 改名 / 头对尾不对 / 剥图后仍能截到【问题描述】 / 配额换工单号仍放行 / 配额换一天仍放行 |
| **G5-ROUNDTRIP** | 🟢 | 传上去的 98481 字节 JPEG,从 `/api/feedback/img/…` 取回来 **sha256 逐位相同**(`b79c4791…67a0b`);再用 `wrangler r2 object get --remote` 直接从 R2 取一次,**还是同一个 sha256** ⇒ 不是被某层缓存骗了 |
| **G5-GITHUB** ★ | 🟢 | issue #5 的 `body_html`(GitHub 自己渲染的 HTML)里,我们的 URL 已被改写成 `camo.githubusercontent.com/…`;**去取那个 camo 地址,回来的字节 sha256 与原图逐位相同** ⇒ GitHub 的代理**真的取到了**我们 R2 上的图。这比截图强:截图只能证明「我这儿画出来了」 |
| **G5-SHOT** | 🟢 | 截图 **`X123-G5-issue5-full.png`(900×5399,已放 Air `~/Downloads/`)**:issue #5 的**首帖 + 两条评论**,三条各带 3 / 1 / 1 张图,全部由浏览器**真的从 camo 取下来**画出来。私有仓库网页要登录,所以画的是 GitHub API 返回的 `body_html`,不是登录后的网页外壳 —— **这条口径写在图上的黄色横幅里**,别把它当成「登录后的 GitHub 网页截图」 |
| **G5-E2E-TEST** | 🟢 | 测试路径(`X-RC-Test: 1`,不碰 GitHub):contact → 传 2 张 → attach(cid=0)→ thread 回 `images` 两个 URL,正文里没有 `![` 字面量 |
| **G5-E2E-REAL** | 🟢 | 真路径三条分支全跑到:① contact 新开 ⇒ issue #5、`cid=0` · 传 2 张 · attach 首帖 ⇒ 帖子末尾出现两行 `![截图](…)`;② reply ⇒ `cid=5581283808` · 传 1 张 · attach 到**那条评论**;③ **同一设备第二次提交(追加分支)** ⇒ `cid=5581436333`,`gh api` 回读证明这个 id **真的存在、真的属于 #5、真的是刚写的那条**(`[用户消息 #2]`)· 传 1 张 · attach 到它。最终 thread:**三条消息各带 3 / 1 / 1 张,图全挂对了消息**,正文里都没有 `![` 字面量 |
| **G5-AUTHZ** | 🟢 | 十条阴性 + 两条阳性对照,见 §6 全表。要紧的两条:**拿 A 工单的 token 把图嵌到 B 工单的评论上 ⇒ 404**,并且回头核对 B 那条评论**一个字节没被改**;**键属于别的工单 ⇒ 400** |
| **G5-TRAVERSAL** | 🟢 | `--path-as-is` 打 `…/img/<id>/../../../functions/_lib/feedback.js` ⇒ 404;编码版 `%2e%2e%2f` ⇒ 404;键里多一层子目录 ⇒ 404;大写十六进制 ⇒ 404。**阳性对照**:同一路径小写 ⇒ 200 + 98481 字节 |
| **G5-RATE-SPLIT** ★ | 🟢 | 改前:attach 用掉 5 次之后,**同一 IP 的 reply 被打成 429**(实测)。改后:reply 桶**仍然是满的**(同一时刻打 reply 仍 429 —— 阴性对照,证明不是「限流整个失效了」),而 attach ⇒ **200** |
| **G5-QUOTA** | 🟢 | 单测:第 23 张被每日 20 张的配额挡住;阳性对照两条(换工单号放行、换一天放行)。线上没有单独把某个工单打满 20 张来验(§7⑥) |
| **G5-SENDLOG** | 🟢 | 记录页上有 `upload` / `attach` 两类行、有键名(63 处 `<32hex>.jpg`)、有「本日第 N 张」;**没有** `data:image`、**没有**图片本体。正文位置写的是「(图片,不记录内容)」/「(图片,只记键名)」 |
| **G5-LEGAL** | 🟢 | 四张法律页与 `/edit` **一个字节没碰**(本轮只新增 `functions/api/feedback/` 下的文件与 `_lib/images.js`,`docs/` / `content/` / `template/` 无改动 —— `git diff --stat` 见 §8) |
| **G5-PRIVACY-TEXT** | 🟢 | 按 owner 令**没有改任何隐私/权限文案**。但这带来一处**口径缺口**,登记在 §7④ 等 owner 裁 |
| **G5-ISSUES** | 🟢 | 本轮开出真 issue **1 个**(#5,上限 1),收工时 `state=CLOSED` + 标签 `user-feedback,test`(`gh issue list` 回读确认;#1~#4 是前几个颗粒的,同样已关闭并打 test) |
| **G5-PERF-CN** | 🟡 **样本太少,不判绿** | 国内侧交替 A/B 被我们自己的限流(40 次/10 分钟/IP)吃掉了大半样本,最后一轮只拿到 **n=4** 对:**4/4 全部变快,配对差中位 −0.31s**。方向一致但 n=4 的符号检验只到 p≈0.06,**够不上一条闸**。首尔边缘那边是 12/12、合计 −1.22s(§2.5)。详见 §7③ |

# 6. 阴性 / 阳性对照全表(线上实打,每条只差一个变量)

| # | 请求 | 期望 | 实得 |
|---|---|---|---|
| N1 | upload,体是 PNG 改名 `.jpg` | 400 | `{"err":"bad_request","detail":"not_jpeg"}` 400 |
| N2 | upload,`X-RC-Ticket-Token` 全 f | 403 | `forbidden` 403 |
| N3 | upload,不带 `X-RC-Key` | 403 | `forbidden` 403 |
| N4 | upload,1383336 字节 | 413 | `too_large` 413 |
| N5 | upload,空体 | 400 | `bad_request` 400 |
| N6 | img,猜一个全 0 的键 | 404 | 404 |
| N7b | img,`--path-as-is` `../../../functions/_lib/feedback.js` | 404 | 404 |
| N7c | img,`%2e%2e%2f%2e%2e%2fsendlog` | 404 | 404 |
| N7d | img,键里多一层子目录 | 404 | 404 |
| N7e | img,键换成大写十六进制 | 404 | 404 |
| N8 | attach,keys 是别的工单的键 | 400 | `bad_request` 400 |
| N9 | attach,5 张 | 400 | `too_many_images` 400 |
| N10 | attach,`cid` 是个不存在的数 | 404 | `not_found`/`cid` 404 |
| **N11** ★ | attach,拿 **#5** 的 token,`cid` 填 **#3** 的评论 id | 404 且 #3 那条评论不变 | 404;`gh api` 回读 #3 那条评论 —— 里面**没有**图片 markdown |
| **P1** ★ | 与 N2 **只差 token 一个变量**,换成对的 | 200 | 200,`bytes:98419` |
| **P2** ★ | 与 N7e **只差大小写一个变量** | 200 | 200,98481 字节 |
| **P3** ★ | G5-RATE-SPLIT:reply 桶满着时打 attach | 200 | 200;同一时刻 reply 仍 429 |

# 7. ★★★ 诚实栏(没做到 / 做错了 / 边界 / 要 owner 拍板)

## ① ★ 图片贴进 GitHub 之后,「私有仓库」这层保护就不完整了 —— **要 owner 知情**

工单帖子里的图,GitHub 会用它自己的图片代理 `camo.githubusercontent.com` 去取一份**并缓存**。
这条链有两个后果:

- 我们这边的 `/api/feedback/img/<key>` **必须公网可读**(camo 是 GitHub 的服务器来取的,
  不会带我们的 `X-RC-Key`)。所以访问控制只剩「**键猜不到**」:`<ticketId>/<32 位随机十六进制>`,
  128 bit 熵,暴力猜不出来。但**拿到这个网址的人就能看图**,和「私有仓库要登录」不是一个强度。
- **camo 那份缓存不归我们管**:就算 90 天后 R2 自动删了,camo 上那份可能还在。
  「彻底删掉一张用户截图」这件事,**我们做不到 100%**。

这不是疏漏,是「要在 GitHub 网页上看见图」这个需求的**结构性代价** —— 只要用 GitHub issue
当工单,就绕不过去。**替代方案只有一个:不往 issue 里贴图,只在 sendlog 页面上给 owner 看**
(那条可以加 key 保护)。代价是 owner 在 GitHub 通知里点进去看不到图,得另开一个页面。
**要不要换,请 owner 拍板。**

## ② 「国内」量的是机房,不是用户的手机

`ssh appserver` 是腾讯云国内的**机房**,不是家宽、不是 4G/5G。它到 Cloudflare 的路由
(落 LAX)和真实用户手机的路由**不一定一样** —— 移动/联通/电信各自出海口不同,
也可能落 HKG 或 NRT。**这张表是数量级参考,不是「用户实际会等 9.8 秒」的承诺**;
真实用户可能更快(离得近)也可能更慢(晚高峰、跨网)。
**手上没有第二个国内观测点**,这条没法交叉验证。真要准,得等 App 上线后从真机埋点回传。

## ③ 国内侧的优化 A/B 没量成(G5-PERF-CN 黄)

优化前后的交替对照,在首尔边缘拿到 **12/12 一致变快、合计 −1.22s**;国内边缘**只拿到 n=4**
—— 一开跑就撞上我们自己的限流(upload 桶 40 次 / 10 分钟 / IP),三轮里两轮的样本被 429 吃光。
拿到的这 4 对是 **4/4 全部变快、中位 −0.31s**,方向一致,但 n=4 的符号检验只到 p≈0.06,
**够不上一条闸**,所以 G5-PERF-CN 判黄不判绿。

另外要说清楚一件反直觉的事:国内省下的(−0.31s)**比首尔少**(−1.22s)。这不矛盾 ——
省掉的是**边缘侧那几次 KV 往返**,是个大致固定的绝对量;而国内那边总时长里有一大块是
跨太平洋的传输,**KV 往返在 LAX 边缘上本来就可能更快**(KV 的主副本位置不同)。
无论如何,**这一改对国内用户的收益是零点几秒级,不是几秒级 —— 真正的几秒在 §2.6 那张表里,
在 App 侧的压缩上。**

**我没有为了测试去调高线上的限流阈值**(那会把「线上真实行为」和「测试便利」混在一起)。

## ④ 隐私政策没改 —— 但现在的文案**没有提到图片**,口径有缺口

owner 明令「不改隐私/权限文案」,我照做了,**一个字没动**。但要如实报告:

- 四张法律页现在说的是「反馈内容最多保留 30 天」一类口径(X122 颗粒 7 改的),
  **没有一句提到用户可以上传截图、截图存在哪里、存多久**。
- 现状是:截图存 Cloudflare R2 **90 天**(不是 30 天),而且会经 GitHub 的图片代理缓存(见 ①)。
- 另外 App 里「选照片」会用到**相册读取权限** —— 权限文案里现在写的是什么、要不要补,
  也没动。

**这不是我能替 owner 决定的**。三条路:(a) 补一句隐私文案;(b) 把 R2 保留期从 90 天改成
30 天,与现有文案对齐;(c) 维持现状,承认文案没覆盖到。**等 owner 裁。**
(改保留期是一条命令:`wrangler r2 bucket lifecycle set`,§4。)

## ⑤ 临时测速端点已删,但**旧的部署 URL 上它还活着**

`functions/api/feedback/netprobe.js` 量完就从生产删了(§8 的最后一个 commit)。
但 Cloudflare Pages 的每个历史部署都有自己的永久 URL(`<hash>.xiaotiantian-app.pages.dev`),
**那几个 URL 上 netprobe 仍然在**。它要 `X-RC-Key` 才能用、有自己的限流桶、不写任何数据,
风险很小;但如果 `X-RC-Key` 泄露,它可以被当成免费带宽靶子。
**要彻底清掉得去 Cloudflare 面板删那几个历史部署**,我没有删(删部署是不可逆操作,
而且会影响 owner 的回滚能力)。**登记在这里,由 owner 决定要不要删。**

## ⑥ 没在线上真的把一个工单打满 20 张

每工单每日 20 张这条,只有**单测**证据(第 23 张被挡 + 两条阳性对照),
线上**没有**真跑到 20 张。原因:线上跑一次要 20 次上传,会先撞上 IP 的 40 次/10 分钟,
而且会往 R2 里灌 20 个对象。**判定写成「单测绿、线上没跑」**,不是「已验证」。

## ⑦ 别的登记

- **只收 raw JPEG,不收 multipart** —— 任务书给了「二选一」,我选了 raw 并写死。
  如果 App 侧已经按 multipart 写了,**要改的是 App**,不是服务端(改服务端就得再养一个解析器 + 一套测试)。
- **只收 JPEG,不收 PNG / HEIC** —— owner 已裁「App 端必须压缩」,压缩产物就是 JPEG。
  用户从相册选一张 PNG 截图,**App 必须自己转成 JPEG 再传**,否则会吃 400。这条要写进 App 侧任务书。
- **`attach` 是「改已有的那条」不是「另发一条只有图的评论」** —— 另发会让 App 的对话里
  凭空多一个空气泡。代价:issue 的编辑历史里会看到「机器人改过自己的帖子」。
- **并发上传时每日配额可能略微超过 20**(配额的写改成事后做了,几个并发请求可能读到同一个计数)。
  这个竞态**原本就有**(读-改-写不是原子的),不是这次引入的;而且我们给 App 的建议就是串行。
- **本轮在 owner 的私有仓库里留下 1 个真 issue(#5)**,已 close + 打 `test`。
  正文里是我编的 deviceId 和测试文案 + 4 张**本地生成的随机噪声图**,没有真实用户数据。
- **R2 里留下了本轮的测试对象**(测试件 + issue #5 的,十几个)。它们 90 天后自动删;
  要现在就清,`wrangler r2 object delete` 逐个删或按前缀写个脚本。**我没删** ——
  删了 §5 那几条闸的证据链就断了(URL 会变 404)。

# 8. 本轮 commit

```
a4a2bba  chore(x123-g5): 临时链路测速端点 netprobe(量完即删)
7122032  feat(x123-g5): 工单支持图片 —— R2 存图 + upload/attach/img 三个接口 + contact 回 cid + thread 回 images;单测 70→95
73dd359  perf(x123-g5): upload 把限流那次 KV 往返与读上传体并发 + 发送记录改 waitUntil;attach 分出自己的限流桶
(见下)  perf(x123-g5): 每日配额的写也挪出关键路径
(见下)  chore(x123-g5): 删掉临时测速端点 netprobe
(见下)  docs(x123-g5): 回执
```

# 9. 交给协调 / X121 颗粒 15 的事

1. **App 侧照 §3 实现**,重点是 §2.6 那六条硬要求(尤其「压到 ≤300 KB」和「串行不并行」)。
2. **`X-RC-Key` 不变**,base URL 不变 —— 这一轮**没有**改动任何已有接口的请求格式。
3. **三件等 owner 拍板**:§7① 图片可见性口径 · §7④ 隐私文案与 90 天保留期 · §7⑤ 要不要删历史部署。
