# 预注册 · X122 颗粒 6 —— 「owner 改了一次内容,线上页整体行间距变了」

> 动手改代码**之前**写死:成因判断、要改什么、四条闸的通过线。写完即 commit,
> 之后的结论只能落在这张表上,不许事后挪门框。 2026.09.08 Naron

## 简短大白话解释

owner 在 `/edit` 里删了一行字,保存后发现"字是改对了,但整页的行间距好像变了"。

**做了什么**:把「你在编辑框里打的字 → 存进云端 → 变成网页」这条链子,一个字节一个字节
地对了一遍——对了四层:云端存的原文、渲染出来的 HTML、线上真取回来的 HTML、浏览器里
实际算出来的行高/字号/栏宽。

**结论**:**这条链子是干净的,owner 那次编辑一个字节都没被改坏**;他删一行,线上就只少
那一行,行高数字前后完全一样(隐私/协议 24px、目录/支持 28.8px,前后各自不变)。
真正让他觉得"整页变了"的是**浏览器缓存**:网页的缓存头里写了
`stale-while-revalidate=86400`,意思是"过期后的 24 小时内,浏览器可以先把上次存的旧页
直接画给你看,再偷偷去后台更新"。所以 owner 编辑**之前**眼睛里那一版,其实是 09:38 的
旧页(日期还写着 2026年5月12日);他一保存,页面被强制取新,于是**一次性跳了两个版本**
——不只是他删的那一行,还有 10:11 那次全文核实改写(隐私页从 25 段变 33 段,高度从
2776px 变 3638px,高了 31%)。他看到的"整体变了"是这个,不是编辑器把他的字排版重排了。

**对产品意味着什么**:不用改渲染器,也不用恢复 owner 的内容(他的内容本来就是对的)。
要改的是缓存策略——让"保存并发布"之后,owner 刷新就一定看到新页,不会再被浏览器拿
一个最长 24 小时的旧页糊弄。另外顺手补上四条自动闸,以后每次改动都能证明"只改一个词、
线上只变那个词"。

---

# 一、成因(取证在前,已完成;此处只登记结论)

| # | 断言 | 证据 |
|---|---|---|
| C1 | KV 里 owner 改动前后两版原文,差异**只有他自己删的那几行** | `diff` 三页各 1 处(privacy/terms 各删 1 行 `<p>`;support 删 1 个 `highlight-box` + 2 行 `<p>` + 改 1 个 `<h2>` 文案) |
| C2 | 渲染后的 HTML 差异与 C1 **逐行一致**,渲染器没多加/少掉任何标签 | 用同一份 `render.js` 离线渲染两版后 `diff` |
| C3 | 线上真取回来的 HTML 与离线渲染**逐字节相同** | `curl` 三页 vs 本地渲染,`diff` 全空 |
| C4 | 浏览器里**实际生效**的行高/字号/栏宽,改动前后完全一致 | WKWebView 内 `getComputedStyle` 全量元素几何:行高取值集合前后相同(privacy/terms `{20,21,24}px`;support `{20,25.2,28.8,31.68,36}px`),差异只有被删元素本身 |
| C5 | ⇒ **编辑链路没有重排;"行间距变了"不是编辑器造成的** | C1–C4 |
| C6 | 真正成因 = 浏览器缓存把**旧版页**画给了 owner | 缓存头 `public, max-age=300, s-maxage=600, stale-while-revalidate=86400`;实测 10:46 用带持久缓存的 WKWebView 取 `/privacy`,拿到的是 **09:38 的旧页**(日期 2026年5月12日、高 5554 device px),同一分钟 `curl` 与加 cache-buster 的同一个工具拿到的是新页(7276 device px) |
| C7 | 保存时的"边缘缓存清理"是**空操作**,清不到任何东西 | 每次 `/api/save` 回执都是 `purged:[["/privacy",false],["/privacy.html",false]]`(`caches.default.delete` 返回 false = 缓存里根本没有这一条) |

**一句话成因**:渲染链是字节直通的;是 `stale-while-revalidate=86400` 让 owner 的浏览器
在他编辑前一直画着一版旧页,保存后才一次性跳到最新,把 10:11 的全文改写和他自己的删行
叠在一起显示出来。

# 二、要改什么(改动前钉死,只改这三处)

| # | 文件 | 改法 | 为什么 |
|---|---|---|---|
| F1 | `functions/_lib/page.js` | 页面响应头改成 `Cache-Control: public, max-age=0, must-revalidate`(**删掉 `s-maxage` 与 `stale-while-revalidate`**),并加 `ETag`(正文 SHA-256)+ 处理 `If-None-Match` 回 304 | 让"保存并发布"名副其实:浏览器每次都问一句,没变就 304(几百字节),变了立刻拿新的。绝不再拿最长 24 小时的旧页顶包 |
| F2 | `docs/_headers` | 四个页面路径单独给 `max-age=0, must-revalidate`;`/assets/*` 的一年长缓存**不动**(文件名带指纹) | 静态回退那一份也不能留同样的坑;字体是内容寻址的,长缓存正确 |
| F3 | `tools/gate_g6.mjs`(新增) | 把下面四条闸写成可重跑脚本 | 以后每次改渲染器/编辑器都能一键复验 |

**不改**:渲染器 `render.js` 一行不动(C2/C3 证明它是字节直通的);
**不改**:任何页面内容与样式;
**不恢复** owner 的内容——他的内容本来就是他想要的,链路没弄坏它(C1)。

# 三、四条闸(通过线写死)

| 闸 | 判定式 | 阴性对照 |
|---|---|---|
| **G6-ONEWORD** | 四页各挑一个词,经**真的 `/api/save`** 保存 ⇒ 线上取回的 HTML 与改前 `diff` **恰好 2 行**(同一行的旧/新),且新行 = 旧行把那个词替换一次的结果 | 用一版故意做归一化的保存链(`\r\n`→`\n` + 行尾去空格 + 重新序列化)跑同一份输入 ⇒ 必须出现 **> 2 行** 差异 |
| **G6-RT** | 四页各做「改一字 → 存 → 改回 → 存」往返 ⇒ 最终线上 HTML 与起点**逐字节相同** | — |
| **G6-CACHE** | 修完后:`/privacy` 等四页响应头**不含** `stale-while-revalidate`,`max-age=0`;带 `If-None-Match` 的条件请求回 **304**;改一个词后同一条件请求回 **200 + 新内容** | 改前的头 `max-age=300, s-maxage=600, stale-while-revalidate=86400`(已实测能画出 68 分钟前的旧页) |
| **G6-SEC** | `tools/test_render.mjs` 单测全绿;7 类 XSS(`<script>` / `onclick` / `javascript:` href / `<iframe>` / `<img onerror>` / 未知 class / `</style><script>`)在整页模式与片段模式下**全被转义** | — |
| **G6-SYNC** | KV / 线上 / `content/` / `docs/` / GitHub 备份站 五处正文**逐字节同**;CF 与备份站两地均 200 | — |

**G6-OWNER**(任务书原列)已在取证阶段判定为**空操作**:C1–C4 证明 owner 的内容与行距
都没被链路改过,没有"要恢复"的东西。回执里贴并排图与几何表作为证据,不做任何内容改动。

# 四、动手顺序

1. 本文件 commit(= 门框钉死)
2. F1 + F2 改代码 → 本地单测 → 部署
3. 跑 G6-ONEWORD / G6-RT / G6-CACHE / G6-SEC(含阴性对照)
4. 四处同步 + G6-SYNC
5. 回执 `RECEIPT-X122-G6.md` + 并排图
