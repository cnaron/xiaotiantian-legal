# 回执 · X122 颗粒 5 —— 四页内容逐句核实到 App 现状

执行:2026-09-08(北京时间)09:53–10:15,mini。执行代理:Claude Code。
预注册:[`PREREG-X122-G5.md`](./PREREG-X122-G5.md)(开工 **6 分钟**落的第一个 commit `b130c5e`)。
owner 原话:「把内容都核实一遍,不要展示官网,价格和当前保持一致,同时也要加入可编辑的页面」
+ 协调追加:①四页都要核;②主页底部技术/托管性说明整块删掉。

**线上地址(主用,填 ASC 用这个,无 `.html` 零跳转)**
<https://xiaotiantian-app.pages.dev/> · [/privacy](https://xiaotiantian-app.pages.dev/privacy) ·
[/terms](https://xiaotiantian-app.pages.dev/terms) · [/support](https://xiaotiantian-app.pages.dev/support)
备份站:`https://cnaron.github.io/xiaotiantian-legal/<页>.html`
编辑器:<https://xiaotiantian-app.pages.dev/edit>(口令见 Air `~/Downloads/xiaotiantian-legal-site/EDIT-PASSPHRASE.txt`)

---

# 简短大白话

上一轮把 owner 五月写的隐私政策和用户协议**原样搬**上了新站,搬完发现里面有十来处跟 App 现在的
实际情况对不上。**这一轮就是把这些话改成真的。**

做法不是凭印象改,而是**每一条先去 App 的源代码里查到出处**(哪个文件第几行),查到什么写什么。
改得最多的三件事:

1. **视频**。原来写「摄像头画面识别完立刻丢掉,绝不保存」—— 这是假的。现在每局都会在手机本地
   留一条视频(只留最新一局,能回看、能导出、不上传)。四个页面全部改成实话。
2. **价格**。原来写「月订阅 ¥6、年订阅 ¥58,3 天免费试用,到期自动续费」—— 现在全变了:
   **1 年 ¥28、3 年 ¥48、永久 ¥128,另外 1 年补 ¥98、3 年补 ¥78 可以升级成永久**;
   **不自动续费、没有免费试用、也不需要"取消订阅"**。用户协议里那张价目表从 2 行改成 5 行。
3. **权限**。原来只讲了 2 个权限(摄像头、健康),实际要 **6 个**(相机、麦克风、相册读、相册写、
   健康写、健康读声明)。App Store 审核是逐条对权限的,这里不能少。**补齐成 6 条,每条写清用途。**

另外按 owner 的要求:**所有指向老网站 gugushizi.com 的字和链接全删了**(原来有 6 处),
需要跳转的地方改成指向本站自己的页面;**主页最下面那段讲「本站是纯静态页面」的技术说明整段删掉**,
全站也不再出现「托管」「GitHub」「Cloudflare」这类词。

**样子没变。**没碰样式表、没碰字体、没碰版式 —— 证据是:privacy / terms 两页从开头到 `</head>`
(整套内联 CSS 和字体都在里面)**改前改后逐字节一模一样**,`<body>` 到正文之间那段外框也只有
**生效日期那两行字**变了。页面变长了纯粹是因为字多了(权限从 2 条变 6 条、价目表从 2 行变 5 行)。

**四个页面现在都能在网页编辑器里改**(主页 `/edit`,其余三页 `/<页名>/edit`)—— 这个是原来就
通了的,这轮实测确认了一遍,没有改任何路由代码。

**七条验证全绿**:官网字样 0 处、价格只出现那五个数、技术性词 0 处、四页两地都 200、
KV / 仓库 / 发布目录 / 备份站四处逐字节一致、四页各存一次「无改动」sha 不动。

**三件请 owner 过目的事**(不影响上线,但我不替 owner 定):见下面 §6。**这一轮没动 App 一个字节。**

---

# 1. 事实核实:24 条,逐条落到代码行

完整表在预注册 [§1](./PREREG-X122-G5.md#1-事实清单逐条给代码出处凡查不到出处的一律不写进页面)。
纪律是**先查出处再写句子**,不是先写句子再找理由。摘最要紧的:

| 事实 | 代码出处 | 原版怎么写的 | 处置 |
|---|---|---|---|
| 每局本地留一条视频,只留最新一局 | `LastReplayStore_claudecode_20260901.swift`(单槽/成功才替换) | 「识别后立即丢弃,绝不保存或上传」 | **改成实话**,四页 |
| 五档价格 ¥28/48/128/98/78,非续期+买断 | `SubscriptionPlan.swift` `fallbackDisplayPrice` / 类型对应表 | 月 ¥6 / 年 ¥58 自动续费 | **整节重写** |
| 没有免费试用(试用态已从枚举删除) | `SubscriptionPlan.swift` `SubscriptionStatus` 注释 | 「3 天免费试用」 | **删** |
| 权限 6 条 | `project.yml:100,103,104,105,108,112` | 只讲 2 条 | **补到 6 条** |
| HealthKit **只写不读**,读取声明是空集合 | `App/HealthKitBridge.swift:59` `read: []` | 同样这么写 | ✅ **是真话,原样保留** |
| 「联系我们」是**唯一**网络请求,发 8 个字段,机型是同型号共用串 | `RCContactRelayClient_claudecode_20260907.swift:4,56-65,94-103` | 「零数据上传」「不收集 IP」 | **改**:写清发什么、什么时候发 |
| 免费档:体测累计 3 次、历史 3 条 | `ProFeature.swift` `freeUsageLimit=3` / `freeHistoryLimit=3` | 3 条(对) | ✅ 保留 |
| 趋势:免费近 3 天 / Pro 近 7 天 | `TrendsView.swift:92,291` | 「完整 7 天 + 30 天」 | **改**(30 天入口已按设计稿删掉,用户到不了) |
| 评分标准 6 套 | `ChinaProvinceStandards.json` `provinces` 实测 6 项 | 「尽力收录」含糊 | **写明 6 套是哪 6 套** |
| 邮箱 `shengtang009@126.com` | `RCContactSheetV3_claudecode_20260907.swift:36` | privacy/terms 是 **009**;support 页是 **003** | **support 页 003 → 009**(颗粒 2 遗留的错) |
| App 内入口叫「设置 → **联系我们**」 | `SettingsView.swift:494` | support 页写「联系开发者」 | **改** |
| 记录可逐条删除 | `RecordsListView.swift:83` `.onDelete`;`RecordDetailView.swift:64` | 「设置→开发选项→清空所有记录」 | **改**(那段整个包在 `#if DEBUG` 里,`SettingsView.swift:686`,上架包不编译 ⇒ 对正式版用户是废话) |

## 1.1 查不到出处 ⇒ **删掉,不是改小**(三条)

| 断言 | 为什么删 |
|---|---|
| 「可能存在 **±5% 左右**的识别误差」 | 全工程 grep `5%` / `±5` **一处出处都没有**。对外承诺一个查无实据的精度数字是法律风险 ⇒ 换成不带数字的定性说明 |
| 「月订阅 → 年订阅 Apple 按剩余天数比例补差价(proration)」 | 买断制下不存在 proration;真实机制是**两个价格固定的升级商品** |
| 「本 App **不联网**」(index / support) | F11 起不成立 ⇒ 改成「除您主动提交『联系我们』之外不联网」 |

---

# 2. 改动前后逐句表(37 处,全部由 `tools/facts_rewrite_g5.py` 精确串替换 + 断言命中恰好一次)

改不到就报错退出,**不存在静默漏改**。编号对应脚本里的 tag。

## 2.1 privacy.html(14 处)

| # | 改前 | 改后 | 依据 |
|---|---|---|---|
| P1 | 版本更新/生效日期 **2026年5月12日** | **2026年9月8日**(两行都保留) | 任务书 |
| P2 | 核心承诺:「摄像头视频帧…**识别后立即丢弃,绝不保存或上传**」 | 「摄像头画面仅用于本机识别;每局会在您的手机本地生成一条跳绳视频,**只保留最新的一局**,可回看与导出,不会上传」 | F8 |
| P3 | (无) | 核心承诺补一条:「全程不联网,唯一的例外是您**主动提交**「联系我们」时」 | F11 |
| P4 | §一.1「…IP 地址等。您无需注册或登录即可使用…」 | 删掉"邮箱/IP 地址"(联系我们会用到邮箱、发请求必带 IP);**另起一段**写清「联系我们」发什么、何时发、不含什么 | F9/F11 |
| P5 | (无) | §一.2 本地数据补一条「**回看视频**:固定只保留最新一局,下一局自动覆盖」 | F8 |
| P6 | 「这些数据在**你**卸载…」 | 「…**您**卸载…」(全页人称统一) | 文字 |
| P7 | §一.3 权限 **2 条**(摄像头 / HealthKit) | **6 条**:相机 / 麦克风 / 相册读 / 相册写 / 健康写 / 健康读声明,每条给用途+说明 | F1–F7 |
| P8 | §二「StoreKit 2:处理**订阅**购买」/ 3 条框架 | 「处理 App 内购买」;补 **Photos** 框架;AVFoundation 那条补「模型装在手机里,不需要联网」 | F10/F11 |
| P9 | 「您可在**设置中清空记录**」 | 「您可在**记录列表中逐条删除**…(含回看视频)」 | F21/F22 |
| P10 | §四 标题「**订阅与支付**」+「Pro 会员订阅服务(月订阅 / 年订阅)」 | 「**购买与支付**」+「一次性购买…**不是自动续费订阅**:1 年 ¥28、3 年 ¥48 非续期,永久 ¥128 买断」 | F14/F15 |
| P11 | 「订阅自动续费由 Apple ID 处理,可随时取消」+ 链到 **gugushizi.com** 的用户协议 | 「**不存在自动续费,也没有『取消订阅』这一步**」+ 补「恢复购买按原始购买日重算」+ 链改 `./terms.html` | F15/F17 |
| P12 | 付费页建议"由家长或监护人指导后**开通订阅**" | 照抄 App 里的真话:"一次付费,到期不自动续费 · 建议在家长指导下开通" | F23 |
| P13 | §六:清除本地数据(开发选项)/ 撤回权限(摄像头·健康)/ 取消订阅 | 删除记录(左滑或详情页)/ 撤回权限(**相机·麦克风·照片·健康** 四项)/ **无需取消订阅** | F21/F22/F15 |
| P14 | §八 联系:邮箱 + **官方网站 www.gugushizi.com**;「**验证您的用户身份后**十五个工作日内回复」 | 邮箱(009)+ **App 内入口:设置 → 联系我们** + **用户协议**(站内链);「**没有账号体系,无法也无需核验您的身份**,尽快回复,通常十五个工作日内」 | F12/F13 + owner 令 |

## 2.2 terms.html(12 处)

| # | 改前 | 改后 | 依据 |
|---|---|---|---|
| T1 | 日期 2026年5月12日 | **2026年9月8日** | 任务书 |
| T2 | 「当您下载、安装、**注册、登录**或使用本应用时」 | 删「注册、登录」,补一句「本应用没有账号体系,无需注册与登录」 | F9 |
| T3 | 服务说明 4 条 | 「摄像头」→「相机」;补「保留最新一局的回看视频」;「Pro 会员」→「Pro」 | F8 |
| T4 | 「各省评分标准我们**尽力按官方文件收录**」 | 「目前内置 **6 套**:国家标准(教育部)、北京、上海、广东(广州)、江苏(南京)、浙江(杭州);未列出地区按国家标准换算」 | F20 |
| T5 | §三 标题「订阅服务(Pro 会员)」+ 价目表 **2 行**(月 ¥6 自动续费 / 年 ¥58 3 天试用自动续费) | 「**购买与付费(Pro)**」+ 价目表 **5 行**(1 年 ¥28 / 3 年 ¥48 / 永久 ¥128 / 1 年→永久 ¥98 / 3 年→永久 ¥78);脚注写明「**不提供免费试用,也不存在任何自动续费商品**」 | F14/F15/F16 |
| T6 | 「Pro 会员解锁功能」:含「周/月趋势图:完整 **7 天 + 30 天**」「零数据上传」 | 「免费档与 Pro 的区别」:趋势 **免费 3 天 / Pro 7 天**;体测免费累计 3 次;历史免费 3 条;基础计数**免费不限次**;「对所有用户都没有广告」(删掉"零数据上传") | F18/F19/F11 |
| T7 | 「3. **自动续费规则**」整节(到期前 24 小时自动续费 / 试用期扣费规则) | 「3. **到期与恢复规则**」:**不会自动续费**、没有取消这一步、有效期按 **Apple 签名凭证**在本机算、**恢复购买**按原始购买日重算 | F15/F16/F17 |
| T8 | 「4. 升级与降级」:月→年 **proration**;年→月 | 「4. **升级为永久版**」:1 年→永久 **¥98**、3 年→永久 **¥78**,**价格固定不按剩余天数折算**;补一行说明入口何时出现、不提供降级 | F14 |
| T9 | 「5. 退款」 | 「5. **退款与发票**」:退款**与发票一律由 Apple 处理**,补 iOS 内申请路径 | 任务书 |
| T10 | 「可能存在 **±5% 左右**的识别误差」 | 「受光线、场地、拍摄距离与角度、衣着等因素影响而出现误差;结果页可微调或直接填入真实次数」 | §1.1 |
| T11 | 下架时「通常 Apple 会按剩余天数比例退款」 | 「按 Apple 平台规则处理;是否退款、退多少由 Apple 判断」 | F15 |
| T12 | §十:邮箱 + **官方网站 www.gugushizi.com** + 隐私政策链到 **gugushizi.com** | 邮箱(009)+ **App 内入口:设置 → 联系我们** + 隐私政策链改 `./privacy.html` | F12/F13 + owner 令 |

## 2.3 index.html(3 处)· support.html(8 处)

| # | 改前 | 改后 | 依据 |
|---|---|---|---|
| I1 | 「摄像头…**本 App 不联网**、不上传、无账号…」 | 「相机…不上传、无账号…**除您主动在 App 内提交「联系我们」之外,本 App 不进行任何网络请求**」 | F11 |
| I2 | 三个入口描述 · 2026年9月7日生效 | 补「跳绳视频存在哪里」「「联系我们」会发送什么」「五档价格与到期规则(不自动续费)」「退款」· **2026年9月8日** | F1/F8/F14 |
| **I3** | **「关于本站」整个 `<section>`**:「本站为纯静态页面,不含任何统计代码…不加载任何外部资源。」 | **整节删除** | **owner 追加令②** |
| S1 | 引言「**不联网**、不上传」+ 邮箱 **003** | 「不上传;除主动提交「联系我们」外不联网」+ 入口改「设置 → 联系我们」+ 邮箱 **009** | F11/F12/F13 |
| S2 | Q3「本 App 里**根本没有上传功能**」 | 「本 App **没有上传视频的功能**」(措辞收紧:联系我们确实会发网络请求) | F11 |
| S3 | Q8 免费档:「购买后…完整历史与趋势图」 | 补「趋势免费**近 3 天**」;Pro 侧写明「历史不限条数、趋势**最近 7 天**」 | F18/F19 |
| S4 | Q10 无账号 | 补一整段:「**唯一一处会联网的地方是「联系我们」**」+ 发什么 + 机型是同型号共用串 | F11 |
| S5 | 联系:邮箱 **003** + 「设置 → **联系开发者**」 | 邮箱 **009** + 「设置 → **联系我们**」 | F12/F13 |
| S6 | Q5 买断说明 | 补「**没有免费试用**」;「非续期订阅」→「非续期购买」 | F16 |
| S7 | Q2 省标 6 套 | 补「**未列出的地区按国家标准换算**」 | F20 |
| S8 | Q9「历史记录可在 App 内逐条删除」 | 写清具体操作:「列表里左滑,或进详情点『删除这条记录』」 | F21 |

---

# 3. ★ 元素条数对账:**我超了自己预注册的预算,如实登记**

预注册 §2.2 在改动前钉的是 **+1 `<p>` / +4 `<li>` / +3 `<tr>` / −1 `<section>`**。
改完机器点了一遍,**实际是**:

| 标签 | 预注册 | 实际 | 差 | 具体落在哪 |
|---|---|---|---|---|
| `<p>` | +1 | **+9** | +8 | P4 拆段 +1;**P7 权限 2 条→6 条,每条自带用途/说明段 ⇒ +7**;P14 联系区 2 段→3 段 +1;T8 升级说明 +1;index 删节 −1 |
| `<li>` | +4 | **+9** | +5 | P7 权限 +4;P3 核心承诺 +1;P5 本地数据 +1;P8 Photos 框架 +1;P11 恢复购买 +1;T6 免费档对比 +1 |
| `<tr>` | +3 | **+3** | 0 | T5 价目表 2→5 行 ✅ |
| `<section>` | −1 | **−1** | 0 | I3 「关于本站」✅ |
| `<h2>` | 未登记 | **−1** | −1 | I3 那一节自带的标题 |
| `<a>` | 未登记 | **−1** | −1 | T12 官方网站链接**变成纯文字**(去官网) |
| `<strong>` | 未登记 | **+45** | +45 | 全是句子内部的加粗(「不自动续费」「只保留最新的一局」这类要点) |
| `<code>` | 未登记 | **+2** | +2 | 两处 `iPhone15,2` 机型示例 |

**为什么超**:预注册时我按"权限那一条 = +4 个 li"估的,**漏算了每个 li 内部自带的用途/说明段落**
(原版每条权限就是 `<strong>` + 2~3 个 `<p>`)—— 补 4 条权限必然带 +7 个 `<p>`,这是当时没算到的。
`<strong>` / `<code>` 是句子里的行内强调,归在"文字"里、预注册没列进计数表,也一并补登。

**没有超的部分**:**没有新标签类型、没有新 class、没有新属性、`<style>` 一个字节没动**
(§4 G5-PIXEL 逐字节验过)。所有新增元素都与它旁边的同类元素**逐字同型**。

---

# 4. 判定式结果(七条,**全绿**;三条文案闸带真阴性对照)

闸本身固化成 `tools/gates_g5.py`,可复跑:`python3 tools/gates_g5.py <目录>`。

| 闸 | 结果 | 证据 |
|---|---|---|
| **G5-NOSITE** | 🟢 | 改后 `grep -i gugushizi` 四页两目录 = **0**;**阴性对照**:同一把闸喂改前 docs = **红,命中 6 处**(privacy 3 + terms 3) |
| **G5-PRICE** | 🟢 | 价格数字全集 = **{¥28, ¥48, ¥128, ¥98, ¥78}**,无第六个;硬禁词 `¥6`/`¥58`/`月订阅`/`年订阅`/`proration` **0 命中**;**阴性对照** = 红,命中 21 处 |
| **G5-TECH** | 🟢 | `静态页面\|托管\|GitHub\|Cloudflare\|编辑入口\|构建\|Pages\|wrangler` 四页 **0 命中**;**阴性对照** = 红(index「静态页面」) |
| **G5-LIVE** | 🟢 | 四页 × 两地 **全 200**,且**取回的正文与 `docs/` 逐字节相同**(不是只看状态码 —— TUN 下 `curl -sI` 会假 200,X122 早前踩过) |
| **G5-SAME** | 🟢 | KV == `content/`、`docs/` == Cloudflare == github.io,**四页 × 四处 sha256 全等** |
| **G5-EDIT** | 🟢 | 四页编辑器 **4/4 打开 200**;各**原样回存一次**,`/api/save` 4/4 `{"ok":true}`;存后 **KV sha 与存前逐位相同**,**线上发布页字节也未变** |
| **G5-PIXEL** | 🟢 | 见 §5 |

## 4.1 ★ 一条闸我改了口径,不静默:G5-PRICE

预注册写的是「`自动续费` 零命中」。**这条写错了** —— 任务书要的是页面上**出现**「**不**自动续费」
(那正是要传达的事实),硬要零命中反而会把正确文案判红。
⇒ 改成:**只禁"肯定语境"的出现**(关键词前 14 字内没有 不/没有/无/未 才算红)。
改后 15 处 `自动续费`/`免费试用`/`自动扣款` 里,**14 处是否定语境**,剩 1 处是 support 的 FAQ
**疑问句标题**「5. 买了会自动续费吗?怎么取消?」—— 紧接着的答案就是「**不会。到期不自动续费。**」
⇒ 闸里给 `<h3>` 疑问句开了一条**写死的例外**(不是放宽阈值),并在这里点名。

# 5. G5-PIXEL:并排图 + 「版式没动」的硬证据

图已放 Air `~/Downloads/xiaotiantian-legal-site/X122-G5/`:
`sidebyside-{index,privacy,terms,support}.png`(左改前 / 右改后整页长图)
+ **`index-before.png` / `index-after.png`**(owner 追加令②要的主页改前改后)。

**⚠️ 这里我没有用"逐像素全等"当判据,理由要说清楚**:权限从 2 条变 6 条、价目表从 2 行变 5 行,
后面所有内容都会**整体下移**,朴素的像素差会从第一处文字改动(日期,y=215)起一路飘红到页尾 ——
那个红**不能区分"版式变了"和"字变多了"**,当判据没有意义。

改用**结构性证据**,这个是逐字节的、不含糊:

| 证据 | 结果 |
|---|---|
| privacy `<head>`(**整套内联 CSS + 自托管字体声明都在里面**)改前 vs 改后 | **8005 B 逐字节相同** |
| terms `<head>` 同上 | **8239 B 逐字节相同** |
| 两页 `<body>` → `<main>` 之间的外框(容器/标题/日期块) | 唯一差异 = **生效日期那两行**(`diff` 原文见下) |
| `template/style.css` / `template/shell.html` / `template/render.js` / `docs/assets/`(字体) | **本轮 `git diff` 为空 —— 一个字节没碰** |
| 本轮改到的文件全集 | `content/*.html`(4)+ `content/pages.json` + `docs/*.html`(构建产物)+ `functions/_lib/assets.js`(构建产物)+ 两个新工具脚本。**没有一个样式文件** |

```
=== privacy / terms  <body>→<main> 的全部差异 ===
<             <p>版本更新日期：2026年5月12日</p>
<             <p>版本生效日期：2026年5月12日</p>
---
>             <p>版本更新日期：2026年9月8日</p>
>             <p>版本生效日期：2026年9月8日</p>
```

页面高度变化(900px 宽渲染):privacy 2777→3662、terms 2912→3255、support 2690→2802、index 不变 ——
**全部来自文字增加**(权限 +4 条、价目表 +3 行、若干补充说明),不是版式变化。

# 6. 请 owner 过目的三件事(不影响上线,我不替 owner 定)

1. **「联系我们」的中转地址其实还在 gugushizi.com 域名下**
   (`RCContactRelayClient…:endpoint_20260907 = https://gugushizi.com/…/ropecounter/contact`)。
   owner 令是"页面上不展示官网",所以隐私政策里我写的是「**经由我们的服务器**转成邮件」,
   **没有点名这个域名**。页面上做到了不展示;但**技术上那个请求确实发往老域名**。
   如果 owner 希望隐私政策把接收方域名写明(有些审核口径会问),或者希望把中转换到新域名,
   请示下 —— **这属于改 App / 改后端,不在本颗粒范围**。
2. **主页与支持页页顶那行「版本更新日期」不在 KV 里,改不了**。
   它住在 `content/pages.json`(外壳级,构建时烤进页面),`/edit` 编辑器只管**正文**。
   这轮我在仓库里把它同步成了 9月8日;**以后 owner 在网页上改正文,这行日期不会跟着变**,
   要改得走仓库改+发布。要不要把它也并进 KV(能在网页上改),等 owner 一句话。
3. **间歇训练(HIIT)四页一个字都没提**。代码里 `ProFeature.hiitMode` 是有的,但
   `Features/` 下**没有任何启动入口**,只在记录详情里当历史模式名显示 ⇒ 写进协议等于
   画一个用户到不了的功能。如果它其实该上,请告知,我再补进 Pro 功能表。

# 7. 没碰的

- **App 一个字节没改**(iOS 工程全程只读)。
- `template/`(外壳/样式/渲染器)、`docs/assets/`(字体)、`functions/` 路由与 `wrangler.toml` —— 全未改。
  编辑器四页机制**本来就是通的**(`functions/_lib/page.js:PAGE_NAMES` 四页齐全;主页走 `/edit`、
  其余走 `/<页>/edit`),任务书里"可能要动 Functions 路由"的那种改造**没有发生**。
- 重复站 `xiaotiantian-legal.pages.dev` —— 按前几轮 owner 令未动(**它现在内容更旧了**,仍写着
  「不保存视频」「自动续费」,和主站已经差两代,**建议 owner 尽快裁:删掉或也发一份**)。
- 没发飞书通知;没改 git config / origin。
- **KV 覆盖前先备份 + 先比对**:导入前把线上四页 KV 拉下来存 `/Users/cc/x122/g5/kv-before/`,
  并逐字节比对确认**与颗粒 4 的仓库版本完全一致** ⇒ owner 在网页上**没有**未同步的改动,覆盖是安全的。
- GitHub 推送:mini 直推**仍然是坏的**(`failed to get: -25308`,登录钥匙串锁着 + `gh` token 失效),
  照颗粒 4 的办法绕 VPS 中转,中转裸仓用完即删,VPS 上不留东西。

# 8. commit

```
b130c5e  prereg  24 条事实逐条给代码出处 + 3 条查无实据者删除 + 七条判定式
50b9c2b  prereg  补登 §2.2 元素条数变化清单(改动前钉死)
c4caa96  feat    四页正文逐句核实(权限 2→6 · 视频说真话 · 五档买断价 · 删光 gugushizi.com · 主页删技术说明)
6fd7fed  fix     目录页/支持页页顶日期同步到 2026年9月8日
```
