# 回执 · X122 颗粒 9 —— 法律页删掉「联系我们」的实现细节,只留用户需要知道的

> 执行:mini 上的 Claude Code 执行 session · 2026-09-08 · 仓库 `/Users/cc/Public/x84sb/legal-site`(公开仓库)
> commit:`4f6ac1e`(改动)+ 本文件 · 上线方式:站内编辑器 `POST /api/save` 逐页保存 + `wrangler pages deploy docs` + `git push`

---

# 简短大白话

owner 说:「不需要写明是怎么联系我们的细节,很多用户不需要知道的内容无需告知。」
**做了什么**:把隐私政策、支持页、目录页里那些**只有工程师才关心**的话删掉了 ——
「你的反馈发到 Cloudflare 上、变成 GitHub 私有仓库里的一条工单」「一共联网三处」
「Cloudflare 那边留一份 30 天后自动删的发送记录」「你的 IP 会被抹掉末段、完整值只在一个
10 分钟就过期的防刷计数里」「设备编号是首次使用时随机生成、存在钥匙串里的」
「购买快照里有商品编号和 Apple 原始交易号」等等,**一句不留**。

**结论**:隐私政策里原本讲这件事的**四大段压成了一段**,支持页那个又长又技术的答案也缩短了。
留下的只有三件用户真正要知道的事:**①怎么找到我们**(App 内「小天天 → 联系我们」)、
**②我们会收到什么、拿来干什么**(你写的内容 + App 版本机型等排查用的技术信息,只用于回复和排查、
不用于营销、不给第三方)、**③多久回复**(14 个工作日,保留不逐一回复的权利 —— 这是颗粒 8 定的话术,
本轮一个字没动)。

**对产品意味着什么**:法律页从「工程说明书」变回「给用户看的告知书」,但**该披露的还在** ——
App Store 要求说明「收集什么、用于什么」的那条底线,用一句概括保住了,所以不影响上架。
另外**用户协议(terms)这一轮一个字节都没改**,因为它本来就没写这些细节;这不是"没查",
是查过为空,而且用截图逐像素比对证明了它前后**100.00% 一模一样**。

⚠️ **一件事需要 owner 知道**:App 现在**还不能发截图**(服务端能收,App 侧没做),所以本轮
**没有**在隐私政策里写「您可以附截图」—— 写了就是假话。等 App 真做了图片上传,这句必须补,
详见 §7①。

---

# 1. 交付了什么

| 项 | 内容 |
|---|---|
| 改了几处 | **9 处**:privacy 4(其中 P2 是把**原来 5 行合并成 2 行**,净删 3 段)· support 3 · index 1 · **terms 0** |
| 落地方式 | 站内编辑器 `POST /api/save` **逐页保存**(index / privacy / support 三页;terms 没改所以没保存)——**没有**用 `kv_import.py` 整体覆盖 |
| 两处托管 | Cloudflare Pages(主)`wrangler pages deploy docs` · GitHub Pages(备份)`git push` |
| 新增脚本 | `tools/edits_g9_claudecode_20260908.py`(可从基线重放,自带 5 条自证) |
| 顺带清账 | `tools/gates_g5.py` 的 `TECH_ALLOW_20260908` **清空**(见 §5) |
| 截图 | Air `~/Downloads/X122-G9/`(4 张并排图 + 4 张改后整页) |

## 保留 / 删除的分界(owner 规则的执行口径)

**保留**(用户需要知道):
1. **入口** —— App 内「小天天 → 联系我们」(privacy 2 处 / terms 2 处 / support 2 处,闸④验)
2. **会收到什么、用于什么** —— 一句概括:你填写的内容 + 排查所需技术信息(App 版本 / 设备型号 /
   系统版本 / 语言 / 会员档位 / 最近一局 / 权限开启状态 / 运行日志 / 购买状态)+ 一个反馈对话编号
   + IP;**只用于回复与排查,不用于营销,不给第三方**
3. **回复时效** —— 颗粒 8 的话术,**本轮一个字节未改**(闸③验:全站仍恰好 3 处)

**删除**(实现层,任务书点名 + 同类):

| 删掉的说法 | 原出处 |
|---|---|
| 部署在 **Cloudflare** 上的接口 | privacy |
| 开发者 **GitHub 私有仓库里的一条工单** | privacy · support |
| 「网络请求共**三处**(提交反馈 / 查看回复 / 追加回复)」 | privacy · support · index |
| Cloudflare 侧**保留 30 天后自动删除**的发送记录 | privacy ×2 |
| IP **已抹去末段、只保留到网段** + **10 分钟后自动过期**的防刷计数 | privacy |
| 设备编号**首次使用时随机生成、只保存在本机钥匙串里**、「只有我们的接口认得它」、防重复领免费次数 | privacy · support |
| 购买快照逐项:**商品编号**、**Apple 为这笔交易签发的原始交易号** | privacy ×2 · support |
| **安装渠道快照**(App Store / 测试渠道)与「描述文件里的设备 UDID 列表一个都不发送」 | privacy · support |
| 日志脱敏字段清单(文件路径 / 系统内部标识) | privacy · support |
| 机型举例 `iPhone15,2`「同一型号的所有手机完全相同」 | privacy · support |
| 「不设置 Cookie,失败也不重试」 | privacy |
| 中考模式省份 / 年级 / 性别**不发送**、五项权限逐项点名 | privacy |
| 「不再发送电子邮件、不再经过任何自建服务器」(讲的是**上一版怎么做的**,用户无需知道) | privacy · support |
| 注释里的裸词「工单」 | support(注释会原样进渲染后 HTML,颗粒 7 踩过) |

# 2. 判据结果(绿 / 红 / 没跑 三态)

| 闸 | 结果 | 证据 |
|---|---|---|
| **G9-BASE** 不覆盖 owner 改动 | 🟢 绿 | 开跑前 `GET /api/export` 取基线;保存后重新导出,四页与「基线 + §3 清单」**逐字节相同**;变更行 index 2 / privacy 13 / support 6 / **terms 0**,全部落在清单内(§3.1) |
| **G9-DETAIL** 实现细节清零 | 🟢 绿 | 尺子 `Cloudflare\|GitHub\|工单\|issue\|发送记录`:改前 KV 四页 **13 命中**(要求 ≥3)→ 改后 **0**;线上八页 **0**(§3.2) |
| **G9-DETAIL+** 扩展尺子 | 🟢 绿 | 再加 `钥匙串\|原始交易号\|商品编号\|共三处\|这三处\|iPhone15,2\|防刷`:改后四页 **0**(脚本内自证①) |
| **G9-MAIL** 邮箱仍 0 | 🟢 绿 | `@126.com\|@qq.com\|邮箱`:KV 四页 0 · `docs/` 五页 0 · 线上八页 0(§3.2) |
| **G9-KEEP** 该留的没被删掉 | 🟢 绿 | ①入口话术 `小天天 → 联系我们` privacy 2 / terms 2 / support 2 处仍在;③颗粒 8 时效话术全站仍**恰好 3 处**、线上八页 6 处(每站 3)(脚本自证③④) |
| **G9-TERMS0** terms 真的没动 | 🟢 绿 | sha 与基线同(`f2364b4978f2fbc6`);**并排截图逐像素 100.00% 相同、diffbox=None**(§3.4) |
| **G9-LIVE** 两站八页 | 🟢 绿 | 主站 4 页 200 / redirects=0、备份站 4 页 200,**八页全部与 `docs/` 逐字节相同**(§3.3) |
| **G9-SHOT** 截图不是缓存版 | 🟢 绿 | 三重:①`shot` 默认非持久仓 + 忽略缓存(颗粒 7 已修);②改后整页高度 privacy 11426→10488 px、support 5652→5540 px,与改前**不同尺寸**;③**同一次运行里 terms 前后 100.00% 相同** ⇒ 工具本身没在"每次都画新的"或"每次都画旧的"(§3.4) |
| **G8 行距闸复跑** | 🟢 绿 | `gate_g8.mjs` **20/20 全绿**,含 `G8-METRIC` privacy/terms 与 support **7 项排版指标全等**(body lh=28.799999px)、6 条真阴性对照(§3.5) |
| **G6 缓存闸复跑** | 🟢 绿 | `gate_g6.mjs` **15/15 全绿** —— 这条顺带**还清了颗粒 7 §8① 留的债**("在线一半欠一次")(§3.5) |
| **G5 文案三闸** | 🟢 绿 | NOSITE / PRICE / **TECH** 全 0 命中,且 TECH 是在**豁免清空后**拿到的绿(§5) |
| **G5-TECH 阴性对照** | 🟢 绿 | 往 support 注入一句新的实现描述 ⇒ **红 FAIL,命中 2**(GitHub / Cloudflare)⇒ 清空豁免没把闸变哑(§5) |
| **渲染器单测 + 双端同源** | 🟢 绿 | `test_render.mjs` **46 passed / 0 failed**;`build.py` 三项自检过(渲染器三副本 sha 一致 / class 白名单 / Function 侧组装 == `docs/*.html` 逐字节) |
| **App 内「联系我们」页文案** | ⚪ **没跑** | 本轮只动**网页四页**;App 内那一页的文案与 ASC 后台入口文案**没看也没改**,仍是颗粒 7 §8③ 的待办 |
| **重复站 `xiaotiantian-legal.pages.dev`** | ⚪ **没跑** | 仍是 09-07 改版前内容(还挂着邮箱),owner 未裁,本轮未动 —— 连着欠三轮了 |

# 3. 实测数字

## 3.1 G9-BASE(生产 `/api/export`,四页 `from=kv`)

```
                改前(基线)                              改后(线上)
index      1358B sha=2fe8ea6a2d5abdc8        →     1304B sha=5440445fa4539097
privacy   25727B sha=b2fb6c6f7e4f2aec        →    23547B sha=2ea1d4a0cf0a7d26
terms     21015B sha=f2364b4978f2fbc6        →    21015B sha=f2364b4978f2fbc6   ← 一个字节没动
support    7242B sha=e35ffc0809b2f48c        →     6882B sha=7b0b27040de22afc
```

`/api/save` 三次回执全 `ok=true`(index / privacy / support;terms 没改所以没保存)。
保存后重新导出,**四页与本地 `content/` 逐字节相同**(`cmp` 0 差异),而 `content/` = 基线 + §3 清单。
基线旁证:开跑时四页 KV 正文与仓库 `content/` 也是 `cmp` 0 差异 ⇒ 颗粒 8 之后 owner 没有再在 `/edit` 上改过东西。
**但基线仍以 KV 导出为准,不以仓库为准** —— 这是"不覆盖 owner 改动"的执行方式。

变更行数(基线 → 线上,`diff` 计 `^[<>]` 行):

| 页 | 变更行 | 拆解 |
|---|---|---|
| index | 2 | 1 改(旧 1 + 新 1) |
| privacy | 13 | 4 处改(5 旧 + 5 新 = 10)+ 3 段整段删(3 旧) |
| support | 6 | 3 处改(3 旧 + 3 新) |
| terms | **0** | — |

## 3.2 G9-DETAIL / G9-MAIL 尺子(两个方向都动过)

| 尺串 | 改前(KV 四页) | 改后(KV 四页) | 改后(线上八页) |
|---|---|---|---|
| `Cloudflare` | 3 | 0 | 0 |
| `GitHub` | 2 | 0 | 0 |
| `工单` | 6 | 0 | 0 |
| `issue` | 0 | 0 | 0 |
| `发送记录` | 2 | 0 | 0 |
| **合计** | **13** | **0** | **0** |
| `@126.com` / `@qq.com` / `邮箱`(颗粒 7 的尺子) | 0 | **0** | **0** |
| `14 个工作日内`(颗粒 8 的话术,**必须不为 0**) | 3 | **3** | **6**(每站 3) |

阴性对照要求 ≥3,实得 **13**(5 串里 4 串有命中)⇒ 这把尺子在改动前**确实看得见**那些说法,
不是"搜不到 = 尺子坏"。反方向也验过:`14 个工作日内` 与入口话术这两条**必须不为 0** 的,改后照样在。

⚠️ 中途有一次**假红**值得记:第一次跑线上尺子时,备份站(GitHub Pages)还在发**改版前**的
`index` / `privacy`(privacy 25728B = 旧尺寸),于是线上八页合计还剩 `Cloudflare 3 / GitHub 1 / 工单 4 / 发送记录 2`。
不是没删干净,是**备份站构建慢了一拍**。轮询到它追平(privacy 23548B / index 4935B)后重跑,才是上表的 0。
**教训**:两处托管的"线上"闸必须等两边都追平再判,否则会把发布延迟读成改动没生效。

## 3.3 G9-LIVE 两地可达

```
主站 Cloudflare(无 .html,零跳转)          备份站 GitHub Pages(带 .html)
  index    200 redirects=0   4935B          index    200   4935B
  privacy  200 redirects=0  23548B          privacy  200  23548B
  terms    200 redirects=0  21016B          terms    200  21016B
  support  200 redirects=0  10531B          support  200  10531B
八页与 docs/ 逐字节:全部 OK
```

## 3.4 G9-TERMS0 / G9-SHOT 截图证据(mini 上 `tools/shot`,900pt 宽,@2x)

改前 = 从 `HEAD~1:docs/*.html` 取出、经本机 HTTP 服务渲染;改后 = 取**线上**页面(带 cache-buster)。

| 页 | 改前高 | 改后高 | 逐像素 |
|---|---|---|---|
| index | 2000 | 2000 | **99.58% 相同**,差异**只在一个框内** `(164,448)–(1631,536)` = 被改的那一段 |
| privacy | 11426 | 10488 | 尺寸不同(−938 px)⇒ 内容确实变了 |
| terms | 9342 | 9342 | **100.00% 相同,diffbox=None** |
| support | 5652 | 5540 | 尺寸不同(−112 px) |

**terms 那行同时是三件事的证据**:①terms 真的 0 改动(字节 + 像素双证);②"改前"与"改后"两条
渲染链路(本机 HTTP vs 线上 Cloudflare)**可比** —— 否则它不会 100.00%;③截图工具**没吃缓存**,
因为同一次运行里它对 privacy/support 画出了新版、对 terms 画出了旧=新的同一版。

## 3.5 复跑的两套老闸

```
gate_g8.mjs   20/20 ✅  含 G8-METRIC privacy/terms 与 support 7 项排版指标全等(body lh=28.799999px)
                        含 G8-CASCADE 阴性对照(修复前字节必须红)· G8-STRUCT 四页各存一次再复原
gate_g6.mjs   15/15 ✅  含 G6-CACHE(max-age=0, must-revalidate,不含 stale-while-revalidate/s-maxage)
                        含 G6-ONEWORD 四页阴性对照(旧式归一化链路必须抓到 26/458/522/108 行重排)
```

两套闸都会**真往线上写一次再改回去**;两者的「收尾」项都验了 **KV 四页正文与开跑前逐字节相同**。
我另外在两套闸跑完后又导了一次 KV,与 `content/` 仍 `cmp` 0 差异 ⇒ 闸没有把 G9 的改动搅掉。


# 4. 逐句改动表(9 处,由真实 diff 机器生成)

### privacy(4 处)

| # | 行 | 改前 | 改后 |
|---|---|---|---|
| P1 | 115 | <li>全程不联网，唯一的例外是「联系我们」：只有在您提交反馈、查看开发者回复、或追着回复一句时才联网（共三处，全部由您的操作触发，见下方第一节）</li> | <li>全程不联网，唯一的例外是您主动使用「联系我们」时（见下方第一节）</li> |
| P2 | 127–131 | <p>我们 <strong>不收集</strong> 用户的任何个人身份信息，包括但不限于：姓名、手机号、身份证号、生日、学校、班级、家庭住址、精确或粗略地理位置、广告标识符（IDFA）、Apple 提供的设备标识（IDFV / UDID）等。本应用<strong>没有账号体系，无需注册也无法登录</strong>，全部功能均可直接使用。（您主动提交「联系我们」时会附带一个<strong>由本应用自己随机生成</strong>的设备编号，它不是上述任何一种标识，详见下一段。）</p> <br> <p><strong>唯一的例外是「联系我们」。</strong>当您在 App 内「小天天 → 联系我们」填写内容并 <strong>主动点击提交</strong> 时，我们会把您填写的问题描述与联系方式，连同下列排查信息一起发送：App 版本号与内部版本编号、设备型号（形如 <code>iPhone15,2</code>，同一型号的所有手机完全相同，不是您这一台的编号）、系统版本、语言设置、当前会员档位、最近一次跳绳的模式与时长、次数、时间（中考模式下的省份、年级、性别<strong>不发送</strong>）、相机 / 麦克风 / 照片读取 / 照片写入 / 健康这五项权限的授权状态、App 自身的运行日志（其中的文件路径、系统内部标识、电子邮件地址与手机号已在发送前抹去）、购买快照（会员档位、商品编号、购买与到期时间、Apple 为这笔交易签发的原始交易号，用于核对购买与退款问题）与安装渠道快照（这个包是从 App Store 还是测试渠道装的；描述文件里的设备 UDID 列表<strong>一个都不发送</strong>）。</p> <br> <p>此外还会附带一个<strong>由本应用在您首次使用时随机生成、只保存在本机钥匙串里的设备编号</strong>：它不来自广告标识符，也不来自 Apple 提供的任何设备 ID，只有我们的接口认得它；它的用途只有两个 —— 把同一台设备的多次反馈接进<strong>同一条对话</strong>，以及防止通过卸载重装重复领取免费次数。</p> <br> <p>这些内容发送到我们部署在 <strong>Cloudflare</strong> 上的接口；该接口会把它变成开发者 <strong>GitHub 私有仓库里的一条工单</strong>，开发者的回复会回传到 App 内显示。<strong>我们不再为此发送任何电子邮件，也不再经过任何自建服务器。</strong>由此产生的网络请求共 <strong>三处</strong>（提交反馈、查看回复、追加回复），它们是本应用 <strong>仅有的</strong> 联网行为，且<strong>全部由您的操作触发</strong>：不携带广告标识符、不携带 Apple 提供的设备唯一标识、不携带任何账号信息，不设置 Cookie，失败也不重试；您不提交，它们就永远不会发生。</p> <br> <p>提交的内容与上述排查信息会保存在那条工单里，以及 Cloudflare 侧一份 <strong>保留 30 天后自动删除</strong> 的发送记录里（该记录中不含 IP 地址）。与访问任何网站一样，发送时服务器会看到您当次的 IP 地址：写进工单的那一份<strong>已抹去末段、只保留到网段</strong>；完整地址仅用于一个 <strong>10 分钟后自动过期</strong> 的防刷计数，不作任何其他用途，也不与您提交的内容关联留存。</p> | <p>我们 <strong>不收集</strong> 用户的任何个人身份信息，包括但不限于：姓名、手机号、身份证号、生日、学校、班级、家庭住址、精确或粗略地理位置、广告标识符（IDFA）、Apple 提供的设备标识（IDFV / UDID）等。本应用<strong>没有账号体系，无需注册也无法登录</strong>，全部功能均可直接使用。（您主动提交「联系我们」时会附带一个反馈对话编号，它不是上述任何一种标识，详见下一段。）</p> <br> <p><strong>唯一的例外是「联系我们」。</strong>当您在 App 内「小天天 → 联系我们」填写内容并 <strong>主动点击提交</strong> 时，我们会收到您填写的问题描述与联系方式，以及为回复与排查所必需的技术信息：App 版本、设备型号、系统版本、语言设置、当前会员档位、最近一局的跳绳记录、相关权限的开启状态、应用运行日志（其中的电子邮件地址与手机号已在发送前抹去）与购买状态，以及一个仅用于把同一台设备的多次反馈接进同一条对话的编号（它不是广告标识符，也不是 Apple 提供的设备标识）；与访问任何网站一样，发送时也会带上您当次的 IP 地址。这些信息<strong>只用于回复您和排查问题，不用于任何其他目的，不用于营销，也不会提供给第三方</strong>。相关网络请求<strong>全部由您的操作触发</strong>，您不提交就不会发生。</p> |
| P3 | 206 | <li>购买状态由 <strong>StoreKit 2</strong> 校验，仅在本设备本地缓存有效期标识，日常使用不上传到任何服务器；只有在您主动提交「联系我们」时，才会把会员档位、商品编号、购买与到期时间、Apple 签发的原始交易号随反馈一并发出，用于核对购买与退款问题。</li> | <li>购买状态由 <strong>StoreKit 2</strong> 校验，仅在本设备本地缓存有效期标识，日常使用不上传到任何服务器；只有在您主动提交「联系我们」时，才会把购买状态随反馈一并发出，用于核对购买与退款问题。</li> |
| P4 | 234 | <li><strong>删除已提交的反馈：</strong>如需删除您提交过的反馈及随附的排查信息，通过本政策下方的联系方式告知我们即可，我们会删除对应的工单；Cloudflare 侧那份发送记录满 30 天自动删除，无需申请。</li> | <li><strong>删除已提交的反馈：</strong>如需删除您提交过的反馈及随附的技术信息，通过本政策下方的联系方式告知我们即可。</li> |

### support(3 处)

| # | 行 | 改前 | 改后 |
|---|---|---|---|
| S1 | 4 | <p>“小天天练跳绳”用 iPhone 相机帮孩子数跳绳次数，并对照公开的体测评分标准给出对应分数。所有识别都在手机本机完成，<strong>不上传</strong>；除了您在「联系我们」里提交反馈、查看回复、追加回复这三处，本 App 不联网。</p> | <p>“小天天练跳绳”用 iPhone 相机帮孩子数跳绳次数，并对照公开的体测评分标准给出对应分数。所有识别都在手机本机完成，<strong>不上传</strong>；除了您主动使用「联系我们」时，本 App 不联网。</p> |
| S2 | 42 | <p><strong>不需要注册，本 App 没有账号体系</strong>，也没有接入任何第三方统计 / 广告 SDK。姓名、学校、位置一概不收集。<strong>唯一会联网的地方是「联系我们」</strong>（提交反馈、查看回复、追加回复三处，都由您的操作触发）：您填好点提交时，才会把您写的问题描述与联系方式，连同 App 版本、机型（形如 <code>iPhone15,2</code>，同型号手机都一样）、系统版本、语言设置、会员档位、最近一局的时长与次数、五项权限的开关状态、已抹去路径与联系方式的运行日志、购买与安装渠道快照发给我们，用来排查问题；同时会带一个<strong>本 App 自己随机生成、只存在本机的设备编号</strong>，用来把您的几次反馈接进同一条对话（不是广告标识符，也不是 Apple 的设备 ID）。这些内容会变成开发者 GitHub 私有仓库里的一条工单，<strong>不再经过我们的服务器、也不再发送电子邮件</strong>。逐项清单详见 <a href="./privacy.html" class="text-blue-600">隐私政策</a>。</p> | <p><strong>不需要注册，本 App 没有账号体系</strong>，也没有接入任何第三方统计 / 广告 SDK。姓名、学校、位置一概不收集。<strong>唯一会联网的地方是「联系我们」</strong>：您填好点提交时，才会把您写的问题描述与联系方式，连同排查所需的技术信息（App 版本、机型、系统版本、语言设置、会员档位、最近一局的跳绳记录、相关权限的开启状态、已抹去联系方式的运行日志与购买状态）发给我们，<strong>只用来回复您和排查问题</strong>；同时会带一个只用于把您的几次反馈接进同一条对话的编号（不是广告标识符，也不是 Apple 的设备 ID）。完整说明详见 <a href="./privacy.html" class="text-blue-600">隐私政策</a>。</p> |
| S3 | 57 | <!-- ── 联系方式(★ owner 令:外部页面不写任何电子邮件地址,统一导流到 App 内工单) ── --> | <!-- ── 联系方式(★ owner 令:外部页面不写任何电子邮件地址,也不写联系链路的实现细节) ── --> |

### index(1 处)

| # | 行 | 改前 | 改后 |
|---|---|---|---|
| I1 | 3 | <p>用 iPhone 相机识别跳绳动作、自动计数，并对照公开的体测评分标准给出对应分数。<strong>识别全程在手机本机完成，不上传、无账号、无广告、无第三方 SDK。</strong>除您在 App 内使用「联系我们」（提交反馈、查看回复、追加回复三处）之外，本 App 不进行任何网络请求。</p> | <p>用 iPhone 相机识别跳绳动作、自动计数，并对照公开的体测评分标准给出对应分数。<strong>识别全程在手机本机完成，不上传、无账号、无广告、无第三方 SDK。</strong>除您在 App 内使用「联系我们」之外，本 App 不进行任何网络请求。</p> |

### terms(0 处)

**0 处改动**(逐字节与基线相同)。


# 5. 一处口径改动(不藏着):`TECH_ALLOW_20260908` 清空

颗粒 7 为了让 `G5-TECH` 放行"必须点名反馈接收方"那几句,开了 5 条**逐字审过的豁免**:

```
'部署在 <strong>Cloudflare</strong> 上的接口'
'开发者 <strong>GitHub 私有仓库里的一条工单</strong>'
'Cloudflare 侧一份 <strong>保留 30 天后自动删除</strong> 的发送记录'
'Cloudflare 侧那份发送记录满 30 天自动删除'
'开发者 GitHub 私有仓库里的一条工单'
```

**这 5 条豁免的对象,正是本轮 owner 要求删掉的那几句。**对象没了,豁免就成了一个只会
**遮挡未来新违规**的空口子 ⇒ 本轮把它**清空**(`TECH_ALLOW_20260908 = []`),`G5-TECH` 恢复满强度。

两个方向都验过:
- 清空后对现网四页跑 → **绿,0 命中**(说明确实不再需要豁免,不是靠豁免过的)。
- 往 support 注入一句新的实现描述「您的反馈会发到 Cloudflare 上的接口,变成开发者 GitHub 私有仓库里的一条工单。」
  → **红 FAIL,命中 2**(`GitHub` / `Cloudflare`)⇒ 闸没被清哑。

**词表一个字没放宽**(`TECH_BAN` 原样),放宽的相反 —— 是把颗粒 7 开的口子关上了。

# 6. 诚实栏

1. **「用户需要知道什么」是我按任务书规则划的线,不是 owner 逐句点头的。** 任务书给了三条
   (入口 / 收什么用来干什么 / 时效)与一张删除清单,清单外的同类项(渠道快照、Cookie、
   中考模式字段、机型举例等)是我**按同一把尺子推的**。若 owner 觉得某句该留,改回来是一行的事。
2. **合规底线是我的判断,没有律师看过。** 我保留了「收集什么 + 用于什么」这一句概括,并且**特意
   保留了 IP 这一项**(改成"与访问任何网站一样,发送时也会带上您当次的 IP 地址",去掉了抹末段/
   10 分钟防刷的机制描述)—— 因为 IP 确实会被收到,整句删掉会变成漏披露。同理保留了"有一个
   反馈对话编号、它不是 IDFA/IDFV/UDID"。**这两处是我在"删细节"和"别漏披露"之间的取舍点,
   最值得 owner 复核。**
3. **删掉的东西里有一部分是"对用户有利的承诺"。** 例如「30 天后自动删除」「IP 已抹末段」
   「UDID 列表一个都不发送」「不设置 Cookie,失败也不重试」—— 它们是**实现细节**没错,但也是
   **对用户的保证**。删了之后**行为没变,只是不再写在纸上**。这是 owner 的取向决定的,我照做,
   但把它明确记在这里。
4. **文案变长了一处**:privacy 那段虽然由 4 段并成 1 段、净少 2180 字节,但**单段密度变高**
   (一段里塞了字段类别 + 编号 + IP + 用途 + 触发)。读起来不如原来分段清爽。若 owner 觉得挤,
   可以拆成两段(会收到什么 / 怎么用),不影响任何闸。
5. **`terms` 0 改动这件事,颗粒 7 栽过一次同样的跟头**(当时预注册写"terms 0 处",被 owner 追加
   裁决推翻成 1 处)。所以这轮我**没有只靠"我看了觉得没有"**:脚本里 `ASSERT_CLEAN` 会在 terms
   被改到时直接报错,尺子也对 terms 扫过(0 命中),并且用**逐像素 100.00%** 做了第三重。
   但这仍只证明"terms 里没有实现细节",**不证明 owner 不想在 terms 上做别的改动**。
6. **App 侧与后台没碰**:App 内「联系我们」页的文案、ASC 后台的支持 URL 与描述,本轮**一个字没看**。
   网页说"入口在小天天 → 联系我们",App 内那页自己怎么写的,**我没核对过**。
7. **重复站 `xiaotiantian-legal.pages.dev` 仍是旧内容、仍挂着邮箱地址**,与 owner 的两条裁决
   (不写邮箱 / 不写实现细节)都冲突。颗粒 4、7、8 都登记过,**连欠三轮**,本轮仍未动(没有裁决)。
8. **口令处置**:编辑器口令全程只从 Air 现取、只在环境变量与 `/tmp/.g9pass`(chmod 600)里,
   **不落进本仓库、不进本回执**。本仓库是**公开仓库**(颗粒 3 踩过一次密钥外泄)。
   ⚠️ 遗留动作:`/tmp/.g9pass` 与 `/tmp/g9_*.mjs` 留在 mini 的 `/tmp` 里,重启即清;
   若要立刻清,`rm -f /tmp/.g9pass`。

# 7. 交给下一颗粒的

1. ★ **App 还不能发截图,所以隐私政策里没写截图 —— 这是有意的,不是漏了。**
   X123 颗粒 5 已经把服务端的图片链路做完了(R2 存图、90 天自动删),但我查了 App 侧
   (`wt-v3design` 分支 `RCContactRelayClient_claudecode_20260907.swift` + `X121G13ContractTests`):
   **只有 `contact` / `thread` / `reply` 三个接口,没有 `upload` / `attach`,也没有 `PhotosPicker`。**
   ⇒ 现在写"您可以附截图"就是假话。**等 App 真接上图片上传,privacy 与 support 必须同步补一句**
   (会收到您选择附上的截图 + 保留期)。这条同时回答了 `RECEIPT-X123-G5.md` §7④ 那个待办:
   **不是"文案漏了",是"功能还没上,现在写才是错的"**。
2. **App 内「联系我们」页与 ASC 后台的入口文案**是否与网页统一成「小天天 → 联系我们」——
   颗粒 7 §8③ 提过,仍未做,本轮也没做。
3. **重复站 `xiaotiantian-legal.pages.dev`**:删掉还是也发一份?**连欠三轮了**,建议这次给个裁决。
4. **§6② 那两处取舍(IP / 反馈对话编号)** 建议 owner 明确点头或推翻 —— 它们是本轮唯二
   "删细节"与"别漏披露"打架的地方。
5. `tools/shot.swift` 的缓存修复,颗粒 7 只验到一个方向;本轮 §3.4 用 terms 的 100.00% 补了
   **另一个方向的一半**(证明它不是"每次都画新的"),但**"给它一个真的陈旧缓存现场、看它不吃"**
   这个正面场景仍没造出来。

# 8. 复跑方式

```bash
cd /Users/cc/Public/x84sb/legal-site

# ① 从 KV 基线重放这 9 处(需先有基线导出目录)
python3 tools/edits_g9_claudecode_20260908.py --dry --src /Users/cc/x122/g9/baseline

# ② 对仓库 content/ 就地改(默认)
python3 tools/edits_g9_claudecode_20260908.py

# ③ 重建 docs/ + 三条文案闸 + 渲染器单测
python3 build.py && python3 tools/gates_g5.py docs && node tools/test_render.mjs

# ④ 两套老闸复跑(会真往线上写一次再改回去)
EDIT_PASSPHRASE=<口令> node tools/gate_g8.mjs     # 行距/层叠/结构 20 条
EDIT_PASSPHRASE=<口令> node tools/gate_g6.mjs     # 缓存/一词一改 15 条

# ⑤ G9-DETAIL 尺子(对 KV 正文目录或 docs 目录都能跑)
for w in Cloudflare GitHub 工单 issue 发送记录; do \
  printf "%-10s %s\n" "$w" "$(cat docs/index.html docs/privacy.html docs/terms.html docs/support.html \
  | grep -o "$w" | wc -l | tr -d ' ')"; done
```

口令在 Air `~/Downloads/xiaotiantian-legal-site/EDIT-PASSPHRASE.txt`。
截图产物在 mini `/Users/cc/x122/g9/shots/`,已同步 Air `~/Downloads/X122-G9/`。
