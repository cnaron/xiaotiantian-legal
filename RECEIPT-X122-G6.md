# 回执 · X122 颗粒 6 —— 「改了一次内容,整页行间距变了」的成因与修法

> 预注册:[PREREG-X122-G6.md](./PREREG-X122-G6.md)(动手前已 commit,门框没挪过;唯一一处
> 偏离在 §6,如实记账)。 2026.09.08 Naron

## 简短大白话解释

owner 在 `/edit` 里删了一行字,保存后说「字是改对了,但整页的行间距好像变了」。

**做了什么**:把「编辑框里打的字 → 存进云端 → 变成网页」这条链子一个字节一个字节对了四层
(云端存的原文、渲染出来的 HTML、线上真取回来的 HTML、浏览器里实际算出来的行高字号栏宽),
又把改动前后的整页截图逐行像素比了一遍。

**结论**:**这条链子是干净的,owner 那一次编辑一个字节都没被改坏。**隐私页和用户协议页
删前删后**逐像素相同的行占 100.00%**,唯一的差别就是他删掉的那一行(24 CSS px);行高数字
前后一模一样。真正让他觉得「整页变了」的是**浏览器缓存**:网页的缓存头里写着
`stale-while-revalidate=86400`,意思是「过期后 24 小时内,浏览器可以先把上次存的旧页直接
画给你看」。所以他编辑**之前**眼睛里那一版其实是 09:38 的旧页(日期还写着 2026年5月12日);
一保存,页面被迫取新,于是**一次跳了两个版本**——不只是他删的那一行,还有 10:11 那次全文
核实改写(隐私页 25 段变 33 段、页高 2776→3638 px,高了 31%)。他看到的「整体变了」是这个。

**修了什么**:不动渲染器、不动内容(他的内容本来就是对的,没有可恢复的东西)。改的是缓存
口径——`max-age=0 + must-revalidate`,浏览器每次都问一句,再也不许画旧页。用**真浏览器**
做了正反两组对照:旧头下保存完刷新看到的还是旧页(高度纹丝不动 7276),新头下同一个操作
立刻看到新页(7580)。另外补了 15 条一键可重跑的闸,证明「只改一个词,线上只变那个词」。

**顺带清了一笔账**:owner 那三处删改此前**只落在 KV 里**,仓库 `content/`、静态 `docs/`、
GitHub 备份站都还是旧的;现在五处已经逐字节追平。

---

# 1. owner 到底改了什么(先把事实摆清楚)

KV 历史里 09-08 的完整时间线(`/api/history` + KV 键名时间戳):

| 时刻 | 页 | 谁 | 内容 |
|---|---|---|---|
| 09:38:08 | privacy | 颗粒 4(我) | 换成 owner 原版整页,18034 B |
| 10:11:03–10:11:28 | 四页 | 颗粒 5(我) | 逐句核实改写,privacy 18034 → **21920 B** |
| **10:31:08** | privacy | **owner** | 删 1 行 |
| **10:33:16** | terms | **owner** | 删 1 行 |
| **10:34:38** | support | **owner** | 删 1 个 `highlight-box` + 2 行 `<p>` + 改 1 个 `<h2>` 文案 |

他改动前后两版 KV 原文的 `diff`(原文照贴,没有省略):

```diff
# privacy(10:11 → 10:31)
240d239
<                 <p><strong>App 内入口：</strong>设置 → 联系我们</p>

# terms(10:11 → 10:33)
278d277
<                 <p><strong>App 内入口：</strong>设置 → 联系我们</p>

# support(10:11 → 10:34)
5,8d4
<             <div class="highlight-box">
<                 <strong>需要人工帮助时：</strong>
<                 <p class="mt-2">在 App 内「设置 → 联系我们」写一句话提交，或直接发邮件到 <a href="mailto:shengtang009@126.com" class="text-blue-600">shengtang009@126.com</a>，我们通常在 2 个工作日内回复。</p>
<             </div>
63c59
<             <h2>三、 如何联系我们</h2>
---
>             <h2>三、其它</h2>
65,66d60
<                 <p><strong>联系邮箱：</strong><a href="mailto:shengtang009@126.com" class="text-blue-600">shengtang009@126.com</a></p>
<                 <p><strong>App 内入口：</strong>设置 → 联系我们</p>
```

**这就是全部**。没有多一个字节,也没有少一个字节:没有 `\r\n` 归一化、没有行首缩进被吃、
没有空行被合并、没有 HTML 实体转换、没有 `<br>`/`<p>` 增删、没有 `style`/`class` 被剥。

# 2. 成因取证(四层,层层逐字节)

| # | 断言 | 怎么量的 | 结果 |
|---|---|---|---|
| C1 | KV 原文差异 = owner 自己改的那几处 | `diff` 改前/改后两版 KV 正文 | ✅ 见 §1,三页各 1 处编辑意图 |
| C2 | 渲染器没多加/少掉任何标签 | 用同一份 `render.js` 离线渲染两版后 `diff` | ✅ 渲染后 HTML 的差异行与 C1 **逐行一致** |
| C3 | 线上取回的 HTML = 离线渲染结果 | `curl` 三页 vs 本地渲染,`cmp` | ✅ **逐字节相同**(privacy 21841 B / terms 20171 B / support 9531 B) |
| C4 | 浏览器里**实际生效**的行高没变 | WKWebView 内 `getComputedStyle` 扫全量元素(新写的 `tools/probe`) | ✅ 见下表 |

## 2.1 C4 —— 「行间距」这三个字的直接测量

| 页 | 改前行高取值集合 | 改后行高取值集合 | body 字号 | 正文栏宽 |
|---|---|---|---|---|
| privacy | `{20, 21, 24}px` | `{20, 21, 24}px` | 16px | 800px |
| terms | `{20, 21, 24}px` | `{20, 21, 24}px` | 16px | 800px |
| support | `{20, 25.2, 28.8, 31.68, 36}px` | `{20, 25.2, 28.8, 31.68, 36}px` | 16px | 800px |

**前后完全一致**,连 `28.799999px` 这种浮点尾巴都对得上。元素几何的全量 `diff` 里,除了
被删掉的那几个元素本身,没有任何一个元素的高度/外边距发生变化。

> 顺带记一笔口径:privacy/terms 的行高是 **24px**(=1.5),不是它们自己 `<style>` 里写的
> `line-height:1.8`——因为 owner 原版页用的是 Tailwind Play CDN,而 Play CDN 的样式是**追加
> 到 head 末尾**的,`html{line-height:1.5}` 排在后面就赢了。颗粒 4 把这份 CSS 原样搬进来时
> 刻意保留了这个顺序,所以**这是忠实于原版的长相,不是本轮或上轮引入的变化**;
> index/support 用的是本站自己的样式表,行高 28.8px(=1.8)。两套页面本来就不同,
> 这一条**改前改后都成立**,与 owner 这次编辑无关。

## 2.2 逐像素:除了他删的那几行,一行不差

整页 WebKit 截图(900 CSS px 宽、2× 缩放),按**行哈希**做序列比对:

| 页 | 删前高 | 删后高 | 逐像素相同的行 | 差异带 |
|---|---|---|---|---|
| privacy | 7320 px | 7272 px | **7272 行 = 100.00%** | 1 处:删前第 6913–6961 行(24 CSS px)= 他删的那一行 |
| terms | 6510 px | 6462 px | **6462 行 = 100.00%** | 1 处:删前第 6214–6262 行(24 CSS px)= 他删的那一行 |
| support | 5604 px | 5100 px | 5062 行 = 99.25% | 4 处(见下) |

support 的 4 处差异带,与他的 4 处编辑一一对上:

| 差异带 | 位置 | 对应他的哪一处编辑 |
|---|---|---|
| delete 164 CSS px | 删前 614–942 行 | 删掉的 `highlight-box` |
| replace 18 CSS px | 4786–4823 ↔ 4458–4495 | `<h2>三、 如何联系我们` → `三、其它` |
| delete 88 CSS px | 删前 4906–5082 行 | 删掉的 2 行联系方式 `<p>` |
| replace **1 个像素** | 5536 ↔ 5032 | **1 个像素、单通道差 1**(226→227)= 卡片圆角的抗锯齿取整,不是排版变化 |

**⇒ C5:编辑链路没有重排。owner 那次编辑没有任何一个字节被改坏,也没有一处行距被动过。**

# 3. 那「整体变了」是哪来的 —— 浏览器缓存

## 3.1 直接抓到的现行犯

改之前,页面的缓存头是:

```
Cache-Control: public, max-age=300, s-maxage=600, stale-while-revalidate=86400
```

`stale-while-revalidate=86400` 的语义是:**内容过期之后的 24 小时内,浏览器可以先把上次
存的旧页直接画出来给你看,同时在后台悄悄更新**。

实测(10:46,带持久磁盘缓存的 WKWebView,和 Safari 同一个引擎):

| 同一分钟、同一个 URL | 拿到的是哪一版 | 页高 |
|---|---|---|
| 普通取 `/privacy` | **09:38 的旧页**(日期还写着 2026年5月12日) | 5554 device px |
| 加 cache-buster 取 `/privacy?cb=…` | 当时的最新页 | 7276 device px |
| `curl`(没有缓存) | 当时的最新页 | — |

**同一时刻,浏览器画的是 68 分钟前的旧页。** 这就是 owner 编辑之前眼睛里那一版。

> 这不是工具的毛病:`~/Library/Caches/shot/WebKit/NetworkCache` 是一个 9.7 MB 的**磁盘持久
> 缓存**,和 Safari 的行为一致。新编的 `tools/probe` 因为缓存目录是空的,取到的一直是新页
> ——两边的差别本身就是这条机制的旁证。

## 3.2 于是 owner 那一刻看到的是「一次跳两个版本」

10:11 的全文核实改写(颗粒 5)写进 KV 时,**没有走 `/api/save`**(是脚本直接写 KV),
所以没有任何一步去动他浏览器里那份缓存;他的浏览器就一直画 09:38 那一版。等他 10:31
在编辑器里删掉一行、点「保存并发布」、再去看线上——这一次是新取的,于是他**同时**看到了:

- 颗粒 5 那次全文改写:privacy 从 **25 段变 33 段**,页高 **2776 → 3638 CSS px(+31%)**;
- 加上他自己删的那一行。

整页的文字密度、段落数、换行位置全变了。「总体行间距变了」说的就是这个。

## 3.3 附带发现:「保存即发布」里的清缓存是空操作

`/api/save` 每一次的回执都是:

```json
{"ok":true,"ts":…,"trimmed":0,"purged":[["/privacy",false],["/privacy.html",false]]}
```

`false` = `caches.default.delete()` 没删到任何东西(Pages Functions 的动态响应本来就不进
那一层边缘缓存)。也就是说这段清缓存代码**从上线起就没起过作用**。修完之后它已经无关紧要
(边缘不再被允许留旧页),代码留着不碍事,但账记在这里。

# 4. 修法(只改两处 + 加一套闸)

| # | 文件 | 改动 |
|---|---|---|
| F1 | `functions/_lib/page.js` | 页面响应头 → `Cache-Control: public, max-age=0, must-revalidate`(**删掉 `s-maxage` 与 `stale-while-revalidate`**);加正文 SHA-256 的弱 `ETag` + `If-None-Match` 回 304 |
| F2 | `docs/_headers` | 静态回退那一份同样改 `max-age=0, must-revalidate`;`/assets/*` 字体的一年长缓存**不动**(文件名带内容指纹,长缓存是对的) |
| F3 | `tools/gate_g6.mjs`(新增) | 15 条一键可重跑的闸,含 4 条阴性对照 |
| F4 | `tools/probe.swift`(新增) | WKWebView 里跑 JS 量真实布局(computedStyle / 字体加载 / 元素几何),带 `cache` / `nocache` 开关 |

**没改**:`template/render.js` 一行没动(C2/C3 已证明它是字节直通的);
**没改**:任何页面内容与样式;
**没恢复** owner 的内容——他的内容本来就是他想要的,链路没弄坏它。

## 4.1 修法的正反两组对照(用真浏览器,不是看头猜)

同一套四步动作(真浏览器普通取一次 → 经 `/api/save` 加一段明显把页面撑高的内容 → 同一个
浏览器**不带 cache-buster** 再取一次 → 看高度变没变),在两种缓存头下各跑一遍:

| | 步骤1 基准高 | 步骤2 保存后 | 步骤3 浏览器再取 | 判读 |
|---|---|---|---|---|
| **旧头**(临时回滚部署,阴性对照) | 7276 | 服务端已变(强制回源量得 7580) | **7276** | ❌ 浏览器吃了旧页 —— **owner 的现象复现成功** |
| **新头**(现役) | 7276 | 同样的改动 | **7580** | ✅ 保存完刷新立刻看到新页 |

并排图:`X122-G6/cache-negative-control.png`。
(阴性对照那一版旧头只在线上存在约 2 分钟,跑完立刻换回;两次改动都已逐字节改回,见 §5 G6-RT。)

# 5. 五条闸的成绩

`EDIT_PASSPHRASE=… node tools/gate_g6.mjs` —— **15 条全绿**:

| 闸 | 结果 | 数字 |
|---|---|---|
| **G6-SEC** | 🟢 绿 | 7 类 XSS × 片段/整页两模式 = **14 例全被转义**;另 `node tools/test_render.mjs` **46 passed, 0 failed** |
| **G6-CACHE** | 🟢 绿 | `Cache-Control: public, max-age=0, must-revalidate`,不含 `stale-while-revalidate` / `s-maxage`(ETag 一栏见 §6) |
| **G6-ONEWORD** | 🟢 绿 ×4 | 四页各改一个词、走**真的 `/api/save`**:线上 HTML 变化行数 = **2**(同一行的旧/新),且新行 = 旧行把那个词替换一次的结果。index「隐私→私隐」/ privacy「不收集→未收集」/ terms「用户→使用者」/ support「常见→常问」 |
| **G6-ONEWORD 阴性对照** | 🟢 绿 ×4 | 同一份输入换成「保存时做归一化」的旧式链路 ⇒ 差异 **26 / 448 / 512 / 104 行**,全都远大于 2 ⇒ 这条闸真的抓得到重排,不是永远绿 |
| **G6-RT** | 🟢 绿 ×4+1 | 四页各做「改一字→存→改回→存」往返,线上 HTML **逐字节复原**;收尾复核 KV 四页正文与开跑前**逐字节相同** |
| **G6-SYNC** | 🟢 绿 | 见 §5.1 |
| **G6-OWNER** | 🟢 绿(空操作) | §2 已证明 owner 的内容与行距都没被链路改过 ⇒ **没有需要恢复的东西**,本轮对他的内容一个字节没动。证据 = §2.2 的逐像素表 + 并排图 |

## 5.1 G6-SYNC —— 五处逐字节同

owner 那三处删改此前**只落在 KV**,仓库与备份站都还是 10:11 那一版。现在已追平:

| 页 | KV 正文 | `content/` | `docs/` | CF 线上 | github.io 备份站 |
|---|---|---|---|---|---|
| index | `6a16ae4b53` | 同 | `bf33329d38` | 同 | 同 |
| privacy | `39453176b5` | 同 | `47822086ee` | 同 | 同 |
| terms | `e98e0e9c9d` | 同 | `0858397513` | 同 | 同 |
| support | `2ff22ceda5` | 同 | `a920d187b5` | 同 | 同 |

两地状态码:`https://xiaotiantian-app.pages.dev/privacy` **200 / 21841 B**;
`https://cnaron.github.io/xiaotiantian-legal/privacy.html` **200 / 21841 B**。

GitHub 直推在 mini 上仍然是坏的(钥匙串锁 + `gh` token 失效,颗粒 4 已记),照旧绕 VPS 中转:
`71352f4..53c4b00 main -> main`,中转裸仓用完即删,VPS 上没留东西。

# 6. 与预注册的唯一一处偏离(如实记账)

预注册的 **G6-CACHE** 写的是「带 `If-None-Match` 的条件请求回 **304**」。**这一半做不到**:

**Cloudflare 会把 Pages Functions 响应上的 ETag 剥掉。** 实测六种组合全被剥——
强 ETag / 弱 ETag(`W/"…"`)× `Accept-Encoding: gzip` / `identity` × 生产域 / 预览域;
同一个 headers 对象里紧挨着的 `X-Content-Source` 却原样送到了客户端,所以不是代码没设上。

处理:
- 代码里的 ETag **留着**(无害;平台哪天不剥就立刻生效),注释写明实测被剥;
- **闸不能拿一个不由我们决定的东西当通过线** ⇒ G6-CACHE 改钉真正管用的那两条:
  头里没有任何允许画旧页的余量(无 `stale-while-revalidate`、无 `s-maxage`)+ `max-age=0` + `must-revalidate`;
- 「保存后刷新立刻看到新页」这件事本身,改由 §4.1 的**真浏览器正反对照**来验,比 304 更贴近产品承诺。

代价:每次访问都会回源取一次整页(约 21 KB),吃不到 304 那几百字节的便宜。
对一个法律页的访问量而言可以忽略;**正确性优先于这点带宽**。

# 7. 给 owner 的两条提醒(只提醒,没动)

1. **support 页现在没有任何联系方式了**:您把「需要人工帮助时」那一块和「联系邮箱 /
   App 内入口」两行都删了,但页面末尾还留着一句「我们通常在 2 个工作日内回复。」,
   而目录页对支持页的介绍还写着「常见问题与**联系方式**」。要不要一起调,请您定;
   本轮**没有替您改任何一个字**。
2. **App 里和 App Store 后台的链接仍然没指向这个站**(颗粒 2 起的老账,不在本轮范围)。

# 8. 产物路径

- 并排图(Air `~/Downloads/xiaotiantian-legal-site/X122-G6/`,mini 同份在 `/Users/cc/x122/g6/sheets/`):
  - `owner-edit-privacy.png` / `owner-edit-terms.png` / `owner-edit-support.png` —— owner 编辑前(颗粒 5 交付版)vs 编辑后(现在的线上)
  - `cache-negative-control.png` —— 旧缓存头 vs 新缓存头,同一套动作的正反对照
- 取证中间产物(mini `/Users/cc/x122/g6/`):`kv/`(各版 KV 原文)、`shots/`(全部截图)、
  `rt/`(往返闸的每一步)、`geom_*.txt`(元素几何全量表)、`export2.json`
- 可重跑:`EDIT_PASSPHRASE=<口令> node tools/gate_g6.mjs`

# 9. 没碰的

App、重复站、页面内容与样式、`template/render.js`、口令与 secret、GitHub origin 配置。
没发飞书通知。
