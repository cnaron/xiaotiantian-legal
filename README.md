# 小天天练跳绳 · 法律与支持页 + 反馈工单后端

> ⚠️ **这个仓库是公开的(PUBLIC)。任何口令 / key / secret / 私钥一律不许写进来。**
> 需要口令的东西一律放 Cloudflare Pages secret,交接项放 mini 本地 `/Users/cc/x123/`。

> **X129 颗粒 C1:已完成并上线(2026-09-10)。「联系我们」改成单向 —— 立即 200、KV 不存消息、
> `thread` / `reply` / `sendlog` 三条路由下线。**
> owner 令:「① 不限制用户发送,想发就发,点击发送立即响应,审核在过程中处理,不需要让用户感知;
> ② KV 不需要记录,因为不需要给用户回复;③ 处理审核后的消息还是留到 GitHub 的 issues 里,
> 用唯一 ID 追加写入;④ 用户只管发,我们只管查收和部分解决,不作响应处理。」
>
> · 现在这个站只剩**一个**反馈接口:`POST /api/feedback/contact`,成功回 **`{"ok":true}`**
>   (没有工单号/token/状态),实测 p50 **7 秒 → 1 秒**;
> · 审核 / 限流 / 拉黑一律**静默丢弃**(照回 200),用户看不到任何失败;
> · KV 总键数 **327 → 103**,`sendlog:`(用户正文 + App 日志)191 条**当场清零**,
>   与反馈有关的只剩 `ticket:<deviceId>` → issue 号这一条索引;
> · ★★★ **重复工单**:立即 200 之后并发会开出两条 —— KV 读后写**没有原子性**,占位锁只降概率。
>   正确性靠**事后收敛**:列出该设备所有 open 工单、**号最小的留下**、大号并进去并关掉。
>   口径:**并发首帖允许短暂重复,任一后续提交之后必收敛为一个 open issue。**
> · ★ 法律站现在还写着「我们会在 App 内回复」「14 个工作日内回复」,与新契约**不符** ——
>   已列成 9 条清单交 owner 定,**本颗粒一个字没改**。
>
> 完整交付说明与闸账(12 条全绿)见 [`RECEIPT-X129-C1.md`](./RECEIPT-X129-C1.md),
> 预注册见 [`PREREG-X129-C1.md`](./PREREG-X129-C1.md)。

> **X123 颗粒 8:已完成并上线(2026-09-09)。服务端不再收 IP + 法律站权限表 5 → 3(相册两条下线)。**
> owner 令:「隐私协议里提到联系我们会上传 IP 地址,这个点不用提了,或者去掉这样的敏感信息提交。
> 同时我们现在也不用上传图片或视频,也无需用户下载保存,所以这些都不需要申请权限,也不需要提及。」
> 追加令(14:50,看了截图):那一段不要罗列这么多细节,尤其「运行日志(其中的电子邮件地址与手机号
> 已在发送前抹去)」这种括号说明 —— 反而像有很多敏感信息;新措辞由 owner 给定,**逐字照抄**。
> **服务端是真的不收了,不是只删文案**:`clientIp_` / `maskIp_` / `auth.js clientIp` 三个函数**删掉**,
> 全仓 `grep CF-Connecting-IP|X-Forwarded-For` **2 → 0**;限流键 `fbrate:<桶>:<IP>` →
> `fbrate:<桶>:<主体>`(contact = 设备编号 / reply·thread = 工单号 / sendlog = 常量 owner),
> **阈值一个没动**。`/edit` 登录防爆破没有主体可锁 ⇒ 改成「全局计数 + 递增延时,不硬锁」
> (口令对的永不被锁),**旧 `G-SEC` 本轮改判**,取舍见 RECEIPT §4。
> 法律站改 9 处:privacy 6 / index 1 / support 2 / **terms 0**(sha256 逐字节相同)。
> 尺子 `IP 地址|相册|Photos|照片|电子邮件|邮箱|抹去|运行日志` 在 **KV / 线上渲染 / 编辑器**
> 三面各 **0**(改前 KV 13、编辑器面 18);权限条目 **5 → 3**;单测 177 → **182**。
> 闸 19 条:**18 绿 · 1 条没跑**(线上 `/edit` 要口令,口令在 Air)。
> 完整交付说明见 [`RECEIPT-X123-G8.md`](./RECEIPT-X123-G8.md);
> **★★★ 最要紧的两条(在其 §5)**:①**App build 118 仍申请相册读/写权限、结果页仍有「保存到相册」**
> ⇒ 现在是「App 要权限、政策不提」,**提审前必须先由 X121 删掉**;②新措辞点名 6 样,
> 服务端实际还会收到**语言设置 / 权限开启状态 / 购买与渠道快照**三样没点名的,**两个选项待 owner 裁**。
> ★ 三个可复用的坑(其 §6):**同一把「剥 HTML 注释」的尺子在三个面各有一种转义形态**(原样 /
> `&lt;` / `\u003c`),我被绊了两次;**`grep` 在 `tools/test_feedback.mjs` 上默默返回空**
> (`file` 判成 `data`,要 `grep -a`)⇒「没命中」不等于「不存在」;**删函数没删干净时
> `node --check` 会给假绿**(看不见 import 了不存在的导出),要用 `node -e "import(…)"` 真加载。

> **X122 颗粒 10:已完成并上线(2026-09-09)。法律站权限表 6 → 5,麦克风整条下线。**
> App 侧 X121 颗粒 18(build 117,已 OTA)把麦克风**彻底删掉**(不申请权限 / 录制不写音轨 /
> 导出产物读回 0 音轨 / 包内权限串恰好 5 条)⇒ 线上隐私政策里「麦克风权限」那一条已经是**假话**。
> 本轮删 `<li>` 整块(标题+用途+说明 5 行)、把「一条**带画面与声音**的视频」改成
> 「一条视频(**只有画面,不含声音**)」、撤回权限路径去掉「麦克风」、支持页常见问题 4 的答案
> 由「会」整个反成「不会」(**问题保留** —— 用户还是会问)、目录页「6 项系统权限」→「5 项」、
> privacy 页顶日期 → 2026年9月9日。**改 7 处:privacy 5 / support 1 / index 1 / terms 0**
> (terms 改前改后**截图 PNG 逐字节相同**,同时证明两条渲染链路可比、截图没吃缓存)。
> 尺子 `麦克风` 在 **KV 正文 / 线上渲染 HTML / 编辑器** 三个面各 **7 → 0**(改前的 7 就是阴性对照);
> `声音` 8 → 4 且**剩下的全是「没有声音」这个方向**(脚本白名单硬闸)。
> 老闸复跑 `gate_g8.mjs` **20/20**(行高 28.799999px 没动)、`gate_g6.mjs` **15/15**、G5 三闸全 0。
> 完整交付说明见 [`RECEIPT-X122-G10.md`](./RECEIPT-X122-G10.md);**7 条诚实边界**见其 §6,
> 最要紧的两条:**①`录音` 那半把尺子改前就是 0 ⇒ 本轮没有判别力**,承重的只有 `麦克风` 的 7→0;
> **②ASC 后台的权限用途说明还写着麦克风**,不在本仓库,**下次提审前必须删**(其 §7①)。
> ★ 两个可复用的坑(都在 dry run 阶段被自己的闸逮住,没进线上):
> ① **「我们不申请麦克风权限」这种否定句也会撞「麦克风 = 0」的尺子** —— 写「删掉某个词」类文案前,
>    先想清楚尺子量的是**词**还是**事实**;② 全角标点闸第一版扫整页,红在页面里既有的
>    **Tailwind CSS 注释**上 —— **红的是尺子**:owner 的全角令管的是用户看得见的文案,
>    不是 CSS 与 HTML 注释(也正对上「改到哪屏统一哪屏,不做全仓」)。

> **X123 颗粒 7:已完成并上线(2026-09-09)。两件事:issue 排版重做 + 「拉黑」与「预审」。**
>
> **① 排版重做**(owner 真机开了 issue #7 后当场提的):**标题 = 用户原话**(去换行、超 60 字加「…」),
> 正文第一屏 = 原话的**引用块 + 大字号**,抓来的元数据全进「设备与诊断信息」折叠块,
> 设备号从明文 `device: xxx` 改成 **HTML 注释**(页面看不见,GitHub 全文搜索**实测仍命中**)。
> `firstPostBody` **两种排版都认**(#1–#7 是旧排版,thread 还要读它们),拿 issue #7 的真正文跑过。
> 「会员档」只给一个说法(**以 `payment.plan` 为准**),带 `devOverridePlan` 时 **DEV 模拟档单列一行**
> —— issue #7「forever vs free」的根因 = 那台设备开 DEV **模拟了 Pro**,App 侧 override 顶掉顶层
> `plan`、真实扫描那条路提前 return ⇒ `payment.plan` 停在初值。
> 顺带修掉:评论前缀 `[用户消息 #2]` 里的 `#2` 会被 GitHub **自动链成「引用 issue #2」**,
> 等于拿别人的工单当留言板 ⇒ 去掉井号(新前缀是旧前缀的**真前缀**,一行 `startsWith` 同时认新老)。
>
> **② 两道门**:**拉黑** —— owner 在 GitHub 贴 `blocked` 标签 ⇒ contact/reply **403**、
> **thread 仍可读**(标签 KV 缓存 5 min,实测撕标签后 **≤293s** 恢复);
> **预审** —— 先过 7 类词表,再过 Workers AI `llama-guard-3-8b`(3 秒超时),
> 拒 S1/S2/S3/S4/S9/S10/S11/S12,**故意不拒** S5/S6/S7/S8/S13/S14;
> 拒绝回 `400 {"ok":false,"err":"rejected"}` **不给理由**、不开 issue,sendlog 标红并显示类别;
> 同主体拒 **3 次自动贴 `blocked`** + 留一句系统说明。**AI 超时/出错/无绑定 ⇒ 放行**并记 `ai_unavailable`。
> 闸 24 条(预注册 20 + 补跑 4)**全绿**,单测 80 → **177**,法律四页与编辑器**逐字节不变**。
> 详见 [`RECEIPT-X123-G7.md`](./RECEIPT-X123-G7.md)。
> ★ 三个可复用的坑 / 待办:
> ① **Workers AI 会对同一句话前后给不同判** —— 线上把无辜的「解封探测 11/12」判成 S9 拒掉,
>    十几分钟后同样两句 5/5 判 safe ⇒ 已加 `temperature: 0` + **判不安全要复核一次**(两次都说才拒);
> ② **延迟方差极大**:n=23,p50 1875ms 但 **39% 超过 3 秒** ⇒ 按现设置约 4 成提交实际不过 AI 这道门。
>    **要不要把门限从 3 秒放宽到 8 秒,等 owner 拍板**(改一个常量),见回执 §8;
> ③ 阴性/反向对照当场抓出两个真 bug:`fuck` 加尾部边界会漏 `fucking`;去空格那一遍把
>    「s b 两个变量」误判成 `sb` ⇒ 拼音缩写整类退出去空格那一遍。**阳性样本全绿时,词表已经既漏又误伤。**

> **X123 颗粒 6:已完成(2026-09-09 收尾)。图片能力整个撤掉 —— 颗粒 5 已作废。**
> owner 令:**不让用户上传图片**。⇒ `/api/feedback/{upload,attach,img/<key>}` 三个接口、
> `_lib/images.js`、`wrangler.toml` 里的 R2 绑定**全部删除**;`thread` 不再回 `images[]`
> (`contact` 的 `cid` **保留** —— reply 也回它、App 红点判据在用,与图片无关)。
> 唯一留下的图相关代码是 `_lib/feedback.js` 里的 `stripImageMarkdown`:**撤能力 ≠ 撤历史**,
> issue #5/#6 正文里已经写进了 `![截图](…)`,owner 在 GitHub 网页上拖图回复也会产生同样的写法,
> 不剥的话 App 气泡里会原样冒出这一行。拿两个真工单的**真正文**跑过:10 处图行全剥净、0 残留。
> 单测 95 → **80** 条。三条接口现在与「从来不存在的路径」**逐字节相同**(POST 405 / GET 404),
> 同一时刻 contact/thread/reply 仍正常(阳性对照)、上一个部署上三条仍活着(阴性对照)。
> **三件不可逆操作已于 09-09 执行完**(owner 明确「继续」后):R2 对象 **74 → 0**、
> **桶 `xiaotiantian-feedback` 已删**(两条独立路径都回 10006 不存在)、
> **重复站 `xiaotiantian-legal.pages.dev` 已删**(四页由 200 变 530,邮箱裸词从 10 处变 0;
> 同一时刻主站五页仍全 200)。⚠️ 其中 issue **#5/#6 的 9 张真截图不可恢复** ⇒ 在 GitHub 上看这两个
> 工单会是**裂图**,预期结果。闸 15 条 **14 绿 1 黄 0 红 0 没跑**,详见
> [`RECEIPT-X123-G6.md`](./RECEIPT-X123-G6.md)。
> ★ 四个可复用的坑:① **部署后立刻探测会读到旧 bundle**(约 1 分钟后才切,一次读数不能当证据);
> ② `wrangler r2 bucket info` 的 `object_count` **滞后**(两次实测:报 68 真实 74;列举已 0 它还报 29)
> ⇒ 清零只认列举;③ **「路由/站点没了」不等于 404** —— 不存在路径收 POST 回 **405**、
> 域名没有对应项目回 **530**,判据要写成「与从来不存在的东西不可区分 / 非 200」而不是写死状态码;
> ④ `wrangler pages project delete` 会交互式确认,自动化必须带 `--yes`。

> **X122 颗粒 9:已完成并上线(2026-09-08)。法律页不再讲「联系我们」是怎么实现的。**
> owner:「不需要写明是怎么联系我们的细节,很多用户不需要知道的内容无需告知。」⇒ 把
> **Cloudflare / GitHub 私有仓库工单 / 联网共三处 / 30 天发送记录 / IP 抹末段与 10 分钟防刷计数 /
> 设备编号的生成方式(随机生成·钥匙串) / 购买快照逐项(商品编号·Apple 原始交易号) /
> 安装渠道快照与 UDID 列表 / 日志脱敏字段 / 机型举例 `iPhone15,2`** 全部删掉。
> privacy 里讲这件事的**四大段压成一段**,support 那个长答案缩短,index 去掉「三处」。
> **改 9 处:privacy 4 / support 3 / index 1 / terms 0**(terms 本来就没写这些 —— 并排截图
> **逐像素 100.00% 相同、diffbox=None**,这同时证明截图工具没吃缓存)。
> 留下的只有三件:**①入口**(App 内「小天天 → 联系我们」)、**②会收到什么、用来干什么**(一句概括)、
> **③回复时效**(颗粒 8 话术,一字未改,闸验全站仍恰好 3 处)。
> 顺带把 `gates_g5.py` 的 `TECH_ALLOW_20260908` **清空** —— 那 5 条豁免的对象本轮整段删了,
> 留着只会遮挡未来的新违规;清空后 G5-TECH **恢复满强度**(词表一个字没放宽,阴性对照实跑仍红)。
> 老闸复跑:`gate_g8.mjs` **20/20**、`gate_g6.mjs` **15/15**(后者顺带清了颗粒 7 §8① 那笔债)。
> 完整交付说明见 [`RECEIPT-X122-G9.md`](./RECEIPT-X122-G9.md);**8 条诚实边界**见其 §6,
> 最要紧的两条:**①App 现在还不能发截图**(服务端能收、App 侧没做)⇒ 隐私政策**故意没写**
> 「您可以附截图」,等 App 接上必须补(这也回答了 X123-G5 §7④);
> **②删掉的东西里有一部分是「对用户有利的承诺」**(30 天自动删 / IP 抹末段 / UDID 一个不发)——
> **行为没变,只是不再写在纸上**,等 owner 过目。

> **X123 颗粒 5:⚠️ 已被颗粒 6 整体撤销(2026-09-08 当天)。下面这段只作历史记录读。**
> **X123 颗粒 5(原文):已完成并上线(2026-09-08)。工单可以带截图了。**
> 用户在「联系我们」里可以附截图 ⇒ 图存 Cloudflare **R2**(桶 `xiaotiantian-feedback`,私有,
> 90 天自动删)⇒ 工单帖子里写一行 `![截图](…/api/feedback/img/<key>)` ⇒ **GitHub 的图片代理
> 真的取到并显示**(用 camo 地址取回的字节与原图 sha256 逐位相同 —— 这比截图强)。
> 新增三个接口 `/api/feedback/{upload,attach,img/<key>}`,`contact` 多回一个 `cid`、
> `thread` 每条消息多一个 `images[]`(**都是纯新增字段**,老 App 忽略也照常工作)。
> 单测 70 → **95** 条(新增 25,其中 12 条是阴性/阳性对照)。
>
> ★ **先量了国内链路再动手,量出来的结论请 App 侧务必照做**:国内到 Cloudflare 的上传
> 只有 **≈100 kB/s**,而且落在**洛杉矶**边缘 —— 传 1 MB 要 **9.8 秒**(p95 11.9 秒),
> 传 300 KB 要 3.6 秒,同一件事从海外只要 0.7 秒(**14 倍**)。
> **这条链路服务端改不了**(Worker 就跑在那个边缘上,慢的是用户到边缘那一段)⇒
> **唯一的办法是 App 把图压小(≤300 KB,长边 1280 / JPEG q≈0.7)。**
> 另外实测推翻了一个想当然:**「四张并行传」并不快**(串行 20.0s vs 并行 16.9s,
> 差值淹没在 ±5.1s 的抖动里),而且并行 24 次里失败 2 次(串行 0 次)⇒ **建议串行 + 重试**。
> 顺手把服务端自己的记账挪出关键路径(首尔边缘交替 A/B **12/12 全部变快,合计 −1.22s**)。
> 完整交付说明见 [`RECEIPT-X123-G5.md`](./RECEIPT-X123-G5.md);
> **三件等 owner 拍板**见其 §7①(图片可见性:贴进 GitHub 后拿到网址就能看)、
> §7④(隐私文案没提图片,而图存 90 天)、§7⑤(历史部署上的临时测速端点)。
> ⚠️ **颗粒 6 起:①④两件随图片能力一并作废**(没有图片能力就没有可见性口径、也没有 90 天保留期问题);
> **⑤仍然挂着**(历史部署删不掉,颗粒 6 §7① 再次登记)。

> **X122 颗粒 8:已完成并上线(2026-09-08)。隐私政策 / 用户协议的行间距修好了。**
> owner:「行间距太小了,就是上次编辑之后出的问题…参考 support 页面。」⇒ 这次**不是缓存**,
> 是**两个 `<style>` 的层叠顺序反了**:颗粒 4 整页搬原版时,按原版 DOM 顺序把 Tailwind 生成的
> CSS(含 Preflight)放在了页面自己那个 `<style>` **之后**。两边同为元素级选择器、优先级相同
> ⇒ 谁在后面谁赢 ⇒ Preflight 把页面自己的排版全盖掉:行高 1.8→1.5(28.8px→24px)、
> **段落间距 16px→0**、`h2` 20px/600→16px/400、`ul` 圆点与缩进直接没了。
> support 页走 `template/style.css`(复位写在第 ① 段、**在前**)⇒ 一直是对的,所以两页长得不一样。
> **修法:只把两个 `<style>` 的位置调转**(CSS 一条没改、`<body>` 逐字节不变),根因修在生成器
> `tools/adopt_original.py` 的 A2。改完三页 **7 项排版指标逐项相等**。
> 另按 owner 令统一回复时效措辞(改前 support 写「2 个工作日」、privacy 写「十五个工作日」,
> **互相打架**)⇒ 三页统一成「**14 个工作日内**回复 + 保留不逐一回复的权利」。
> 新增闸 `tools/gate_g8.mjs`(**20 条,含 6 条真阴性对照**),其中 **G8-STRUCT** 钉的是
> 「经 `/api/save` 存一次只改一个词之后,`<style>` / 所有 `class` / `style=` / 换行剖面逐字节不变」。
> 完整交付说明见 [`RECEIPT-X122-G8.md`](./RECEIPT-X122-G8.md);**8 条诚实边界**见其 §7,
> 最要紧的一条:**我们的页面从此不再逐像素等于 owner 原版** —— 原版实测也是 24px,它自己就没按
> 自己的样式表长过;现在等于的是「原版样式表本来想要的样子」= support 的样子,**等 owner 过目确认**。

> **X123 颗粒 3:已完成并上线(2026-09-08)。App 的「联系我们」工单后端搬到了这里。**
> owner 令:**跳绳 App 相关的一切不再涉及 App server 与老域名,只用免费 Cloudflare。**
> 于是这个站除了四张法律页,现在还跑着四个接口(`/api/feedback/{contact,thread,reply,sendlog}`):
> 用户提交反馈 ⇒ 以 GitHub App `xiaotiantian-feedback` 的身份在私有仓库 `cnaron/rope-counter`
> 开一个 issue ⇒ owner 收 GitHub 通知、直接在 issue 里回话 ⇒ App 拉得到、还能再回。
> **不发邮件**(GitHub 自己会通知),**不依赖任何自有服务器**。
> 端到端在生产上跑通了(含「开发者回一句 → App 看到 dev 消息且 cid>0」这条上一轮没验到的链路),
> 完整交付说明见 [`RECEIPT-X123-G3.md`](./RECEIPT-X123-G3.md);
> **App 侧要改的两处(base URL + `X-RC-Key` 新值)见其 §6,老服务器的退场清单见其 §8。**

> **X122 颗粒 6:已完成并上线(2026-09-08)。「改一个词,线上只变那个词」现在是有闸把着的。**
> owner:「编辑是生效了,但是好像总体行间距却改变了」⇒ 查下来**编辑链路是干净的**:
> 他那次编辑一个字节都没被改坏,隐私页/协议页删前删后**逐像素相同的行占 100.00%**,
> 行高数字前后一模一样。真凶是**浏览器缓存**——旧缓存头里的 `stale-while-revalidate=86400`
> 允许浏览器把最长 24 小时前的旧页先画出来;他编辑前看的其实是 09:38 的旧页,一保存被迫
> 取新,于是**一次跳了两个版本**(颗粒 5 的全文改写 + 他自己删的那行),看着就像整页重排。
> 修法:缓存口径改 `max-age=0, must-revalidate`(真浏览器正反对照实测:旧头保存后刷新还是
> 旧页,新头立刻是新页);另加 `tools/gate_g6.mjs` **15 条可重跑闸**(含 4 条阴性对照)。
> 顺带把 owner 那三处只落在 KV 的删改**同步回 `content/` / `docs/` / GitHub 备份站**。
> 完整交付说明见 [`RECEIPT-X122-G6.md`](./RECEIPT-X122-G6.md);**两条提醒**见其 §7。

> **X122 颗粒 5:已完成并上线(2026-09-08)。四页内容逐句核实到 App 现状。**
> owner:「把内容都核实一遍,不要展示官网,价格和当前保持一致,同时也要加入可编辑的页面」
> ⇒ 颗粒 4 那张「原版与现状差异表(10 处)」**已逐条改成真话**,每条都落到 iOS 源码行号
> (预注册 [`PREREG-X122-G5.md`](./PREREG-X122-G5.md) §1 的 24 条事实清单)。
> 最要紧的三处:**视频留存说实话**(每局本地留一条,只留最新一局,可回看/导出,不上传)、
> **订阅制改买断五档**(¥28 / ¥48 / ¥128,升级 ¥98 / ¥78,**不自动续费、无免费试用**)、
> **权限 2 条补到 6 条**。另按 owner 令:**站上 `gugushizi.com` 已清零**(改前 6 处),
> 主页「关于本站」那段技术/托管说明**整节删除**,全站不再出现「托管 / GitHub / Cloudflare」等字样。
> 七条判据全绿(三条文案闸带真阴性对照)。**样式表/外壳/字体一个字节没碰** ——
> privacy/terms 的 `<head>`(整套内联 CSS)改前改后逐字节相同。
> 完整交付说明见 [`RECEIPT-X122-G5.md`](./RECEIPT-X122-G5.md);**三件待 owner 过目**见其 §6。

> **X122 颗粒 4:已完成并上线(2026-09-08)。隐私政策 / 用户协议改用 owner 原版页面。**
> owner:「你这个隐私政策跟原版还是有差距呀,直接用原版代码就好了呀」⇒ 不再自己重写,
> 把原版两页**整篇搬过来**,只删页脚版权行 + 把两处外链资源搬成同源。
> 并排逐像素实测:**除被删的底栏外全等**。见 [`RECEIPT-X122-G4.md`](./RECEIPT-X122-G4.md)。
> ⚠️ 那一轮登记的「原版与现状 10 处出入」**已由颗粒 5 全部处理**,此告警作废。

> **X122 颗粒 3:已完成并上线(2026-09-07)。站内多了一个带口令的文档编辑器。**
> 打开 **<https://xiaotiantian-app.pages.dev/edit>** → 输口令 → 左边改字、右边同步看
> 「发布出来长什么样」→ 点「保存并发布」,**约 3 秒**线上就换了,不用碰 GitHub。
> 口令在 Air `~/Downloads/xiaotiantian-legal-site/EDIT-PASSPHRASE.txt`。
> 完整交付说明见 [`RECEIPT-X122-G3.md`](./RECEIPT-X122-G3.md)。
>
> **⚠️ 正文的权威来源从此是 Cloudflare KV,不再是本仓库的 `content/`。**
> owner 在网页上改完之后,仓库里的 `content/` 与 `docs/` 就落后了;
> GitHub Pages 备份站(`cnaron.github.io/xiaotiantian-legal/`)**不会自动跟随**,
> 只当应急。要把线上内容倒回仓库,见回执 §7「怎么同步回 GitHub 备份站」。
>
> 颗粒 2 遗留、仍等 owner 拍板一项:① 样式取「按样式表写的值」还是「照抄老页面实际长相」。
> (② 联系邮箱 `003` 还是 `009` —— **X122 颗粒 7 起不再是问题**:owner 2026-09-08 裁决
>  **外部四页一律不暴露任何邮箱地址**,联系方式统一写成「请在 App 内「小天天 → 联系我们」
>  提交,我们会在 App 内回复」。)
>
> ⚠️ 另有一个内容已过时的重复站 `xiaotiantian-legal.pages.dev` **仍挂着邮箱地址**,
>  与上面这条裁决直接冲突,**等 owner 裁:删掉,还是也发一份**。

iOS 应用「小天天练跳绳」(App ID `com.playtime.ropecounter`)的**公开静态页面**,
供 App Store Connect 填写「隐私政策 URL」「支持 URL」,以及 App 内设置页链接使用。

## 两条改文案的路(颗粒 3 起)

| | A. 网页编辑器(owner 日常用) | B. 改仓库再发布(改样式/结构时用) |
|---|---|---|
| 入口 | <https://xiaotiantian-app.pages.dev/edit> | 本仓库 `content/` + `template/` |
| 改的是 | Cloudflare KV 里的正文 | 仓库源文件 |
| 生效 | 点「保存并发布」约 3 秒 | `python3 build.py` → `git push` → `source tools/cfenv.sh && npx wrangler pages deploy` |
| 影响面 | 只换正文 | 外壳/样式/新页面都能改 |

⚠️ **两条路会互相盖**:走 B 重新导入 KV(`python3 tools/kv_import.py`)会把 owner
在网页上改的内容盖掉;反过来只发布代码(`wrangler pages deploy`)不会动 KV,是安全的。
要先把线上内容拉回仓库,见回执 §7。

⚠️ **走 A 改完之后,记得也走一次「拉回仓库」**(`curl -b <cookie> …/api/export` → 覆盖
`content/` → `python3 build.py` → push),否则改动只活在 KV 里:仓库、静态回退页和 GitHub
备份站都还是旧的。颗粒 6 就是在清这笔账(owner 09-08 那三处删改此前只在 KV)。

## 缓存口径(颗粒 6 定,别改回去)

> ⚠️ 颗粒 8 补一句:owner 后来又说了一次「行间距变了」,那次**与缓存无关**(成因是层叠顺序,
> 见上面颗粒 8 那段)。**同一个现象可以有两个独立成因** —— 下次别查到缓存就收手。


四个正文页的响应头是 `Cache-Control: public, max-age=0, must-revalidate`,
**不许出现 `stale-while-revalidate` 或 `s-maxage`**。原因:那两个东西允许浏览器/边缘
把旧页先画出来,实测能让人看到 **68 分钟前**的版本 —— owner 的「改了一个词整页行间距变了」
就是这么来的(他看的「改之前」其实是两个版本以前的页)。字体 `/assets/*` 的一年长缓存
**保持不动**(文件名带内容指纹,长缓存是对的)。
详见 [`RECEIPT-X122-G6.md`](./RECEIPT-X122-G6.md) §3–§4。

## 构建(2026-09-07 起)

**整页模式(颗粒 4 起)**:`privacy` / `terms` 在 `content/pages.json` 里带 `"fullpage": true`,
它们的 `content/*.html` 就是**整篇文档**(自带 `<html>`/`<head>`/内联 `<style>`),
外壳 `template/shell.html` 与 `template/style.css` 对这两页**完全不生效**。
要重新从原版生成,跑 `python3 tools/adopt_original.py <原版html> <dumpcss产物> content/<page>.html`。

`docs/` 是**构建产物,不要直接手改**。源在:
`template/shell.html`(外壳)+ `template/style.css`(样式)+ `content/<page>.html`(正文片段)
+ `content/pages.json`(标题/日期)+ `template/render.js`(渲染器),
改完跑 `python3 build.py` 重新生成 `docs/` 与 `functions/_lib/` 里的产物。
`build.py` 会自检三件事,任一不过就构建失败:渲染器三份副本 sha256 一致 /
正文用到的 class 都在渲染器白名单里 / **Function 侧组装出来的四页与 `docs/*.html` 逐字节相同**。

单测:`node tools/test_render.mjs`(25 条,含 7 类 XSS 阴性对照)。

## 页面(docs/ 下)

| 文件 | 内容 |
|---|---|
| `index.html` | 目录页(静态网站索引文档)|
| `privacy.html` | owner 原版隐私政策整页(整页模式,不套外壳);**正文已按颗粒 5 核实到 App 现状,2026-09-08 生效** |
| `terms.html` | owner 原版用户协议整页(整页模式,不套外壳);**正文已按颗粒 5 核实到 App 现状,2026-09-08 生效** |
| `support.html` | 支持与帮助(常见问题 + 联系方式 —— 导流到 App 内工单,**不写邮箱地址**) |
| `404.html` | 错误文档 |
| ~~`style.css`~~ | **已删**:样式在构建时内联进每页(零外链) |
| `assets/noto-sans-sc.css` + `assets/fonts/*.woff2` | 自托管中文字体(101 分片 4.5 MB),privacy/terms 用。**是 vendored 资产,不是构建产物,`build.py` 不动它** |

## 托管(**两处,内容必须保持一致**)

| 站点 | URL 形态 | 发布方式 |
|---|---|---|
| **Cloudflare Pages**(★ 主用,填 ASC) | `https://xiaotiantian-app.pages.dev/privacy`(**无 `.html`**,直接 200 零跳转) | mini 上 `npx wrangler pages deploy docs --project-name xiaotiantian-app` 直传(**不接 GitHub 仓库**) |
| **GitHub Pages**(备份) | `https://cnaron.github.io/xiaotiantian-legal/privacy.html` | `git push` 到 `main`,**`/docs` 目录**发布 |

⚠️ Cloudflare 会把 `.html` 后缀 308 跳转掉;GitHub Pages 不会。两套地址形态不同,内容相同。
⚠️ **改文案要两边都发**:改 `content/` → `python3 build.py` → `git push` + 重跑一次 `wrangler pages deploy docs`。
⚠️ **还有一个内容已过时的重复站** `https://xiaotiantian-legal.pages.dev/` —— 本轮按 owner 令未动,
  它仍是 2026-09-07 改版**之前**的内容(写着「不保存视频」「自动续费」)。**等 owner 裁:删掉,或也发一份。**
**发布目录 = `docs/`**(只含对外文件)。两处托管都只发 `docs/`,
仓库根的 `README.md` / `RECEIPT-X122.md` 属内部文档,**不会被公网访问到** ——
这也是 owner 日后在 Cloudflare 里「Connect to Git」时能安全自动发布的前提
(输出目录填 `docs`)。

## 反馈接口契约现状(X129-C1 起,单向;X132-B 补记「当前默认值」)

`POST /api/feedback/contact` 是新契约(2026-09-10 起)—— 立即回 `{"ok":true}`,不给工单号/token,
用户**只能发,收不到回复**;`thread` / `reply` / `sendlog` 三条路由已下线。以下两条是**当前实现的
默认值**,不是写死不能改的规则,owner 想改随时可以:

- **KV 只留一种反馈相关键**:`ticket:<deviceId>` → GitHub issue 号(用于把同一设备的多次反馈接进
  同一条工单)。没有消息正文、没有发送记录留在 KV 里。
- **静默上限**(超限一律照回 200,用户无感知):全站 **300/天**、单设备 **5 次/10 分钟**。
  改法:改 `functions/_lib/feedback.js` 里的 `RATE_GLOBAL_DAY_20260908` /
  `RATE_MAX_20260908.contact` 两个常量后重新部署。

完整背景、诚实边界与并发收敛机制见 [`RECEIPT-X129-C1.md`](./RECEIPT-X129-C1.md) §3/§8。
法律站三页(support/privacy/terms)的「回复」「工作日」措辞已在 X132-B(2026-09-11)统一改成
「只接收、不逐一回复」,不再与上面这条单向契约冲突,详见 [`RECEIPT-X132-B.md`](./RECEIPT-X132-B.md)。

**★ X132-E(2026-09-11)—— 标题 = 最新一条**:同一设备**首条**留言,issue 标题按老规则
= `titleFromDesc(desc, …)`(用户原话前 60 字 + 省略号,空描述兜底成 `[反馈] <当次北京时间>`)。
**这台设备之后每追加一条**(走 comment 路径,不是新开单),标题会被 `PATCH` 成**这一条**的
同一套规则结果(内容 + 这一条自己的北京时间日期)—— 换句话说,GitHub 通知列表里的标题永远是
「这台设备最后说了什么」,不是「最早说了什么」。没有另外的敏感信息剥离/长度规则:标题只有
`titleFromDesc` 这一套加工,首条追加条都复用同一个函数,超时/限流导致的 `PATCH` 失败静默吞掉
(评论已经落地、用户已经拿到 200,不会因为改标题失败而回滚或报错)。代码在
`functions/api/feedback/contact.js` 的 `retitleIssue_claudecode_20260911`。

## 约定

- **纯静态、零脚本、零外域**:公开页没有 JavaScript、没有统计 / 广告 / 第三方资源、不设置 Cookie。
  颗粒 4 起 privacy/terms 会额外取一份**同源**字体(`assets/noto-sans-sc.css`)——
  仍然一个外部域名都不连,但不再是「每页一个自包含文件」了。
  渲染器在整页模式下**结构性**保证这点:白名单里没有 script/img/link/iframe,
  唯一的 `<style>` 口子由 `checkCss()` 挡住外域 `url()` 与 `@import`;`build.py` 另有一道零外域自检。
- **页面之间用相对链接**(`./privacy.html`)⇒ 换域名 / 换托管商不需要改任何文件。
  (代价:Cloudflare 上站内点击会多一次 308;给 ASC / App 的入口 URL 一律用无后缀形式,零跳转。)
- **内容必须与 App 实际行为一致**:改 App 行为(权限、视频留存、付费档位)时,
  必须同步改本仓库对应段落并更新页顶版本号与生效日期。
  ✅ **颗粒 5(2026-09-08)起四页都满足这一条**:每句话都能指到 iOS 源码里的出处,
  查不到出处的三条断言(±5% 误差 / proration / 「本 App 不联网」)已删除而非改小。
  文案闸 `python3 tools/gates_g5.py docs` 可随时复跑(NOSITE / PRICE / TECH 三条)。
  ✅ **颗粒 9 起 `TECH_ALLOW_20260908` 已清空**:颗粒 7 为「必须点名反馈接收方」开的 5 条豁免,
  其对象已被颗粒 9 整段删除 ⇒ 豁免作废,`G5-TECH` 恢复满强度(词表未放宽)。**别再往里加豁免** ——
  要加之前先问:是不是这句话本来就不该写在法律页上?

## 事实基线(2026-09-08 复核,逐条有代码出处 —— 见 PREREG-X122-G5.md §1)

- 权限:**法律页写 3 条**(相机 / 健康写 / 健康读声明);**App build 118 包里仍是 5 条**
  (多出相册读 `NSPhotoLibraryUsageDescription` + 相册写 `NSPhotoLibraryAddUsageDescription`)。
  ⚠️ **这两个数现在对不上,而且是不利方向**(App 申请、政策不提)——X123 颗粒 8 按 owner 令
  先改的政策,**提审前必须由 X121 侧把这两条权限与「保存到相册」功能删掉**。见 `RECEIPT-X123-G8.md` §5①。
  (**X122 颗粒 10(2026-09-09)起**:麦克风那条已随 App build 117 一并下线 —— App 侧不申请权限、
   录制不写音轨、导出产物读回 0 音轨,见 `wt-v3design/docs/RECEIPT-X121-G18-20260909.md`。
   ⇒ 法律四页搜 `麦克风` 必须 **0** 命中,改前 7;视频口径统一为「只有画面,不含声音」)
  ⇒ 法律四页搜 `相册|Photos|照片` 必须 **0** 命中,改前 11(X123 颗粒 8)
- 每局生成一条本地视频,**单槽只留最新一局**,不上传
- 付费:1 年 ¥28、3 年 ¥48(非续期订阅,到期不自动续费)、永久 ¥128;
  升级 1 年→永久 ¥98、3 年→永久 ¥78
- 免费档:体测模式累计 3 次、历史最近 3 条、趋势近 3 天(Pro 近 7 天)
- **联网 = 「联系我们」三处**(提交反馈 / 拉取回复 / 追加回复,全部用户动作触发);除此之外全程不联网。
  后端是 Cloudflare Pages Functions,以 GitHub App 身份开**私有仓库工单**,**不发邮件、不经自建服务器**
  (X121 颗粒 13 / X123 颗粒 3)。提交携带:版本/机型/系统/语言/会员档/最近一局/各项权限开启状态/脱敏日志/
  购买快照(含 Apple 原始交易号)/渠道快照,以及**App 自己生成、只存本机钥匙串的设备编号**(非 IDFA/IDFV/UDID)。
  **X123 颗粒 8(2026-09-09)起不再有 IP**:服务端一处都不读 `CF-Connecting-IP`,工单正文没有「来源 IP」行,
  限流键也不再以 IP 建。⚠️ 上面这张清单里,**语言设置 / 权限开启状态 / 购买与渠道快照**三样
  **没有**写进对外新措辞(那段只点名 6 样)—— 两个处理选项待 owner 裁,见 `RECEIPT-X123-G8.md` §5②。
  ⚠️ **以上是内部事实,X122 颗粒 9 起对外四页不再写这些实现细节**(owner 令):公开页只写
  「会收到您填写的内容与排查所需的技术信息,只用于回复与排查」+ 入口 + 时效。
  尺子:四页搜 `Cloudflare|GitHub|工单|issue|发送记录` 必须 **0** 命中(改前 13)
- **外部四页不写任何邮箱地址**(owner 2026-09-08 裁决,X122 颗粒 7):联系方式一律写成
  「请在 App 内「小天天 → 联系我们」提交,我们会在 App 内回复」。尺子:四页搜
  `@126.com|@qq.com|邮箱` 必须 **0** 命中(改前 7)
- 记录可逐条删除;「设置→开发选项→清空所有记录」整段包在 `#if DEBUG`,上架包不编译
- 间歇训练(HIIT)**在 UI 上没有入口** ⇒ 四页一律不提
- 内置评分标准 6 套:国家标准(教育部)/ 北京 / 上海 / 广东(广州)/ 江苏(南京)/ 浙江(杭州)
