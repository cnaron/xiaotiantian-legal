# X129 颗粒 C1 · 「联系我们」服务端改单向 · 回执

> 状态:**已上线并在生产上逐条验过**,2026-09-10 12:5x 北京时间。
> 预注册 [`PREREG-X129-C1.md`](./PREREG-X129-C1.md)。
> 新契约一句话:`POST /api/feedback/contact` 校验完**立刻**回 `{"ok":true}`,其余全在后台;
> `thread` / `reply` / `sendlog` 三条路由**不存在了**;KV 里与反馈有关的只剩
> `ticket:<deviceId>` → issue 号这一条索引。
> 真链路自证:[#21](https://github.com/cnaron/rope-counter/issues/21) ·
> [#22(被自动并走)](https://github.com/cnaron/rope-counter/issues/22) ·
> [#23 / #24(第二层收敛)](https://github.com/cnaron/rope-counter/issues/24)
>
> ⚠️ 本仓库是**公开**仓库(`cnaron/xiaotiantian-legal`)。口令 / key 一律不写进来。

## 大白话解释

**做了什么。** owner 今天改了口径:用户在 App 里点「联系我们」,**想发就发,点一下立刻就回**,
我们只管查收,**不再在 App 里回复用户**。照这个口径,服务端做了三件事:
① 点提交之后,服务器只检查「钥匙对不对、有没有写字」,立刻回一个「好的」;审核、找帖子、
写帖子这些活全挪到后台背着用户做。**实测从平均 7 秒变成 1 秒**。
② 用户写的话**一个字都不再存在 Cloudflare 的小仓库(KV)里**,只写进 GitHub 的帖子;
仓库里原来存着的 191 条发送记录(含用户原话和 App 日志)也**当场删干净了**。
③ 「看对话」「再回一句」「owner 的发送记录页」三个网址整个删掉,现在跟从来没存在过一样。
另外,审核不过的、超上限的,**悄悄扔掉**,用户那边看起来也是「发出去了」——这是 owner
明确要的「不需要让用户感知」。

**中途翻了两次车,都在这份账里。** 立刻回话之后,用户连点两下会有**两个后台任务同时跑**,
结果同一台手机开出了两个帖子(实测两次,#12/#13 和 #14/#15)。我第一反应是加一把「开单中」
的锁,主控当场纠正:**这把锁在原理上就靠不住**(Cloudflare 那个小仓库不保证「先看一眼再写」
是一气呵成的,两个人可以都看到空、都写成功)。于是改成**事后收敛**:开完帖子回头数一遍,
同一台手机要是有两个帖子还开着,就把号大的那个并进号小的、关掉、留一句「已并入 #N」。
第一版收敛也没生效(GitHub 那边刚开的帖子要几秒才查得到),又补了两层:开单后等 5 秒再数;
以及**这台手机以后每发一条消息都顺手数一遍**——这一层不看时间,所以哪怕这次漏了,
下一条消息到来时也一定合并。**最终口径:并发时允许短暂出现两个帖子,但下一条消息之后必然合成一个。**

**要 owner 拍板的两件事。** (a) KV 里我留了「这台手机 → 哪个帖子」的对照表(7 条),它不是
消息,是「同一台手机的话接在同一个帖子后面」的定位手段;要 KV 彻底空也行,代价是每次得去
GitHub 搜,索引有延迟,重复帖会变多。(b) 那个用户看不见的上限沿用现值:全站 300 条/天、
单台手机 5 条/10 分钟。

**还发现一件必须请 owner 定的事。** 法律站(隐私政策 / 用户协议 / 支持页)里现在**白纸黑字
写着「我们会在 App 内回复」「通常 14 个工作日内回复」**,而新契约是**不回复**。这几句话
现在是不准确的。**我没有改**,清单在 §6,请 owner 定怎么改。

---

# 1. 新契约(给线 A 的 X129-G3 用)

| 项 | 旧 | 新 |
|---|---|---|
| 提交 | `POST /api/feedback/contact` | **不变** |
| 请求头 | `X-RC-Key: <值>` | **不变**(值没轮换) |
| 请求体 | `desc/contact/deviceId/app/version/build/device/os/locale/plan/payment/…` | **一个字段都没变** |
| 成功响应 | `{"ok":true,"ticket":{"id","token","createdAt","mode","cid"}}` | **`{"ok":true}`** —— 只剩这一个字段 |
| 响应时间 | 实测 7.84s / 6.06s | 实测 15 次 **p50 = 1.04s**(最快 0.36s) |
| key 错 | `403 {"ok":false,"err":"forbidden"}` | **不变** |
| 请求体坏 / `desc` 为空 | `400 {"ok":false,"err":"bad_request"}` | **不变** |
| 限流 | `429 rate_limited` | **没有了**,永远 200(上限转为静默丢弃) |
| 审核不过 | `400 rejected` | **200 + 静默丢弃** |
| 拉黑 | `403 blocked` | **200 + 静默丢弃** |
| `GET /api/feedback/thread` | 拉整条对话 | **不存在**(404,与从来没有过的路径逐字节相同) |
| `POST /api/feedback/reply` | 用户追加一句 | **不存在**(POST 收 405,同上) |
| `GET /api/feedback/sendlog` | owner 的发送记录页 | **不存在** |

**App 侧只需要做一件事**:别再读响应体里的 `ticket` / `token` / `cid`,别再调 thread / reply。
提交成功的判据 = `HTTP 200` 且 `ok === true`。

# 2. 删了什么 / 留了什么

## 2.1 路由与文件(删)

| 文件 | 为什么删 |
|---|---|
| `functions/api/feedback/thread.js` | App 不再展示对话 |
| `functions/api/feedback/reply.js` | 追加改成「再提交一次 contact」,由 issue 侧按设备号接在一起 |
| `functions/api/feedback/sendlog.js` | 那页的数据源就是 KV 里的用户正文,而这一轮 KV 不留消息 |
| `functions/_lib/ticket.js` | 只有上面两条在用 |

随之变成死码一并清掉:`cleanId` / `hmacHex` / `ticketToken(Valid)` / `firstPostBody` /
`classifyComment` / `stripImageMarkdown` / `logBodyOn` / `stampId` / `sendlogAppend` /
`noteRejection` / `ensureBlockLabel`,以及 `REPLY_*`、`SENDLOG_KEEP_SEC`、`USER_REPLY_PREFIX`
等常量。**这些「真的不在了」由单测 §5 的模块表面积尺子钉住(带阳性对照)。**

## 2.2 KV(删)

| 前缀 | 是什么 | 改前 | 改后 |
|---|---|---|---|
| `sendlog:` | **发送记录:用户原话 + App 日志**,存 30 天 | 191 | **0**(写入删掉 + 存量清空) |
| `fbtest:` | 测试件(测试模式下的一份消息副本) | 28 | **0** |
| `fbimgday:` | 上一轮图片能力的残留计数 | 7 | **0** |
| `feedback:log_body` | 「要不要落用户正文」的开关 | 1 | **0**(没有正文可落了) |
| `fbrate:thread:10` | 已下线路由的限流窗口 | 1 | **0** |
| **总键数** | | **327** | **103** |

> ★ 存量的 226 条不是「等它过期」,是当场 `--remote` 批量删掉的。收工时又补删了 2 条
> —— 那 2 条是**我自己在改动前打基线**时旧代码写下的,不在最初那份快照里(见 §7④)。

## 2.3 KV(留)

| 键 | 是什么 | 为什么留 |
|---|---|---|
| `ticket:<deviceId>` → issue 号(7 条) | **索引**,不是消息 | 「同一台设备的话接在同一个 issue 后面」的定位手段。**★ 假设 1,待 owner 确认**,见 §3 |
| `fbrate:contact:<deviceId>` / `fbrate:global:<日期>`(5 条) | 静默上限的滑动窗口 | 保护 GitHub / Workers AI 免费额度。**★ 假设 2,待 owner 确认**,见 §3 |
| `ghtok*`(1 条) | GitHub App installation token 缓存(50 分钟) | 鉴权,不是消息 |
| `labels:<issue>`(2 条) | 某条 issue 的标签缓存(5 分钟) | 拉黑判定用,不是消息 |
| `history:` / `page:`(88 条) | **法律页正文与编辑历史**,与「联系我们」无关 | 本颗粒一个字没碰 |

# 3. ★ 两条待 owner 确认的假设

## 假设 1:KV 里留 `ticket:<deviceId>` → issue 号 这一条索引

- **我的判断**:它不是消息,是「唯一 ID 追加写入」这件事的定位手段。存的是一个设备编号
  和一个数字,没有任何用户写的内容。
- **如果 owner 要 KV 彻底零**:备选是每次去 `/search/issues` 搜设备号。**价签**:GitHub 搜索
  索引有几十秒延迟 ⇒ 同一台设备短时间内的两条消息会各开一个 issue(靠 §4 的事后收敛并回来,
  但 owner 会先看到重复帖再看到它被并走),而且每次提交多一次 GitHub 调用。

## 假设 2:静默上限的数值(用户看不见,超了就丢)

- **全局日限 300 条/天**、**单设备 5 条/10 分钟** —— 一个数都没动,只是从「回 429」改成「静默丢」。
- 存在的唯一理由是保护免费额度(GitHub API、Workers AI neurons、KV 写次数)。
- owner 觉得太紧/太松,改两个常量即可:`RATE_GLOBAL_DAY_20260908` / `RATE_MAX_20260908.contact`。
- **★ 代价**:超上限的那条消息用户和 owner **都不会知道**。

# 4. ★★★ 重复工单:两次翻车与最终处置

## 4.1 事故

立刻回 200 之后,同一台设备的两条提交会有**两个后台任务并行**。生产实测**两次**开出重复工单:
`#12 / #13`(第一次)、`#14 / #15`(把占锁时机往前挪之后,又一次)。

## 4.2 我错在哪(主控当场纠正)

我的处方是「加一把 KV 占位锁」,并且在第一次没挡住之后去**调时机**。主控指出:
**Cloudflare KV 是最终一致存储,「读到空 → 写入」不是原子操作**,两个并发 worker 完全可以
都读到空、都写成功。所以那两次重复**不是时机没调对,是原语选错了** —— 再怎么挪占锁的位置,
也只是把窗口做小,不可能做没。这条判断在第三次实测上又被证实了一次(见 4.4 的 `#19 / #20`)。

## 4.3 最终处置(选主控给的方案 a,不选 b)

| 层 | 做什么 | 性质 |
|---|---|---|
| ① 占位锁 | 读完 `ticket:` 立刻写 `lock:<时间戳>` 进同一个键,开完用真号覆盖;别的岔路在 `finally` 里删掉 | **只降低概率**,不保证任何事(KV 无原子性) |
| ② 开单后收敛 | 等 5 秒再列一次「这台设备所有 open 工单」,**号最小的留下**,我不是最小号就把自己并进去 | 仍在跟 GitHub 列表接口的读副本延迟赛跑,**也只是降概率** |
| ③ **每条后续提交都收敛** | 追加路径上再跑一次同样的收敛,**不等待** | **不依赖时间** ⇒ 这一层才是保证 |

**为什么不选 b(Durable Object 按 deviceId 串行化)**:DO 能给真原子(免费计划现已支持
SQLite 存储的 DO),但要新增一个绑定、一套部署面和一个新的失败模式,而这一轮刚把服务端收到
「一条路由 + KV 只存一条索引」。事后收敛不引入任何新基础设施,也不与「KV 不存消息」冲突。
**价签摆在这儿,owner 要真原子随时可以换 DO。**

**收敛规则是纯函数式的**(列出该设备所有 open 工单 ⇒ `Math.min` ⇒ 谁不是最小号谁并自己),
两个并发请求各算一次结论必然相同,不需要任何协调 —— 这是**收敛**不是**互斥**。

## 4.4 因此「同设备第二条接在同一帖子」这条闸的通过口径改了

> **并发首帖允许短暂重复;任一后续提交之后必须收敛为一个 open issue。**

原来那条「第二条必须落在同一个 issue 上」的写法在并发下**结构上不可达**,继续用它只会
逼出调参凑答案。

# 5. 闸账(预注册 §2 的 10 条 + 事故后补的 2 条)

| 闸 | 判定 | 证据 |
|---|---|---|
| **C1-FAST** | 🟢 | 旧版实测 **7.84s / 6.06s**;新版 15 次真实提交 **p50 = 1.04s**(区间 0.36–1.76s)。响应体逐字节 `{"ok":true}` |
| **C1-ISSUE-NEW** | 🟢 | 新设备提交 ⇒ 30 秒内出 [#21](https://github.com/cnaron/rope-counter/issues/21),标题 = 用户原话 |
| **C1-ISSUE-APPEND** | 🟢 | 第二条 ⇒ 同一 issue 多一条 `[用户消息 2]`(cid=5613100513,作者 `xiaotiantian-feedback[bot]`) |
| **C1-REOPEN** | 🟢 | `gh issue close 21` ⇒ `state=closed`;再发一条 ⇒ `state=open` 且 `comments` 1→2 |
| **C1-MOD-SILENT** | 🟢 | 禁词一条 ⇒ **HTTP 200**;#21 评论数 **2 → 2**,最新 issue 号 **22 → 22**。**阴性对照**:同设备上一条正常消息把评论数从 1 顶到 2 ⇒ 这把尺子不是恒绿 |
| **C1-NO-THREAD** | 🟢 | 参照物 = 一条**从来不存在**的路径 `/api/feedback/__never__`:GET 回 404 + 4086 字节。`thread` / `sendlog` GET **状态码与正文逐字节相同**;`reply` POST 回 405 + 0 字节,与**同样用 POST 打那条不存在路径**逐字节相同 |
| **C1-KV-CLEAN** | 🟢 | 全程 `--remote`。总键数 **327 → 103**;`sendlog:` 191→**0**、`fbtest:` 28→**0**、`fbimgday:` 7→**0**;跑完整轮验证后**没有一个新的记录类键** |
| **C1-UNIT** | 🟢 | `node tools/test_feedback.mjs` **184/184**(改前 182)。删掉四整节(被测对象随路由一起没了),新增第 ⑮ 节 |
| **C1-SECRET** | 🟢 | `git grep` 现役 `X-RC-Key` 值扫**全历史** ⇒ **0 处**。**阴性对照**:搜一个确实在仓库里的串(`reconcileDeviceIssues`)⇒ 命中 2 个文件 |
| **C1-LEGAL** | 🟢 | 四页 + `/edit` 部署前后**逐字节相同**:index 4935 / privacy 21635 / terms 21016 / support 10257 / edit 1918 字节 |
| **C1-CONVERGE**(事故后补) | 🟢 | 真并发两条 ⇒ 开出 #21 / #22 ⇒ **#22 自动关闭**并留下 `[系统] 重复工单,已并入 #21`,#21 收到那条用户消息,KV 索引 = **21** |
| **C1-CONVERGE-2**(事故后补) | 🟢 | 人为造出 #23(小号)/ #24(大号)并把索引指向 #24 ⇒ 一条普通提交之后:**#24 关闭**并留合并说明、#23 收到消息、索引 = **23**、仅剩一条 open |

**没跑的**:无。**红的**:无。
**★ 中途红过三次**,都在 §7 诚实栏里:`#12/#13`、`#14/#15`、`#19/#20`。

# 6. ★ 法律站文案:与新契约对不上的句子(**只列不改**,请 owner 定)

新契约是**不回复用户**,而这三页现在写着「会在 App 内回复」「14 个工作日内回复」。
**正文的权威源是 KV**(X122 颗粒 3 起),所以改这些要走 `/edit` 编辑器或 `tools/kv_import.py`,
不是改仓库文件就完事 —— 这也是我不擅自动它的原因之一。

| # | 页 | 位置 | 原句(节选) |
|---|---|---|---|
| 1 | support | 「联系方式」 | 请在 App 内「小天天 → 联系我们」提交,**我们会在 App 内回复** |
| 2 | support | 「回复时效」 | 提交后,我们通常会在 **14 个工作日内**通过 App 内「联系我们」回复 |
| 3 | support | 该节末尾小字 | **开发者的回复会直接回到那一页的对话里,您也可以在同一条对话里接着回复** |
| 4 | support | 「不需要注册」那段 | ……为**回复**与排查所必需的基本信息……**只用来回复您和排查问题**;同时会带一个只用于把您的几次反馈**接进同一条对话**的编号 |
| 5 | privacy | §「唯一的例外是联系我们」 | ……为**回复**与排查所必需……**只用于回复您和排查问题**……仅用于把同一台设备的多次反馈**接进同一条对话**的编号 |
| 6 | privacy | 联系方式 | ……请在 App 内「小天天 → 联系我们」提交,**我们会在 App 内回复** |
| 7 | privacy | 联系方式末尾小字 | 提交后,我们通常会在 **14 个工作日内**通过 App 内「联系我们」回复 |
| 8 | terms | §联系我们 | ……请在 App 内「小天天 → 联系我们」提交,**我们会在 App 内回复** |
| 9 | terms | 该节末尾小字 | 提交后,我们通常会在 **14 个工作日内**通过 App 内「联系我们」回复 |

**我的判断(不是决定)**:第 1/2/3/6/7/8/9 条现在是**不准确的承诺**,建议改成
「我们会查看每一条反馈,但不逐一回复」一类;第 4/5 条里「接进同一条对话」的说法可以保留
(GitHub 那边确实是接在同一条工单上),但「用于回复您」这半句需要跟着改。
**另外**:隐私政策原来那条「不保存用户反馈内容」与旧版的发送记录矛盾(X123-G3 §7⑤ 提出过、
owner 未裁)——**这一轮把发送记录整个删了,那个矛盾自动消失了**,可以顺手在文案里确认。

# 7. ★★★ 诚实栏(做错的 / 没做到 / 边界)

## ① 我把「加锁」当成了正确性方案,主控纠正之前连错两次

第一次(#12/#13)之后我加了 KV 占位锁;第二次(#14/#15)之后我**又去调时机**。
正确的判断是主控给的:**KV 读后写没有原子性,这条路子在原理上就到不了「保证」**。
教训写在这儿:**遇到并发,先问「我用的原语有没有原子性」,而不是「窗口能不能再小一点」。**
占位锁最后留下来了,但它的定位已经降级为「降低概率」,§4.3 的表里写死了。

## ② 「等 5 秒再收敛」也只是降概率

第二层(开单后等 5 秒)本质上还是在和 GitHub 列表接口的**读副本延迟**赛跑。
真并发实测 `#19/#20` 就是它输的那一次:两个任务几乎同时列表,**各自都只看到自己刚开的那条**。
所以真正兜底的是第三层(每条后续提交都收敛),它不看时间。**如实写:①②都只降概率,③才是保证。**

## ③ 被并走的那条工单里,那条用户消息有一份副本

收敛的顺序是「先追加到索引指向的那条 → 再发现自己是大号 → 补到小号上并关掉自己」,
所以那条消息在**大号(已关闭)和小号(open)上各有一份**。owner 在 open 列表里只会看到一条,
但点进已关闭的重复帖能看到同样的话。判断:不值得为它多绕一次 GitHub 往返去删。

## ④ 我打基线时用旧代码写了 2 条发送记录,收工才发现

清 KV 的那份「待删清单」是**改动前**列的,而我在那之后又用**旧版**打了两条基线请求
(为了量改动前的响应时间)—— 那两条写进了 `sendlog:`,不在清单里。收工核对时数出
`sendlog: 2` 才发现,已补删,终态 0。**教训:清单和产生数据的动作要按时间对齐,
「改前快照」不等于「删干净」。**

## ⑤ 静默丢弃 = 丢了就是丢了

后台任何一步不过(超静默上限 / 拉黑 / 审核不过 / GitHub 拿不到 token / 开单失败),
用户与 owner **都不会知道**。旧版至少还有发送记录页能查。这是 owner「不需要让用户感知」
口径的直接后果,不是疏漏,但值得在这儿写第二遍。
**尤其**:Llama Guard 已实测会抖(见 `_lib/moderation.js` 顶部),复核机制只降低误伤概率,
被误伤的用户会以为自己发出去了。

## ⑥ 拿不到 GitHub token 时不再排队

旧版会把内容排进 KV 的 `fbq:` 等下次补发。排队就是存消息 ⇒ 撤掉。GitHub App 鉴权挂掉那段时间
的反馈**直接丢**。

## ⑦ 没有 issue 的设备拉黑不了

「连拒 3 次自动拉黑」整段撤了(它要在 KV 里记拒绝次数)。拉黑现在**只有一条路**:
owner 在 GitHub 上给这台设备的 issue 贴 `blocked` 标签。一台还没开过 issue 的设备
在 GitHub 上没有可贴标签的对象 ⇒ 拉不了。已写成单测断言,免得日后当 bug 查。

## ⑧ 本轮在 owner 的仓库里留下的痕迹

真 issue **#11–#24**(14 条,含 4 条手工造的对照件),**全部已 close + 打 `test` 标签**;
收工时 open 的只剩 [#10](https://github.com/cnaron/rope-counter/issues/10)(owner 的真实用户工单,一个字没动)。
KV 里本轮造的 6 条 `ticket:` 测试索引已全部删除。

## ⑨ 别的边界

- **`TICKET_SECRET` 这个 Pages secret 现在没人用了**(工单 token 和 sendlog 口令都随路由退役)。
  我**没有删**它 —— 删 secret 是不可逆操作,交 owner 定。留着没有风险,只是一个死配置。
- 收敛每次多一次 GitHub 列表调用;最坏路径 `waitUntil` 里要等 5+5 秒。
  Workers `waitUntil` 上限 30 秒,留了一倍余量,**这笔账写在常量注释里**,改大之前先重算。
- 并发压测只做到 **2 路**。3 路以上没测过。
- 本轮**没有轮换** `X-RC-Key`,线 A 不需要改 key,只需要改「别读响应体里那些字段」。

# 8. 运维速查(给 owner,替换 X123-G3 §9)

| 想做什么 | 怎么做 |
|---|---|
| 看有哪些反馈进来了 | `cnaron/rope-counter` 里 `user-feedback` 标签的 **open** issue。GitHub 会给你发通知 |
| 回复用户 | **回不了** —— 新契约是单向的。在 issue 里写的东西 App 看不到,只是你自己的备份记录 |
| 屏蔽某台设备 | 给它的 issue 贴 `blocked` 标签,最多 5 分钟生效;撕掉标签即恢复 |
| 看到一条「[系统] 重复工单,已并入 #N」 | 正常现象:并发时短暂开出的重复帖被自动并走了,内容在 #N 里 |
| 暂时不开真工单 | `npx wrangler kv key put --remote --namespace-id <LEGAL_CONTENT> "feedback:test_mode" "on"`(**必须带 `--remote`**);恢复就 `delete` 掉 |
| 改静默上限 | 改 `functions/_lib/feedback.js` 的 `RATE_GLOBAL_DAY_20260908` / `RATE_MAX_20260908.contact` 后重新部署 |
| 部署 | `cd legal-site && source tools/cfenv.sh && npx wrangler pages deploy`(部署后**约 1 分钟**才切到新 bundle) |
| 跑单测 | `node tools/test_feedback.mjs` |
