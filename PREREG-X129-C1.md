# X129 颗粒 C1 · 「联系我们」服务端改单向 · 预注册

> 写在动手之前。owner 2026-09-10 12:10 改口径:用户只管发,我们只管查收,**不作响应处理**。

## 大白话解释

**要做什么。** 现在用户在 App 里点「联系我们」提交,服务器要先做一堆事(审核、找 GitHub 帖子、
写记录),做完才回话,慢的时候要等好几秒;而且我们把用户写的话在 Cloudflare 的小仓库(KV)里
也存了一份,还提供「看对话 / 再回一句」两个接口。owner 现在的口径变了:**用户想发就发,点一下
立刻就回,后面的活我们背地里慢慢做;我们不再在 App 里回复用户**。

**所以这一轮要改成。** ① 点提交 ⇒ 服务器只检查「钥匙对不对、有没有写字」,立刻回一个「好的」,
其余全部放到后台跑。② 用户写的话**一个字都不再存进 KV**,只存进 GitHub 的帖子(一台设备一个帖子,
以后每条消息接在后面)。③ 「看对话」「再回一句」这两个接口整个删掉。④ 审核不过的,**悄悄扔掉**,
用户那边看起来也是「发出去了」——这是 owner 明确要的「不需要让用户感知」。⑤ 限速对用户不再可见
(永远回「好的」),但为了不把 GitHub 的免费额度打爆,后台还留一个**看不见**的上限,超了就丢。

**结论会是什么。** App 那边只需要「发出去」,不需要读服务器回什么;服务器上不再有用户消息的副本;
owner 查收的地方只剩 GitHub 的 issue 列表。

**要 owner 确认两件事(写在回执里,不替 owner 拍板)。** (a) KV 里我留了**一条**东西:
「这台设备 → 哪个帖子」的对照表 —— 它不是消息,是「同一台设备的话要接在同一个帖子后面」这件事
的定位手段;要是 owner 要求 KV 彻底空,备选是每次去 GitHub 搜索,代价是搜索索引有几十秒延迟,
连着发两条会开出两个帖子。(b) 那个看不见的上限数值(全站 300 条/天、单台设备 5 条/10 分钟)是
沿用现值,owner 可以改。

---

## 1. 新契约(目标态)

| 项 | 旧 | 新 |
|---|---|---|
| `POST /api/feedback/contact` | 同步做完审核+开单,回 `{ok,ticket:{id,token,createdAt,mode,cid}}` | **校验后立刻** `{"ok":true}`,其余进 `waitUntil` |
| 用户可见 429 | 全局日限 / 单设备限流 | **没有了**(永远 200) |
| 审核不过 | `400 rejected` | **200 + 静默丢弃**(不写 issue、不留痕) |
| 拉黑 | `403 blocked` | **200 + 静默丢弃** |
| `GET /api/feedback/thread` | 拉整条对话 | **删除**(路由不存在) |
| `POST /api/feedback/reply` | 用户追加一句 | **删除** |
| `GET /api/feedback/sendlog` | owner 看发送记录页 | **删除** |
| KV 里的消息 | `sendlog:*`(正文+日志,30 天)、`fbtest:*`、`fbq:*` | **一条都不写** |
| KV 里保留 | — | `ticket:<deviceId>` → issue 号(索引,**待 owner 确认**)、`fbrate:*`(静默限流)、`ghtoken*`(鉴权缓存)、`labels:<n>`(拉黑标签缓存) |
| issue 形态 | 一台设备一个 issue,后续消息一条 comment,关掉后再来消息自动 reopen | **不变** |

## 2. 逐条判据(闸)

| 闸 | 判什么 | 通过标准 |
|---|---|---|
| C1-FAST | 立即返回 | 生产实打,p50 响应时间显著低于旧版(旧版首帖实测 7.26s、后续 1–2s);响应体逐字节 `{"ok":true}` |
| C1-ISSUE-NEW | 新设备开单 | 打完 60 秒内 GitHub 出现新 issue,标题=用户原话 |
| C1-ISSUE-APPEND | 同设备追加 | 第二条 ⇒ **同一个 issue** 多一条 `[用户消息 2]` comment,**不新开** |
| C1-REOPEN | 关掉后再来 | `gh issue close` 后再打一条 ⇒ 该 issue 回到 open 且多一条 comment |
| C1-MOD-SILENT | 禁词静默 | 禁词一条 ⇒ HTTP 200 且**该设备 issue 评论数不变 / issue 总数不变**(阴性对照:同设备正常一条 ⇒ 评论数 +1) |
| C1-NO-THREAD | 路由下线 | `thread` / `reply` / `sendlog` 的响应与一条**从来不存在**的路径(`/api/feedback/__never__`)不可区分(状态码 + 正文逐字节) |
| C1-KV-CLEAN | KV 不存消息 | 全程 `--remote` list:跑完之后 `sendlog:` / `fbtest:` / `fbq:` 前缀键 **0 个新增**;总键数前后各记一次 |
| C1-UNIT | 单测 | 全绿;删掉的功能对应单测一并删,新契约的补上;报改前/改后条数 |
| C1-SECRET | 公开仓库无密钥 | `git grep` RC_KEY / secret 字面值 0 处(带阴性对照) |
| C1-LEGAL | 不碰法律页 | 四页 + `/edit` 部署前后逐字节相同 |

## 3. 明确不做

- 不改 App(线 A 的 G3);不改法律页正文(⑥ 只列出不改);不动 `/edit`、KV 里的页面正文;
  不碰 App server / gugushizi.com;不新增第三方服务。
- 审核被拒**不在 issue 里留痕**(owner:不需要过程记录)。
- GitHub 拿不到 token 时**不再排队**(排队要存正文进 KV)⇒ 那一条留言直接丢。代价登记在回执。

## 4. 已知代价(先写下来,免得事后说成设计)

1. **丢了就是丢了**:立刻回 200 之后,后台失败(GitHub 抽风、审核误伤、超静默上限)用户与 owner
   **都不会知道**。旧版至少有发送记录页能查。这是 owner「不需要让用户感知」口径的直接后果。
2. **审核误伤不可申诉**:Llama Guard 已实测会抖(见 `_lib/moderation.js` 顶部注释),被误判的
   用户以为发出去了,其实没有。复核(问两次)保留,但只降低概率不消除。
3. **`ticket:<deviceId>` 若被清**:同一台设备会新开一个 issue(GitHub 搜索兜底有几十秒延迟)。
