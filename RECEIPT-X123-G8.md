# RECEIPT · X123 颗粒 8 —— 工单不再收 IP + 隐私政策去 IP / 去相册权限(5 → 3)

> 仓库 `cnaron/xiaotiantian-legal`(**公开**),分支 `main`,起点 `c0b418f`(X122 颗粒 10)。
> 预注册 [`PREREG-X123-G8.md`](./PREREG-X123-G8.md)(动手前写的,含所有「改前」分母)。
> 线上部署:`e906b3c5.xiaotiantian-app.pages.dev`(production 别名已指向它)。

## 简短大白话

**owner 说了两件事:一、隐私协议里"联系我们会带上您的 IP 地址"这句别写了,干脆别收;
二、现在不上传图片视频、也不用保存到相册,相册权限不该申请、也不该提。看了页面截图之后又加一条:
那一段罗列得太细("运行日志(其中的电子邮件地址与手机号已在发送前抹去)"这种),反而像收了很多敏感信息,
改成一段说人话的。**

这轮做完的事:

1. **服务端是真的不收了,不是把文案删掉。** 工单正文里的「来源 IP」那一行没了;
   原来服务器用 IP 当「这是谁发的」来防刷,现在改用**手机自己生成的设备编号**(提交时)和
   **工单号**(追加回复时)。**能发几条的上限一个都没改。**读 IP 的那几个函数是**删掉**,不是留着不用。
2. **网页上的话跟着改了。**隐私政策删掉 IP 那句、删掉「相册读取」「相册写入」两条权限(**5 条变 3 条**)、
   删掉框架清单里的 Photos 那行、撤回权限的路径去掉「照片」;目录页「5 项系统权限」改「3 项」;
   支持页那两句「保存到相册」改成「分享」。「联系我们」那一段换成 owner 给的新措辞,一个字没自己发挥。
3. **验过了,不是"应该没问题"。**真的开了一条工单(GitHub 上的 #9,已关闭并打了 test 标签),
   正文里搜「IP」是 **0**;同一把尺子量改之前那条工单(#8)是 **1**。防刷还在:同一台设备连发 6 条,
   第 6 条被拦(429),换一台设备第 1 条照样能发。网页那边,四张页面在**三个地方**
   (数据库正文 / 线上看到的 / 编辑器里)都搜不到那些词,改之前一共 18 处。

**★ 有一件事要 owner 现在就知道:手机 App(build 118)还在申请相册权限**,结果页上也还有
「保存到相册」按钮。所以现在是「App 要权限、政策不提」——**这个方向对审核不利**,
必须先由 X121 那边把相册读/写两条权限和保存功能删掉,再提审。详见 §5①。

**★ 还有一件事请 owner 定:**新措辞里点名的是「App 版本、设备型号、系统版本、会员状态、
最近一局、用于排查的运行记录」六样,但服务器实际还会收到**语言设置、各项权限的开启状态、
购买与渠道快照(含 Apple 原始交易号)**。要么认为「基本信息(……)」是举例、就这么放着;
要么加半句概括。**我没有替您改**,两个选项写在 §5②。

---

# 1. 改了什么

## 1.1 服务端(6 个文件)

| # | 文件 | 改动 |
|---|---|---|
| A1 | `functions/api/feedback/contact.js` | issue 正文删 `['来源 IP', maskIp(ip)]` 整行;限流主体 → **设备编号**;限流位置从「解析之前」挪到「解析出设备编号之后」 |
| A2 | `functions/api/feedback/reply.js` | 限流主体 → **工单号**(放在 token 校验之前,挡住"没票的人反复触发 GitHub API")|
| A3 | `functions/api/feedback/thread.js` | 同上 |
| A4 | `functions/api/feedback/sendlog.js` | 限流主体 → 常量 `owner`(这页只有 owner 看,本来就是单主体)|
| A5 | `functions/_lib/feedback.js` | **删掉** `clientIp_claudecode_20260908` / `maskIp_claudecode_20260908`;`rateAllow` 的形参 `ip` → `subject`;新增兜底桶常量 `RATE_SUBJECT_NONE_20260909 = 'nodev'` |
| A6 | `functions/_lib/auth.js` + `functions/api/login.js` | **删掉** `clientIp` / `failCount(ip)` / `bumpFail(ip)` / `clearFail(ip)` / `FAIL_LIMIT`;`/edit` 登录防爆破改成「全局计数 + 递增延时,不硬锁」(§4)|

**限流阈值一个没动**:contact 5 / thread 30 / reply 5 / sendlog 60,窗口 600 秒,全局 300 条每天。

KV 键从 `fbrate:<桶>:<IP>` 变成 `fbrate:<桶>:<主体>`。线上实测(§3.3)现存 6 条 `fbrate:` 键里
**IP 形状 0 条**,改前同一把尺子在同样的请求之后能列出 `fbrate:contact:<IPv4>`。

## 1.2 法律站(9 处:privacy 6 · index 1 · support 2 · terms 0)

| # | 页 | 位置 | 改动 |
|---|---|---|---|
| B1 | privacy | §一「唯一的例外是「联系我们」」整段 | 换成 **owner 14:50 给定的措辞**(逐字照抄):删 IP 句、删「应用运行日志(其中的电子邮件地址与手机号已在发送前抹去)」这类括号说明、罗列收敛成「基本信息(…)」|
| B2 | privacy | §一.2「回看视频」 | 「也可以主动保存到相册或分享出去」→「也可以主动分享出去」|
| B3 | privacy | §三 设备权限调用 | **整块删**「相册读取权限」`<li>`(5 行)|
| B4 | privacy | §三 设备权限调用 | **整块删**「相册写入权限」`<li>`(5 行)|
| B5 | privacy | §二 系统框架清单 | **整行删**「**Photos:**用于(在您主动操作时)从相册选取视频、或把本次视频保存到相册。」|
| B6 | privacy | §六 撤回权限 | 路径「相机 / 照片 / 健康」→「相机 / 健康」|
| B7 | index | 隐私政策入口描述 | 「**5 项**系统权限」→「**3 项**」|
| B8 | support | 常见问题 3 | 「您也可以主动把它保存到相册或分享出去」→「您也可以主动分享出去」|
| B9 | support | 常见问题 10 | 同一份罗列的第二份副本,跟着 B1 收敛(去掉「语言设置 / 相关权限的开启状态 / 已抹去联系方式的运行日志与购买状态」与「(不是广告标识符,也不是 Apple 的设备 ID)」)|
| — | terms | — | **0 处**:改前尺子就是 0(**查过为空,不是没查**;`ASSERT_CLEAN` 会验它,§3.4 有逐字节证据)|

生效日期已是 **2026年9月9日**(颗粒 10 改的)⇒ **本轮不动**。
「可回看」「分享出去」**保留** —— 分享走系统分享面板,不需要相册权限,App 侧仍有分享。

改法沿用颗粒 9/10:`tools/edits_x123g8_claudecode_20260909.py`,每条 = (文件, 唯一行锚点, 新整行),
**锚点命中数必须恰好 1**,否则报错退出;整块删除另断言「`<li>…</li>` 区间里没有第二个 `<li>`」。
可从任意基线重放:`python3 tools/edits_x123g8_claudecode_20260909.py --dry --src <目录>`。

## 1.3 改后的 3 条权限

| 隐私政策条目 | 对应包内权限串 |
|---|---|
| 相机权限 | `NSCameraUsageDescription` |
| Apple 健康(HealthKit)写入权限 | `NSHealthUpdateUsageDescription` |
| Apple 健康(HealthKit)读取声明 | `NSHealthShareUsageDescription` |
| ~~相册读取权限~~ | ~~`NSPhotoLibraryUsageDescription`~~ —— **App build 118 里还在**,见 §5① |
| ~~相册写入权限~~ | ~~`NSPhotoLibraryAddUsageDescription`~~ —— **App build 118 里还在**,见 §5① |

线上页面实测条目数 **= 3**(从 `<h3>3. 设备权限调用</h3>` 那个 `<ul>` 里数 `<li><strong>`)。

---

# 2. owner 追加令(14:50)那段的处理

## 2.1 逐字照抄,只把原有的 `<strong>` 放回原位

owner 给的是纯文字,页面上那段原本有四处加粗。加粗是排版不是文字 ⇒ 按原来的位置放回去:
「唯一的例外是「联系我们」。」/「主动点击提交」/「只用于回复您和排查问题,不用于任何其他目的,
不用于营销,也不会提供给第三方」/「全部由您的操作触发」。**文字一个字没动、没加没减。**
(原版「填写内容并 <strong>主动点击提交</strong> 时」两侧有空格,渲染出来会多两个空格;
owner 给的文本没有,所以空格去掉了 —— 这是让渲染结果与 owner 写的**逐字一致**,不是改文案。)

## 2.2 support 那份副本一起收敛

同一份罗列在 support 常见问题 10 里还有一份(「语言设置、会员档位、……已抹去联系方式的运行日志与购买状态」)。
只改 privacy 会留下两处互相打架的说法 ⇒ 按同样口径收敛。

## 2.3 ★ 一处我**没有**照「手机号 = 0」执行,请 owner 过目

追加令里的尺子是「IP / 邮箱 / 手机号词 = 0」。四页里「手机号」现在还剩 **1 处**,在 privacy §一第一段:

> 我们 **不收集** 用户的任何个人身份信息,包括但不限于:姓名、**手机号**、身份证号、生日、学校、班级……

这句是**反方向的陈述**——它说的是"我们不收集手机号",删掉等于自己把一条对用户有利的承诺抹了。
所以我把尺子按「**描述"收集了什么"的段落里 = 0**」执行,并在改动脚本里写成一条**显式白名单**
(`PHONE_ALLOW`),不是漏掉。若 owner 认为这句也要拿掉,一行就能改。
其余词(IP 地址 / 相册 / Photos / 照片 / 电子邮件 / 邮箱 / 抹去 / 运行日志)四页三面**全 0**。

---

# 3. 闸账 —— 预注册 17 条,实跑 **19 条:18 绿 · 1 条没跑**

## 3.1 服务端

| 闸 | 结果 | 数字 |
|---|---|---|
| **G8-SRV-GREP** | 🟢 | `grep -rn "CF-Connecting-IP\|X-Forwarded-For" functions` = **0**(改前 **2**) |
| **G8-SRV-FN** | 🟢 | `grep -rn "clientIp\|maskIp" functions` = **0**(改前 **10 行**);三个函数是删掉,不是留着不用 |
| **G8-SRV-ISSUE** | 🟢 | 真路径新开 issue **#9**,正文 `IP` 命中 **0**;**阴性对照**:同一把尺子量 #8(改前代码开的)= **1** |
| **G8-SRV-RATE** | 🟢 | 同一设备连发 6 条:200×5 + **429**(阈值没变) |
| **G8-SRV-RATE-ISO** | 🟢 | 设备 A 打到 429 后,设备 B 第 1 条 **200** ⇒ 键真按主体分。**顺带是"IP 桶真没了"的正证**:这 7 条请求全来自同一个出口 IP,换成改前的代码,第 6 条起就该被 IP 桶拦死 |
| **G8-SRV-KVKEY** | 🟢 | 跑完闸后 `fbrate:` 共 6 条,**IP 形状 0 条**;`login_fail:` 前缀 **0 条**。阴性对照:改前跑同样请求后能列出 `fbrate:contact:<IPv4>`(**1** 条) |
| **G8-SRV-UNIT** | 🟢 | `node tools/test_feedback.mjs` **182 条全过**(改前 177 + 新增 5) |
| **G8-SEC**(取代旧 `G-SEC`) | 🟢 | 见 §4 |

## 3.2 法律站

| 闸 | 结果 | 数字 |
|---|---|---|
| **G8-L-SCAN3** | 🟢 | `IP 地址\|相册\|Photos\|照片\|电子邮件\|邮箱\|抹去\|运行日志` 在 **KV 正文 / 线上渲染 / 编辑器** 三面各 **0**;阴性对照改前 KV **12 + 1 = 13**、编辑器面 **18** |
| **G8-L-PERM3** | 🟢 | privacy §三 条目 **3** 条(改前 5),标题恰为 相机 / 健康写入 / 健康读取声明 |
| **G8-L-INDEX3** | 🟢 | index 「3 项系统权限」1 处,「5 项」0 处 |
| **G8-L-TERMS0** | 🟢 | `content/terms.html` 与 `docs/terms.html` sha256 = `792df4f20fd1d5a6…`,**与改前逐一相同** |
| **G8-L-DIFFSET** | 🟢 | 按 `<section>` 切块比 sha:privacy 10 块只变 3(一/二/六)、index 3 块变 1、support 5 块变 1、**terms 12 块变 0** —— 与 §1.2 预期集合一一对上 |
| **G8-L-CASCADE** | 🟢 | `node tools/gate_g8.mjs` **5 条全绿**(行高仍 28.799999px,含它自己的阴性对照) |
| **G8-L-G5** | 🟢 | `python3 tools/gates_g5.py docs` NOSITE / PRICE / TECH 各 **0 命中** |
| **G8-L-BUILD** | 🟢 | `python3 build.py` 三项自检过(渲染器三份 sha 一致 / class 白名单 16=16 / 四页逐字节 parity);`node tools/test_render.mjs` **46 过**;`node tools/gate_g6.mjs` 绿 |
| **G8-L-KVSYNC** | 🟢 | 发布后 KV 正文与 `content/*.html` 四页 sha256 **逐一相同** |
| **G8-L-MIRROR** | 🟢 | 主站 / GitHub Pages 备份站 / 仓库 `docs/` **三者四页逐字节相同**(push 后自动追平,没单独发布) |
| **G8-EDIT-ONLINE** | ⚪ **没跑** | 带口令去线上 `/edit` 抓页面 —— **口令在 Air,不在 mini**(与颗粒 3/6 同一限制)。编辑器面是用**线上 KV 的真字节 + 编辑器真代码**(`functions/_lib/editor.js` 的 `editorPage`)在本地量的,见 §3.5 |

## 3.3 服务端实测原文(节选)

```
设备A #1..#5 -> 200      设备A #6 -> 429      设备B #1 -> 200
fbrate: 共 6 条 | 桶 contact ×3(主体 G8DEVICE-AAA / G8DEVICE-BBB / X123G8-VERIF)
                | 桶 global ×2(20260908 / 20260909) | 桶 thread ×1(主体 7)
★ IP 形状的键: 0
issue #9  -> 0        issue #8(阴性对照) -> 1
```

## 3.4 terms 一个字节没动的证据

`docs/terms.html` 的 sha256 在本轮改前(`c0b418f`)与改后完全相同:`792df4f2…`;
同时 `<section>` 逐块比 sha **12 块 0 变**;线上、备份站、`docs/` 三者又逐字节相同 ⇒
线上那份 terms 也没动。

## 3.5 编辑器面怎么量的(以及为什么它算数)

没有口令进不去线上 `/edit`。但编辑器页面的内容 = `page:<name>` 这个 KV 键的正文
喂进 `editorPage()` 渲染 —— 所以我把**线上 KV 拉下来的真字节**喂进**仓库里那份真的 `editorPage`**,
量它的输出:四页各 **0**。
**阳性对照**:同一把尺子量**改前**的 KV 字节 ⇒ privacy **15** / support **3**,合计 **18** 命中。
只会说「0」的尺子没有信息量,这条对照是为了证明它两个方向都动得了。
闸脚本:`tools/gate_x123g8_editorface_claudecode_20260909.mjs`。
**这条不能替代 G8-EDIT-ONLINE**:它证明「编辑器代码 + 当前 KV 字节 ⇒ 0」,
不证明「线上那台机器上跑的就是这份代码」(后者由 G8-L-MIRROR / 部署 id 间接支撑,但没有直接量)。

---

# 4. `/edit` 登录防爆破:换了机制(本轮唯一一处"换机制",请 owner 过目)

**旧**:同一 IP 连错 5 次 ⇒ 15 分钟内**即使口令对也 429**。这条依赖 IP 当主体,和 owner 令冲突。

**新**:全局失败计数 + 递增延时,**不硬锁**。口令**对**的请求 0 延时、永远进得去;
口令**错**的请求按"最近 15 分钟全局失败次数"延时 `250ms × n`,**封顶 4 秒**。

为什么不做「全局硬锁」:端点是公开的,任何人连按 5 次错口令就能**把 owner 自己锁在门外**,
这是白送的 DoS。为什么不按 cookie:清一下 cookie 就绕过了。三个候选的完整比较在 PREREG §3。

**线上实测(G8-SEC)**:连错 16 次,**16 次全是 401,一次 429 都没有**;
耗时从第 1 次的 1.92s 涨到第 16 次的 6.61s。把全局计数键删掉(回到 n=1)之后再错,
耗时回落到 2.69s / 2.43s ⇒ **多出来的 3～4 秒就是延时本身**,不是网络抖动
(mini 走 TUN 到首尔,基线 RTT ≈ 1.9～2.5s)。计数键回读值 **= 16**,与请求次数对上。

**诚实的局限**:并发爆破可以绕过延时(每条请求各自延时,不串行)。真正的防线仍然是
口令强度(19 字符,熵 ≈ 79 bit)+ 常数时间比较。旧的按 IP 硬锁对换 IP 的攻击者本来也无效,
对分布式攻击**新做法反而更有针对性**(计数是全局的)。

⚠️ **一条我没能直接验的**:「口令对 ⇒ 200 + 下发 cookie」这条路没跑过(口令在 Air)。
它在**结构上**不可能被本轮改坏的理由:①`login.js` 里现在**一个 429 分支都没有**(可 grep);
②成功分支那一行(`json({ok:true},200,{Set-Cookie:…})`)本轮**一个字符没动**;
③改前改后 `import` 都能解析(`node -e "import(...)"` 五个模块全 OK)—— 引用了不存在的导出会在这一步就炸。
**但这仍然是"论证"不是"测量"**。owner 下次进 `/edit` 时如果进不去,回滚点是 commit `703d41f`。

---

# 5. 要 owner 决定 / 别的执行线接手的

## ① ★ App 与政策现在不一致,而且是**不利方向**(必须先修再提审)

`wt-v3design/ios/Resources/Info.plist` 现在仍有:

```
NSPhotoLibraryAddUsageDescription  用于把本次跳绳视频保存到你的相册。
NSPhotoLibraryUsageDescription     用于让你直接从相册选一条视频……
```

`MeasurementResultExamView.swift:640` 还有「保存到相册」按钮,`MeasurementViewModel.saveToPhotos`
仍会 `PHPhotoLibrary.requestAuthorization(.addOnly)`,`X77PhotoPicker` 还在。

⇒ 现在的状态是「**App 申请权限、政策不提**」。颗粒 10 走的是安全顺序(App 先删、政策后改),
**这次是反的**(owner 令要求政策先改)。**提审前必须由 X121 侧删掉这两条权限与相册相关功能。**
在那之前,这个不一致就摆在这里,我不替它掩饰。

## ② ★ 新措辞点名 6 样,服务端实际还会收到 3 样没点名的

新措辞点名的:App 版本、设备型号、系统版本、会员状态、最近一局、用于排查的运行记录。
服务端实际还会收到并写进工单:**语言设置**、**各项权限的开启状态**、**购买与渠道快照(含 Apple 原始交易号)**。

两个选项(**我没有替您选**):

- **A. 就这么放着** —— 把「为回复与排查所必需的基本信息(……)」读成举例,不是穷举。省事,但严格说不完整。
- **B. 加半句概括** —— 例如把括号末尾改成「……和用于排查的运行记录等」,或加「以及少量用于核对权益的购买信息」。
  一行改动,`tools/edits_*.py` 加一条就行。

**不建议的第三条路**:把这几样从 App 端不发了。issue #7 那次会员档矛盾的根因,靠的正是购买快照
——删了以后同类问题查不动。

## ③ 工单正文里的「权限」那行还在列麦克风和相册

issue #9 的诊断折叠块里仍是「相机 granted / 麦克风 - / 相册读 granted / 相册写 granted / 健康 …」。
这是服务端**原样渲染 App 发上来的字段**:麦克风那格 App 从 build 117 起就不发了(所以显示 `-`),
相册两格等 X121 删完也会变成 `-`。**服务端不需要改**;等 App 侧删干净之后可以把这两格从
`permissionText` 里去掉(顺手活,不急)。

## ④ reply / thread 失去了"按 IP 拦陌生人"这一层

主体换成工单号之后,拿到 `X-RC-Key` 的人可以换工单号绕开单主体窗口(RC_KEY 在 App 二进制里,
视同半公开)。contact 那条还有全局 300/天 兜着,**reply / thread 没有全局兜底**。
本轮**没擅自加新阈值**(任务书写了"阈值不变")。要补的话就是给这两条也接上 `globalDayAllow`,
或者单开一个更宽的全局桶 —— 请 owner 裁。

## ⑤ contact 限流的计数时机变了

原来"请求体解析之前"就计数(垃圾请求也算),现在要先解析出设备编号才能计数
⇒ **JSON 坏掉/超长的请求不再计入单主体窗口**,只被全局 300/天 计入。
这是"主体在请求体里"的必然代价,PREREG §5③ 事先写了。

---

# 6. 三个可复用的坑

## ① ★★★ 同一把「剥 HTML 注释」的尺子,在三个面各遇到一种不同的转义形态

- 线上渲染面:`<!-- … -->` 原样
- 编辑器 textarea 面:`&lt;!-- … --&gt;`
- 编辑器把正文灌进 JS 字符串那一段:`\u003c!-- … -->`(`<` 被转成了 `\u003c`)

只剥第一种,`owner 令:外部页面一律不写任何电子邮件地址` 这句**注释**会被当成命中,
尺子就会在两页上报红。我在编辑器面上先后被这个绊了**两次**(先只剥原样,再只加 `&lt;`)。
处方:**先把两种转义还原成 `<`/`>` 再剥注释**,并且注释本身单独过一把尺子(不许出现邮箱地址形状)。
——与 X122-G8「取 `<style>` 前必须先剥 HTML 注释」是同一族的坑,但那次只涉及一种形态。

## ② ★★ `grep` 在这个仓库的 `tools/test_feedback.mjs` 上**默默返回空**

`file` 认它是 `data`(文件里有让 macOS grep 判成二进制的字节),于是 `grep -n "rateAllow" 文件`
**什么都不输出、退出码还是 1** —— 看起来像"这个文件里没有这个符号",实际有 6 处。
用 `grep -a` 才对。**教训:在这个仓库里,"grep 没命中"不能直接当成"不存在",先确认 `file` 的判定。**

## ③ ★★ 删函数要连"谁在用"一起删完,否则 `node --check` 会给你一个假绿

我一次把 `auth.js` 改完、`login.js` 没改成功(脚本前半段抛错、后半段没跑),
`node --check functions/api/login.js` 照样**通过** —— 语法检查看不见"import 了一个已经不存在的导出"。
是 `grep clientIp functions/ = 2` 这条闸把它逮住的。之后补了 `node -e "import('./…')"` 逐模块真加载,
才是能抓这类问题的尺子。

---

# 7. 收尾状态

- 测试工单 **issue #9**:已 `close` + 打 `test` 标签。本轮真路径只开这 1 个。
- 本轮闸造的 KV 键**已清并回读核对**:`fbtest:` 11 条 → 0、`ticket:X123G8-VERIFY-0909` → 0、
  `login_fail:` → 0(三个前缀回读均 0 条)。`fbrate:` 里剩的是闸留下的设备桶,600 秒自然过期。
- 改前基线抓在 `/Users/cc/x123/g8/baseline/`(线上四页 + KV 四页),改后在 `/Users/cc/x123/g8/after/`
  与 `mirror/`。**都在 mini 上,没有进这个公开仓库。**
- 本回执与仓库里**没有任何口令**:`X-RC-Key` / sendlog key / CF 凭证一律不写(交接项见 `/Users/cc/x123/DONE-G3`)。
- 部署:`e906b3c5.xiaotiantian-app.pages.dev`(production 别名已指向);备份站随 `git push` 自动追平,没单独发布。
